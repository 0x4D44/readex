# HLD — mdrcel M9: Differential-Fuzz Hardening (V4 — FINAL, ratification-ready)

**Date:** 2026-05-22
**Author:** Arthur (via supervisor agent, Claude Opus 4.7)
**Status:** RATIFICATION-READY — **V4**. Three review rounds (simplicity / Rust-feasibility /
test-methodology / devil's-advocate); Round-3 found **zero CRITICAL issues** across all lenses. V4
folds in the final reviewer-proposed MINOR refinements. Awaiting only Arthur's §14 ship-vs-harden
ruling.
**Parent:** `2026.05.16 - BRIEF - Rust Content Extraction Library.md`,
`2026.05.19 - HLD - mdrcel Trafilatura Port (M3).md`
**Runs concurrently with:** M8 metadata parity. M9 is supervisor-led.

## Revision note (V1 → V2 → V3)
**V1 → V2 (Round-1):** fixed two oracle-drift seams (per-doc URL; minimiser config vs cache) and a
factual mask-duplication error; deferred the minimiser, triage-skill, and signature taxonomy;
collapsed to one harness; added a Stage-0 falsification spike, a config-locked oracle path with
cache-integrity, and a fidelity KPI; escalated ship-vs-harden to Arthur.

**V2 → V3 (Round-2):** Round-2 found the Stage-0 gate — V2's headline "teeth" — under-defined and
biased (a CRITICAL). V3: (1) **reframes a Stage-0 NO-GO as an *effort/yield* gate, never a proof of
faithfulness**; (2) **sharpens the content filter to KEEP malformed/unusual markup** (in-scope, prime
bug territory) and drop only *non-content* pages; (3) **pre-registers numeric go/no-go thresholds
counted on *new-class* loci after discounting the three known clusters**; (4) lightly stratifies the
spike sample; (5) makes the false-merge split criterion **mechanical** (diff-shape-hash count) rather
than odds-reducing; (6) **freezes the content filter** so the KPI can't be gamed, and notes the KPI
certifies the *web distribution*, not the hard corpus; (7) gives §14 **teeth** — Arthur's
ship-vs-harden ruling is a *precondition* to building past Stage 0, not a silent default; (8) folds in
two MINOR prerequisites (extract a shared oracle fn in run.py; three distinct mask shapes).

**V3 → V4 (Round-3, all MINOR):** Round-3 found no CRITICAL issues. V4 (1) **sets the split parameter
K = 1** (any second distinct diff-shape splits a group), removing an undefended tunable; (2) makes the
diff-shape-hash's one residual seam **self-detecting** — triage records any single diff-shape that
resolves to > 1 confirmed root cause, which signals the hash needs refining (so the closure is
self-correcting, not blindly trusted); (3) notes Stage 0 uses a *provisional* filter that Stage 3
freezes.

---

## 1. The design in one paragraph

Our fidelity is proven on **51 hand-curated snapshots** — structurally "testing on the training set."
M9 widens that to **low-thousands of real-world pages from a Common Crawl slice** and runs the
existing **one-reference byte-diff** (mdrcel vs pinned Python `trafilatura`) at scale. But before
building any apparatus, a **Stage-0 spike** (§6.0) cheaply tests M9's own premise — *do thousands of
new pages reveal new root-cause clusters, or just re-hit the three we already know?* If the latter, we
stop. If it pays off, divergences funnel through a **mechanical-group-then-batch-LLM** triage into the
existing 3-bucket discipline; only **minimised reproducers of confirmed, legitimate-content bugs are
promoted** into the committed gate corpus. No new heavyweight deps, no feature flags, no env vars;
opt-in is `#[ignore]` + explicit invocation.

---

## 2. Goal & non-goals

**Goal.** Find port-fidelity bugs the curated corpus cannot surface, triage them efficiently, fix the
real ones, and lock each as a permanent regression — *only if* Stage 0 shows the bug yield is real.

**Non-goals (explicit):**
- **Not** a quality/benchmark exercise; **one-reference only** (mdrcel vs trafilatura). The three-way
  readability comparison answers a different question and belongs to a later track.
- **Not** metadata (that's M8 — §10 coordination).
- **Not** tens-of-thousands scale; **no** persistent oracle server, sharding, or distributed fetch.
- **Not** solving the known deferrals (blake2b fingerprint, U+2063); the pipeline *masks* them.
- **Not** testing charset detection or JS-rendering — both are explicitly the *caller's* job per the
  founding brief. Inputs are decoded to UTF-8 once and both sides see identical bytes (§5.2, §12).

---

## 3. Decisions locked

| Decision | Value | Rationale |
|---|---|---|
| Corpus source | Common Crawl slice | Realistic distribution for unknown-unknowns |
| First-run size | Low thousands | Stresses fidelity + triage without server-grade throughput |
| Reference count | One (mdrcel vs trafilatura) | Fidelity question |
| Input strategy | Real-world harvest; **no** pure-random; mutation only if Stage 0 justifies | Bug-density per input |
| Triage | Mechanical group → **ad-hoc batch LLM** → supervisor ratifies | Skill-ify only once stable |
| Minimiser | **Deferred** — build only if manual reduction proves too slow | No bug exists yet |
| Bulk corpus | Ephemeral; manifest-reproducible; git-ignored | Repo-weight; reproducible |
| Permanent artifacts | Minimised reproducers of *confirmed, legitimate-content* bugs | Fuzzing feeds the gate |
| **Milestone gate** | **Stage-0 spike go/no-go (§6.0)** | Falsify the premise cheaply first |

---

## 4. Pipeline & data flow

```
STAGE 0 (cheap, gates everything): few-hundred CC pages, existing per-doc oracle, masks OFF
        --> count distinct first-diff loci + garbage-rate --> GO / NO-GO (§6.0)

if GO:
[CC WARC slice] --harvest--> fuzz_corpus/<sha>.html      (run ONCE, config-locked oracle)
                              + manifest.jsonl (sha,url) --batch oracle--> oracle_cache.jsonl
                                                                            (+ integrity header)
   (fast, Rust, re-runnable without Python)                                      |
                                                                                 v
                          [diff (identical html+url both sides) + classify + group on read]
                                                                                 |
                                                              divergences.jsonl  (group = read-time
                                                                                  group-by, no extra file)
                                                                                 |
                                          [batch triage: every distinct diff-shape/group --> LLM
                                           hypothesis + vendored-source verify + draft bucket/ADR (§7)]
                                                                                 |
                            +------------------------+------------------------+--------------------+
                            v                        v                                             v
                       substantive              deferred (draft)                          allowlist (DRAFT —
                       (fix; fix must            rubber-stamp                               SUPERVISOR RATIFIES,
                        pass ALL group                                                      never auto-commit)
                        members; promote
                        only legit content)
```

The slow Python step runs **once**; all iterative Rust work reads the cache. (The minimiser, if ever
built, is the only thing that would re-invoke Python — and only on a handful of representatives, via
the *same* config-locked oracle path; see §11 deferred.)

---

## 5. Component design (maximal reuse, minimal new surface)

### 5.1 Shared comparison helpers — *prerequisite refactor* (corrected)
**Reality (verified against the gate files):** NFC normalisation (`.nfc()`) is duplicated across
*all* gates, and the coarse diff/report helpers (`collapse_ws`, `first_diff_index`, `window_around`,
oracle-spawn, escape) are near-verbatim duplicated between e.g. `trafilatura_markdown_gate.rs` and
`trafilatura_txt_gate.rs`. The fingerprint / U+2063 / `<teiHeader>` masking, however, lives
essentially **only in `trafilatura_tei_gate.rs`** and is *format-specific* — it is **not** applied by
the txt/json/markdown gates.

**Change:** extract into `tests/common/mod.rs` a `normalize(output: &str, format: Format) -> String`
that always applies NFC + the shared coarse helpers, and applies fingerprint/header masking **only
for the formats that emit them** (parameterised by `format`, not blanket). Note the masks are **three
distinct shapes**, not one toggle: xml masks a single `fingerprint=` attribute, TEI collapses the
whole `<teiHeader>` element, csv masks a column — the `format` switch must encode each. Plus the
shared oracle-spawn + diff-window reporting. Refactor all gates to `mod common;`.
**Invariant, mechanically checked:** capture each gate's current output hash; after the refactor the
hashes must be byte-identical (a recorded pre/post comparison, not a "still green" assertion).
`common` carries `#[allow(dead_code)]` since each test crate uses a different subset.

The fuzz harness lives in the same test crate, so it shares `common` directly — no cross-crate
plumbing, no new workspace member, no public-API widening.

### 5.2 Harvester — `benchmark/fuzz/harvest.py`
Downloads N Common Crawl WARC file(s) over HTTPS from `data.commoncrawl.org`, streams records with
`warcio`, keeps `text/html` decoded to UTF-8 that pass a **content filter** (see below), light-
stratifies toward content-bearing long-form, samples to target count, and writes:
- `benchmark/fuzz_corpus/<sha>.html` (git-ignored)
- appends `benchmark/fuzz/manifest.jsonl`: `{sha, source_url, warc_path, warc_offset, length}`
  (committed — reproduces the corpus without storing it; **`source_url` is load-bearing**, §5.3)

**Content filter (defines the test population — documented, because tuning it tunes bug yield):**
The filter draws a sharp line that V2 blurred. It **excludes only *non-content* pages** — soft-404s,
JS-stub/empty bodies (little/no `<p>` text), truncated payloads, binary mislabelled as html, size
outside ~1KB–2MB. It **deliberately KEEPS malformed, unusual, and badly-nested markup** — broken
tags, weird whitespace, exotic entities, deep nesting. *That is exactly where parser/serializer
divergence bugs live, and parsing "the bytes as given" is in scope per the founding brief* (only
charset detection and JS-rendering are the caller's job, not malformed-markup tolerance). So the
filter removes inputs that reveal *nothing*, not inputs that reveal *bugs* — directly answering the
Round-2 concern that filtering would suppress the very pages M9 needs to see.

**Filter is FROZEN once set (Stage 3) and may not be tuned during triage** — its definition is part of
the KPI baseline (§8); changing it invalidates and re-baselines the KPI. This closes the "tighten the
filter to inflate the KPI" gaming vector. (Stage 0 runs against a *provisional* version of this filter;
the harvest re-runs under the frozen filter at Stage 3, so a Stage-0/Stage-5 filter mismatch cannot
affect either gate — a NO-GO ends the milestone, a GO re-harvests.)

**Language note:** Python is a *deliberate* exception (CLAUDE.md "unless otherwise specified"):
`warcio` is Python-native/battle-tested and this is oracle-adjacent data-prep run rarely. Project
logic (diff/group) stays Rust. `warcio` pinned in the oracle venv.

**Selection-bias caveat (explicit):** CC is English-heavy and under-represents mdrcel's hard shapes
(SEC/regulator filings, table-heavy financial docs). **The 51 curated snapshots remain authoritative
for those shapes**; a green CC run is a claim about the broad web distribution, *not* about the hard
corpus. Light stratification mitigates but does not eliminate this.

### 5.3 Batch oracle — extend `benchmark/oracles/trafilatura/run.py`
Add `--batch <corpus_dir> <manifest>`: one process, one `trafilatura` import, loops over the manifest
and for each doc invokes the **single shared oracle function** with **config locked** and the
**per-doc `source_url` threaded in as `url=`** — identical to what mdrcel is fed. It replicates the
TEI `with_metadata=True` forcing for the `xmltei` field. Emits one JSONL line per doc with **only the
formats we actually diff** (start with txt + markdown; widen if Stage 0 shows signal elsewhere).

**Prerequisite (Round-2 finding):** `run.py` today has *no* shared oracle function — each mode inlines
its own `use_config`/`extract` block. Stage 2's first task is to **extract that into one function**
that the existing per-doc modes, `--batch`, and any future minimiser all call. This guarantees a
single config/flag path (the anti-drift invariant) and is small but not free.

**Cache integrity (CRITICAL fix):** the first line of `oracle_cache.jsonl` is a header
`{traf_version, cfg_sha, run_py_sha, manifest_sha}`. `fuzz_diff.rs` refuses to run if any differ from
the live environment — a venv bump or cfg edit must not silently turn stale-cache divergences into
phantom "bugs."

### 5.4 Diff + classify + group — `tests/fuzz_diff.rs` (one `#[ignore]`d harness)
Invoked explicitly: `cargo test --test fuzz_diff -- --ignored --nocapture`. For each manifest doc it
feeds mdrcel the **identical `(html, source_url)`** the oracle saw, normalises both via
`common::normalize`, byte-compares; on mismatch computes the simple signature (§6) and appends to
`benchmark/fuzz/divergences.jsonl`. **Grouping is a read-time group-by over that file** (no separate
`clusters.json`). Reads `oracle_cache.jsonl` (after the integrity check); never spawns Python.

### 5.5 Batch triage — ad-hoc first, skill later
The first triage batch is a supervisor-driven prompt following §7. To guard against false-merges
hiding a second root cause, the split criterion is **mechanical, not statistical** (Round-2 finding —
"sample N members" only *reduces the odds* of missing a hidden cause):
- within each group, hash the **diff-shape** of every member (the normalised diff-window with literal
  text elided);
- **K = 1**: a group with **more than one distinct diff-shape hash is auto-split** before triage — i.e.
  the LLM is fed **every distinct diff-shape**, never a sampled subset (this also removes any tunable
  to mis-set);
- **self-detecting residual (Round-3):** the one remaining seam is hash *aliasing* — two distinct
  causes that elide to the same diff-shape. So triage **records whenever a single diff-shape resolves
  to > 1 confirmed root cause**; that event is the signal that the diff-shape hash is too coarse and
  must be refined. The closure is therefore self-correcting, not a trusted assumption.

This makes "did we miss a second cause?" a deterministic, observable property of the data, not a
sampling gamble.
Promote to a committed `.claude/skills/fuzz-triage/` skill **only once the workflow has stabilised**
across a run or two. (Avoids committing reusable infra before its shape is known.)

### 5.6 Promotion (guarded)
A divergence root-caused as a real mdrcel bug **and fixed** has its reproducer copied into
`benchmark/corpus/snapshots/` as a permanent fixture, picked up by the existing gate. Guards:
- the fix must turn **all** members of the group byte-equal (not just one reproducer);
- **supervisor confirms the reproducer is legitimate content** (not a soft-404/JS-stub/spam page) —
  matching Python on garbage is overfitting noise, not correctness, and must never become a fixture;
- minimised if a minimiser exists; otherwise the smallest natural reproducer.

---

## 6. Stages

### 6.0 Stage 0 — falsification spike (gates the whole milestone)

**What this gate is and is NOT (Round-2 reframe).** Stage 0 is an **effort/yield gate**, not a proof
of faithfulness. A NO-GO does **not** assert "the port is faithful" — that would be inferring a
universal from a small sample (absence-of-evidence). It asserts the far weaker, sound claim: *"the
expected marginal bug-yield of building the full apparatus is too low to justify the effort right
now."* That is a legitimate cost-benefit inference even from a few hundred pages, and it is all the
gate needs to do.

**Method.** Harvest a *few hundred* CC pages through the §5.2 filter, **lightly stratified** to
include non-English and table-heavy/long-form pages (so the spike is not purely the easy English
distribution). Run the **existing per-doc oracle** with masks **off**, and tabulate the **distinct
first-diff loci / shape-classes**, **classifying each as a known-class or new-class locus**.

**Discounting the known classes (Round-2 fix).** Masks are off, so the three known clusters
(U+2063/`Cf` chars, blake2b fingerprint, TEI header) will dominate the *raw* divergence volume — that
volume is meaningless here. The gate is computed **only on new-class distinct loci**, after
mechanically discounting any locus attributable to the three known classes.

**Pre-registered Go/No-Go (decide the thresholds X, Y before looking at results, to avoid an
anti-inversion "looks like the usual noise" rationalisation):**
- **NO-GO** iff fewer than **X distinct new-class root-cause loci** appear across the sample (default
  X = 3). Report to Arthur; do not build Stages 1-5.
- **GO** iff ≥ X new-class loci, or any single new-class locus recurs across ≥ **Y** pages (default
  Y = 5, signalling a systematic rather than one-off divergence). Use Stage-0 data to choose which
  formats to cache (§5.3) and whether mutation is warranted.
- Thresholds are defaults; Arthur may set them at ratification. The point is that they are *fixed in
  advance*, not chosen to fit the result.

This spike is ~½ day and can save weeks. It is the cheapest test of M9's premise — and because its
NO-GO is framed as "not worth the effort" rather than "proven faithful," it cannot be mis-read as a
faithfulness certificate.

### 6.1–6.5 (only on GO)
1. **Stage 1** — shared-helpers refactor (§5.1) with mechanical pre/post hash check. Land early (M8
   shared seam).
2. **Stage 2** — config-locked batch oracle + cache-integrity header + URL threading + `warcio` pin
   (§5.3). Validate on the existing 51.
3. **Stage 3** — harvester + content filter + manifest + light stratification (§5.2).
4. **Stage 4** — diff/classify/group harness (§5.4, §6).
5. **Stage 5** — ad-hoc batch triage + guarded fix/promote loop (§5.5, §5.6, §7), measured by the
   §8 fidelity KPI.

---

## 6'. The grouping signature (deliberately dumb v1)

Start with the **simplest key that groups**: `(format, shape_class, first-differing-node tag)` where
`shape_class` ∈ {whitespace, text-content, attribute, missing-element, extra-element, structural}.
This may over-merge; that is made *safe* (not merely tolerable) by the **mechanical diff-shape-hash
split** in §5.5 (K = 1) — any coarse group containing more than one distinct diff-shape is auto-split,
and every distinct diff-shape is shown to triage. So the dumb key cannot silently bury a second cause,
and the residual hash-aliasing risk is self-detecting (§5.5). Refine the key *only* if it proves to
over-split or the self-detection fires — never up front.

---

## 7. Triage contract — asymmetric trust + anti-inversion

Per group, triage **forms a root-cause hypothesis, then opens the vendored Python source
(`benchmark/oracles/trafilatura/.venv/Lib/site-packages/...`) and confirms it** before asserting.
Trust is asymmetric by blast radius:

| Bucket | Meaning | Autonomy |
|---|---|---|
| **substantive** | mdrcel wrong; fixable | Self-proving, **but** the fix must pass *all* group members and the reproducer must be confirmed legitimate content before promotion (§5.6). |
| **deferred** | mdrcel wrong; blocked on a dep we won't add | Drafts ADR `wrk_docs/m9-deferred/<hash>.md`; supervisor rubber-stamps. |
| **allowlist** | **the reference is wrong, mdrcel is right** | Extraordinary — where one would hide one's own bugs. Drafts with vendored-source proof in `wrk_docs/m9-allowlist/<hash>.md`; **supervisor MUST ratify; never auto-committed.** |

The substantive-lane guard (all-members + legitimate-content) is the V2 addition that stops the LLM
baking "match Python on a garbage page" into a permanent fixture.

---

## 8. Definition of done — a measurable fidelity KPI (not "diminishing returns")

Reserve a **frozen held-out harvest slice** that is *never used for triage*. The KPI is its
**byte-equal page rate** (per format), measured before Stage 5 and after each triage iteration.
- **Done** = the held-out KPI **plateaus** (successive iterations move it < ε), *not* "backlog
  drained" — because masking and coarse grouping can shrink the backlog without improving fidelity.
- **Anti-gaming (Round-2):** the held-out slice and the §5.2 content filter are both **frozen before
  Stage 5** and unchanged during triage. Tightening the filter would otherwise inflate the KPI without
  improving real fidelity; freezing both removes that vector. Any filter change re-baselines the KPI.
- **Scope caveat (Round-2):** the held-out slice is drawn from the same CC distribution, so the KPI
  certifies the **broad web distribution only** — it is *blind to the hard shapes* (SEC/regulator/
  table-heavy) CC under-represents. Hard-shape fidelity remains certified by the **51 curated gates**,
  which stay green throughout regardless of the KPI. The KPI is a generalisation check, not a
  replacement for the curated corpus.
- A NO-GO at Stage 0, or a KPI that starts already-high and doesn't move, is a legitimate "M9 found
  little on the web distribution" outcome — report it; don't manufacture work.

---

## 9. Directory / file layout

```
benchmark/
├── corpus/snapshots/           # EXISTING committed gate corpus (51 → grows only via §5.6 promotion)
├── fuzz_corpus/                # NEW: bulk ephemeral snapshots (git-ignored)
├── fuzz/
│   ├── harvest.py              # NEW: CC WARC → snapshots + manifest (Python, §5.2)
│   ├── manifest.jsonl          # NEW: committed; reproduces fuzz_corpus (sha, source_url, ...)
│   ├── oracle_cache.jsonl      # NEW: git-ignored; header line + per-doc oracle output (§5.3)
│   └── divergences.jsonl       # NEW: git-ignored; per-doc diffs (grouping computed on read)
└── oracles/trafilatura/run.py  # EXISTING: + --batch; single config-locked oracle fn (§5.3)
tests/
├── common/mod.rs               # NEW: shared normalize()/diff helpers (§5.1); gates refactored
├── fuzz_diff.rs                # NEW: #[ignore]d diff/group harness (§5.4)
└── trafilatura_*_gate.rs       # EXISTING: refactored to `mod common;`
wrk_docs/
├── m9-allowlist/<hash>.md      # NEW: supervisor-ratified only
└── m9-deferred/<hash>.md       # NEW
.gitignore                      # + fuzz_corpus/, oracle_cache.jsonl, divergences.jsonl
# DEFERRED (build only if needed, §11): tests/fuzz_min.rs, .claude/skills/fuzz-triage/
```

---

## 10. Coordination with M8 (parallel stream)

- **M9 owns:** everything "NEW" above, `run.py`'s `--batch`, and non-metadata divergence fixes
  (serializers, xpath, cleaning, tail-handling).
- **M9 does NOT touch:** `metadata*.rs`, the `<teiHeader>` rendering in `output.rs`, or the TEI gate's
  masks. A metadata divergence the fuzzer finds is **recorded for M8**, not fixed.
- **Shared seam:** `tests/common/mod.rs` (land Stage 1 early, announce it) and `output.rs` (localised,
  atomic commits). `m9-*` ADR trees are disjoint from `m8-*`.
- **Supervisor is the integration point:** main stays gate-green; commits serialised.

---

## 11. Deferred (do NOT build until evidence demands it)

- **Minimiser (`tests/fuzz_min.rs`).** Only if manual reduction of real reproducers proves too slow.
  When built: it must route through the *same* config-locked oracle path as the batch run, validate
  its first interestingness call reproduces the cached divergence byte-for-byte before shrinking, and
  **keep each candidate `Dom` alive during its interestingness test** — the rcdom `Drop` quirk
  (documented in `Cargo.toml` Stage-2d/4d notes) drains descendants of dropped trees even while `Rc`
  handles are held; a tight clone/mutate/drop loop will hit it.
- **Committed triage skill (`.claude/skills/fuzz-triage/`).** Only once the ad-hoc workflow stabilises.
- **Structure-aware mutation.** Only if Stage 0 shows the real-world harvest under-yields.

---

## 12. Simplicity ledger — what we deliberately did NOT build

No feature flags, no env vars (opt-in is `#[ignore]` + explicit `--ignored`; data-prep is a separate
script). No persistent oracle server. No new workspace crate. No migration/rollback/back-compat. No
bulk corpus in git. No three-way comparison. No ML/embedding clustering. **No minimiser, no committed
skill, no signature taxonomy beyond the dumb key — all deferred behind evidence.** No statistical
power machinery on the Stage-0 gate (it's an effort gate, not a proof). Honest core: *spike → (if it
pays) harvest → run oracle once → diff against cache → group → fix & promote real bugs.* ~5 parts,
holdable in one head.

---

## 13. Risks

- **Premise risk** (DA): mitigated by the Stage-0 go/no-go (§6.0).
- **Garbage-input overfitting** (DA): mitigated by the content filter (§5.2) + substantive-lane guard
  (§7) + legitimate-content promotion gate (§5.6).
- **Oracle drift** (methodology): mitigated by single config-locked oracle path + cache-integrity
  header + URL threading (§5.3).
- **False-merge hides a bug** (methodology): mitigated by the **mechanical diff-shape-hash split** —
  every distinct diff-shape in a group is shown to triage (§5.5, §6'); deterministic, not a sampling
  gamble.
- **CC selection bias** (methodology/DA): mitigated by keeping the 51 curated authoritative for hard
  shapes + light stratification + explicit scope statement (§5.2, §8).
- **Biased / over-claiming Stage-0 gate** (DA CRITICAL): mitigated by reframing NO-GO as an effort
  gate (not a faithfulness proof), keeping malformed markup in the filter, discounting known classes,
  and pre-registering numeric thresholds (§6.0).
- **KPI gaming** (DA): mitigated by freezing the content filter + held-out slice before Stage 5 (§8).
- **Unbounded effort** (DA): mitigated by the §8 KPI-plateau exit + Stage-0 gate + §14 precondition.

---

## 14. Escalation to Arthur (above the review team's authority)

The devil's advocate's strongest challenge is strategic, not technical: **the crate has never been
integrated into the consumer or published — the project's original purpose — yet this is the seventh
fidelity milestone.** A ~1-day experiment (wire the existing crate into the consumer's `extract_content`
shim and run the known-bad URLs against the founding brief's *coarse word-count* acceptance bar) could
reveal that byte-fidelity beyond what we have is not the binding constraint at all — which would make
M9 lower priority than shipping.

**Teeth (Round-2 fix — this is a precondition, not a footnote).** The review team cannot decide
ship-vs-harden; **Arthur must rule explicitly before M9 builds anything past Stage 0.** "Harden" is
**not** a silent default. Two cheap (~½–1 day each) falsification experiments now exist — the Stage-0
yield spike (§6.0) and the integration spike above. Recommended order:
1. Arthur rules on ship-vs-harden (this section).
2. If "harden" (or "do both, harden-first"): run Stage 0; honour its go/no-go.
3. Only on a Stage-0 GO do Stages 1-5 begin.
If Arthur has not ruled, M9 does not proceed past the Stage-0 spike. If the call is "ship first," M9
waits.

## 15. Open questions for Arthur
1. **Ship-vs-harden** (§14) — the one real decision.
2. **WARC slice** — latest monthly / arbitrary segment fine for Stage 0?
3. **Promotion cap** — bound promoted fixtures per run to keep the gate fast? (Default: no cap,
   minimised+legitimate only.)
