# M3 forward push — supervisor-driven /goal loop

**Started:** 2026-05-21 (after Arthur asked for a new /goal loop)
**Goal:** open-ended M3 forward push (3-B residual → Stage 4 onward).
**Pattern:** supervisor dispatches focused subagents, reviews each
report/commit, journals, picks the next sub-task. Supervisor protects
its own context by NOT doing the deep work itself.

## Starting state

- Commit `cda48a5` — overnight wrap-up journal landed.
- Stage 3-B gate: 9/10 fixtures pinned. Apple_Inc Wiki residual.
- 582 lib tests, all 4 BLOCKER gates green.
- Cargo 0.4.19. Working tree clean apart from `.claude/`.

## Loop discipline

1. **Brief each agent fully** — they don't see prior conversation. Hand
   over: goal, context (cite journals + line numbers), constraints,
   commit-ask, stopping rule.
2. **Demand tight reports** — under 300 words. Verify by reading the
   diff myself, not by trusting the agent's prose.
3. **Stopping rule for agents:** if they can't resolve cleanly in their
   context budget, STOP and report root cause + recommended fix plan.
   Speculative code is worse than a clean handoff.
4. **Stopping rule for supervisor:** Arthur returns / three consecutive
   agent failures / scope drifts beyond M3.
5. **Commit cadence:** each green fixture / each substantive fix is its
   own commit. Version-bump on every Cargo.toml touch (PATCH for fixes,
   MINOR for new functionality).

## Iterations

### Iter 1 — Apple_Inc Wiki pin (Stage 3-B 9/10 → 10/10)
**Dispatched:** general-purpose agent, foreground.
**Brief:** diagnose+fix the token-2407 nested-list-in-item ordering bug,
add fixture to gate, regression test, bump Cargo, commit. Stop+report
if can't resolve cleanly.
**Status:** AGENT FAILED — returned only "Not logged in · Please run
/login" after 89s. Auth infra broken for this session.

**Pivot:** supervisor switches to direct-codes mode for the rest of the
night. Same discipline (tight scopes, one commit per substantive fix,
journal as we go) but I'm the implementer. Context-protection becomes
about doing focused reads (only the file regions I need) rather than
delegating.

**Resuming Iter 1 directly.**

### Iter 1 progress notes — root cause identified

After dumping both XMLs and aligning tokens:
- **Tokens 2390-2401** (Cardwell + Cole, with quotes): both Rust & Python emit
  `<item>^ <list rend="ul"><item>Cardwell<quote/></item><item>Cole<quote/></item></list></item>`. Identical.
- **Tokens 2402-2403**: Python emits a DUPLICATE
  `<item>Cardwell, Diane (...) Archived... December 30, 2016. </item>` — i.e.
  the text from the FIRST inner item of the previous list. Rust does NOT emit
  this duplicate.
- Token 2406-2415: env-reports list — both agree on the LOCAL shape, just
  shifted by 2 tokens because of the Cardwell duplicate above.

**Mechanism (empirically verified with lxml):** `iterdescendants("item")`
PRE-COMPUTES the next-pointer at yield time. The body of the iter recursively
calls `handle_lists` on a nested list, which sets inner items' `tag = "done"`.
But the precomputed next-pointer ALREADY points at the first inner item, so
the outer iter yields it AGAIN despite the tag mutation. Subsequent siblings
are skipped (tag check is current). So exactly ONE inner item gets a duplicate
emission per nested-list-in-item.

This is a Python/lxml quirk that the Rust port currently does NOT mimic.
Trafilatura faithfulness requires we replicate it.

**Empirical lxml proof:**
```
xml = '<list><item id="x"><list><item id="y1"/><item id="y2"/></list></item></list>'
for elem in tree.iterdescendants('item'):
    print(elem.get('id'), elem.tag)
    if elem.get('id') == 'x':
        for it in elem.iterdescendants('item'):
            it.tag = 'done'   # mutate y1, y2
# OUTPUT:
#   x item
#   y1 done    ← yielded despite mutation (precomputed)
# y2 NOT yielded (current tag check skips it)
```

**Fix shape:** after Rust's `handle_lists` processes an item that contained
a nested list, ALSO process the FIRST descendant item from that nested list
as a sibling at the outer level (mimicking the precompute). Skip subsequent
inner items (their tags are already "done", as today).

Implementing.

### Iter 1 — RESOLVED ✓ (Apple_Inc Wiki pinned, 10/10)

**Fix:** in `handle_lists`'s outer loop (Rust at `main_extractor.rs:266+`):
1. BEFORE the non-leaf else branch, snapshot the first descendant `<item>`'s
   text + has-children flag (`peek_first_descendant_item`).
2. Run process_nested_elements as normal.
3. After the gate/append/replace_element_tag steps, IF the snapshot has
   non-empty text AND had element-children, append a phantom `<item>`
   with text=snapshot to processed_element.

Mirrors lxml's iterdescendants precompute quirk faithfully. Comments and
empirical lxml proof inlined in the code.

**Cargo:** 0.4.19 → 0.4.20 PATCH (detailed CHANGELOG entry).
**Lib tests:** 582 → 583 (+1 regression pin:
`handle_lists_emits_phantom_duplicate_for_nested_list_with_quote_child`).
**Gate corpus:** 9/10 → **10/10**.
**All 4 gates green:**
- parser_equivalence: 4/4
- trafilatura_equivalence: 8/8
- trafilatura_extract_smoke: 1/1
- trafilatura_extract_content (Stage 3-B): 5/5 (1 main + 4 tokenizer self-tests)

Stage 3-B is **complete** — full byte-meaningful structural equivalence with
Python on all 10 fixtures.

Next iteration: pivot to Stage 4 (vendored_readability fork). Will dispatch
a Plan agent if the agent infra recovers, otherwise scope it myself.

### Iter 2 — Stage 4 scoping (Explore agent, foreground)

**Agent infra recovered.** Explore agent returned a 400-word scoping report.
Stage 4 broken into 4 sub-stages:
- **4a:** Data structures + scoring primitives (Candidate, score_node,
  class_weight, link_density) — ~150 LOC, standalone unit-testable.
- **4b:** Document.summary() core orchestration (unlikely-candidate strip,
  paragraph scoring, top-candidate selection) — ~200 LOC.
- **4c:** Sanitize + retry logic (sanitize conditionals,
  ruthless/lenient/fallback) — ~250 LOC.
- **4d:** is_probably_readerable() + integration into cascade arbiter —
  ~200 LOC.

Total ~750-1020 LOC across 4 commits.

**Sharp edges flagged:**
- lxml `.tail` precompute quirk (similar to the Cluster D we just fixed)
- Serialization round-trip not idempotent on malformed HTML
- `drop_tree()` mutations destructive (Mozilla M2 uses re-parse, faithful
  port may need different state mgmt)
- Score-side-table divergence vs M2 (readability_lxml uses local Candidate
  objects, no side table)
- Bridge layer needed: NodeRef ↔ "HtmlElement-equivalent"

### Iter 3 — Stage 4a (Candidate + scoring primitives) ✓ RESOLVED

**Agent:** general-purpose, foreground. Commit `a1428d2`.
**Delivered:** new module `src/trafilatura/readability_fork.rs` (655 LOC,
≈340 production + 315 tests). Contents: 9 tag-set constants, 6 OnceLock
regex accessors, `Candidate` struct, 5 primitives (text_length,
class_weight, score_node, score_paragraph_text, link_density), 20 unit
tests.
**Lib tests:** 583 → 603 (+20).
**Cargo:** 0.4.20 → 0.5.0 MINOR (new public surface).
**All 4 gates green** (verified post-commit).

Agent notes: 4 unused-yet (dead_code-annotated) constants + 4 regex
accessors are deliberately vendored now so the readability_lxml.py
constant surface lives in one module; 4b/4c will retire the annotations
as callers land.

### Iter 4 — Stage 4b (Document.summary() core) ✓ RESOLVED

**Agent:** general-purpose, foreground.
**Delivered:** extends `src/trafilatura/readability_fork.rs` ADDITIVELY
(Stage 4a public surface FROZEN) with the `Document` class + the
`summary()` ruthless/lenient retry loop + the four helpers it directly
invokes — port of `readability_lxml.py:102-225`. Functions exposed:
- `Document { ... }` struct + `Document::new(html)` /
  `Document::with_options(html, min, retry)` constructors
  (readability_lxml.py:102-122).
- `Document::summary() -> Option<NodeRef>` (readability_lxml.py:124-166).
  Re-parses `self.html` on every retry attempt (HLD §m-3 pattern; M2 1c
  precedent) to avoid stale `drop_tree` side effects between modes.
  Calls a `_stage4b_sanitize_stub` pass-through so the
  `article_length < retry_length` retry gate has a quantitative signal
  until Stage 4c lands the real sanitize.
- Private helpers `remove_unlikely_candidates`,
  `transform_misused_divs_into_paragraphs`, `score_paragraphs`,
  `get_article` — each line-cited verbatim to its Python source.
- Public free fn `select_best_candidate(&[(NodeRef, Candidate)]) ->
  Option<Candidate>` (readability_lxml.py:209-218). Tie-break parity:
  FIRST inserted wins on equal scores.
- 4 previously `#[allow(dead_code)]`-annotated 4a items are now live
  (FRAME_TAGS, unlikely_candidates_re, ok_maybe_re,
  div_to_p_elements_re); annotations removed. Stage 4c consumers
  (TEXT_CLEAN_ELEMS / LIST_TAGS / DIV_TO_P_ELEMS / video_re) remain
  dead-code-annotated.

**Lib tests:** 603 → 612 (+9 in readability_fork::tests):
document_summary_returns_best_candidate_for_simple_article;
document_summary_falls_back_to_lenient_when_ruthless_fails;
document_summary_returns_body_for_empty_html;
remove_unlikely_candidates_strips_comment_div;
transform_misused_divs_into_paragraphs_renames_text_only_div_to_p;
select_best_candidate_picks_highest_scored;
select_best_candidate_returns_none_for_empty_input;
select_best_candidate_keeps_first_tied_entry;
document_new_uses_python_defaults.

**LOC added:** +830 in `src/trafilatura/readability_fork.rs` (production
~ 530, tests ~ 300). Cargo.toml +69 (changelog entry).

**Cargo:** 0.5.0 → 0.5.1 PATCH. Rationale recorded in the Cargo entry:
this is an additive extension WITHIN the already-public 4a module (no
new module; 4a's CHANGELOG entry explicitly flagged the module as
incomplete pending 4b/c/d). Reviewer arguably prefers MINOR (new
`Document` type crosses the strict reading of "new public type =
MINOR"); both rationales documented, PATCH recorded as conservative.

**All 4 BLOCKER gates green** (verified post-bump):
parser_equivalence_gate, trafilatura_equivalence_gate (4/4 conv +
4/4 frozen = byte-identical), trafilatura_extract_smoke (1/1),
trafilatura_extract_content_gate (5/5 incl. fixture).
`cargo clippy --release --all-targets -- -D warnings` clean.

**Honest scope deferral (called out, not hidden):**
- `transform_misused_divs_into_paragraphs` ports the retag pass only;
  the br/text-rescue pass (readability_lxml.py:312-324) is omitted as
  it does not affect downstream paragraph scoring (text_content
  concatenates descendant text either way). Stage 4c or 4d will revisit
  if a corpus divergence demands it.
- `Document::summary` returns `Option<NodeRef>` (caller serializes via
  `serialize_converted_tree`); Python returns a string. Documented in
  the docstring + brief approved this Rust-idiom deviation.

**Concerns for supervisor verification:** the sanitize stub means the
length-based retry-trigger fires off raw serialized HTML length, NOT
the (smaller) sanitized output Python computes. Tests confirm the
ruthless→lenient retry fires when ruthless strips the only content
(test 2), but real corpus behaviour will shift once Stage 4c plugs in.

**Remaining work for Stage 4c:** port `sanitize` (readability_lxml.py:
326-438) — the largest single function in the file (~110 LOC source).
Delete `_stage4b_sanitize_stub` and route `summary()` through the
real sanitize. Wire up the four still-dead-code-annotated constants /
regex accessors (TEXT_CLEAN_ELEMS / LIST_TAGS / DIV_TO_P_ELEMS /
video_re). Stage 4d: `is_probably_readerable` (lines 459-512) plus
cascade integration of `Document::summary()` into the M3 extract
orchestrator. Both 4c and 4d are PATCH-class additive extensions of
the same module.

### Iter 5 — Stage 4c (sanitize) ✓ RESOLVED

**Agent:** general-purpose, foreground. Commit `89d352f`.
**Delivered:** replaces `_stage4b_sanitize_stub` with the real port of
`readability_lxml.py:326-438`. Activates `TEXT_CLEAN_ELEMS`, `LIST_TAGS`,
`video_re` (dead-code annotations removed); `DIV_TO_P_ELEMS` stays
annotated (Stage 4b consumer only, not used by sanitize).

**Lib tests:** 612 → 621 (+9 sanitize tests).
**Cargo:** 0.5.1 → 0.5.2 PATCH.
**All 4 BLOCKER gates green.**

**Agent honest-deviation flags:**
- `sanitize_drops_img_without_src` brief test was renamed to
  `sanitize_keeps_img_without_src_faithful_to_python` — Python's sanitize
  has NO img removal clause, so the test was reversed to match (HLD §4
  anti-inversion).
- The `document_summary_falls_back_to_lenient_when_ruthless_fails` test
  swapped `class="sidebar"` → `class="extra"` because sidebar triggers
  BOTH unlikelyCandidatesRe AND negativeRe (the real sanitize correctly
  drops the article-bearing div).

### Iter 6 — Stage 4d (is_probably_readerable + cascade) ✓ RESOLVED

**Agent:** general-purpose, foreground. Commit `b5342f4`.
**Delivered:** final piece of Stage 4. Two parts:
1. `is_probably_readerable` + `is_node_visible` ported from
   readability_lxml.py:459-512. Visibility gate + sqrt-score accumulation.
2. Cascade integration via new public functions:
   - `try_readability(html) -> Option<NodeRef>` (external.py:32-42)
   - `cascade_prefers_readability(...)` — partial port of
     external.py:66-85 (deferred focus-tuned borderline arms to Stage 6)
   - `bare_extraction_with_cascade(html, &Options) -> Option<NodeRef>` —
     orchestrates own + readability arms.

**Agent's sharp insight:** `extract_content` is the **own-arm only** in
Python. The cascade lives one level up at `core.trafilatura_sequence`.
So Stage 4d wires into a brand-new free function and `extract_content`
stays byte-identical — the Stage 3-B gate is unaffected (as predicted
in the brief).

**Lib tests:** 621 → 628 (+7).
**Cargo:** 0.5.2 → 0.6.0 MINOR (new public functions).
**All 4 BLOCKER gates green.**

## Stage 4 — COMPLETE ✓

Across 4 commits (a1428d2, aa60492, 89d352f, b5342f4):
- ~2300 LOC of new code in `src/trafilatura/readability_fork.rs`
- +45 lib tests (583 → 628)
- Cargo 0.4.20 → 0.6.0 (2 MINOR + 2 PATCH bumps)
- All 4 BLOCKER gates green throughout
- New public surface: `Candidate`, `Document`, `is_probably_readerable`,
  `try_readability`, `bare_extraction_with_cascade`, plus several
  primitive functions.

The Trafilatura readability fork is now fully ported. The remaining M3
umbrella stages are Stage 5 (jusText port — third cascade arm),
Stage 6 (full arbiter with focus-tuned borderline cases), Stage 7
(bare_extraction + metadata), Stage 8 (extract entry-point), Stage 9
(HLD §7.6 wiring).

### Iter 7 — Stage 5 scoping (Explore agent) ✓ RESOLVED

**Agent:** Explore, foreground. Returned a 500-word scoping report.

**Key findings:**
- jusText is a 522-line Python paragraph classifier with a 2-phase decision
  tree (context-free classify + context-sensitive revise).
- An existing Rust crate (`justext 0.2.0`, nchapman/justext-rs) exists and
  is API-compatible. Agent recommended VENDORING the crate.
- 100 language stoplist files (~300KB total) — static data, vendorable as
  .txt files.

**Supervisor decision: OVERRIDE the agent's vendor-the-crate recommendation
and do the full faithful port.**

**Rationale:** Arthur's whole project is built on anti-inversion / line-cite
doctrine — every Rust function cites the Python source line. Adding a
third-party Rust crate that's "not line-for-line identical to Python
justext" would break the faithfulness contract that the WHOLE codebase
is built on. The agent doesn't know this doctrine; I do. Per CLAUDE.md
"Push back when you disagree, citing technical reasons or gut feeling" —
this is one of those cases.

The extra ~325 LOC of full port (475 total vs 150 crate-integration) is
the price of doctrine. The decision is reversible — if Arthur disagrees
when he wakes, we can pivot to the crate. But the safer-doctrinally
default is to start with the full port.

**Sub-stage phasing (faithful port):**
- 5a: Stoplist vendoring + accessor (~100 LOC + 300KB data) — needed
  either way (crate path also needs the data)
- 5b: Paragraph struct + ParagraphMaker port (~150 LOC)
- 5c: classify_paragraphs + revise_paragraph_classification (~120 LOC)
- 5d: cascade integration — extend bare_extraction_with_cascade (~60 LOC)

### Iter 8 — Stage 5a (stoplist vendoring) ✓ RESOLVED

**Agent:** general-purpose. Commit `996f813`.
**Delivered:** 100 .txt files vendored (~2.4 MB — agent flagged the brief's
300 KB estimate was 8× low because Indic-script stoplists use multi-byte
UTF-8). Module `src/trafilatura/justext_stoplists.rs` exposes `get_stoplist`
+ `get_stoplists`. Lazy-init per-language `OnceLock<Vec<String>>`.
Lib tests: 628 → 633. Cargo 0.6.0 → 0.6.1 PATCH.

**Honest faithfulness divergences (per agent):** signature is
`&'static [String]` not `&'static [&'static str]` (Python's `.lower()`
needs owned). Returns empty slice for unknown language (Python raises
ValueError). Both documented.

### Iter 9 — Stage 5b (ParagraphMaker port) ✓ RESOLVED

**Agent:** general-purpose. Commit `36bb39e`.
**Delivered:** new module `src/trafilatura/justext_core.rs` (937 lines total
incl. doc + tests; ~450 production). Paragraph struct + make_paragraphs
via recursive walker (SAX → tree-walk translation). Preserves the
`<br><br>` paragraph-separator quirk verbatim.
Lib tests: 633 → 646 (+13). Cargo 0.6.1 → 0.6.2 PATCH.

### Iter 10 — Stage 5c (classify + revise) ✓ RESOLVED

**Agent:** general-purpose. Commit `2caa7b9`.
**Delivered:** classify_paragraphs (context-free) + revise_paragraph_
classification (context-sensitive 3-phase). Added 7 pub const thresholds,
5 pub fn, 2 new Paragraph fields (`cf_class`, `heading`).
Lib tests: 646 → 656 (+10). Cargo 0.6.2 → 0.6.3 PATCH.

### Iter 11 — Stage 5d (cascade integration) ✓ RESOLVED

**Agent:** general-purpose. Commit `5d9ae99`.
**Delivered:** `try_justext` + `justext_rescue` ported. Extended
`bare_extraction_with_cascade` from 2-arm → **3-arm** (own / readability
/ jusText). The override gate fires when winning arm has SANITIZED_TAGS
descendants OR text < min_extracted_size; jusText replaces winner if
non-empty AND winning_len <= 4 × jt_len (faithful to Python's 4× ratio).
Lib tests: 656 → 662 (+6). Cargo 0.6.3 → **0.7.0** MINOR (3-arm shift).

## Stage 5 — COMPLETE ✓

Across 4 commits (996f813, 36bb39e, 2caa7b9, 5d9ae99):
- ~2,500 LOC of jusText port code (stoplists vendoring + segmentation +
  classifier + cascade integration)
- +34 lib tests (628 → 662)
- Cargo 0.6.0 → 0.7.0 (3 PATCH + 1 MINOR bumps)
- All 4 BLOCKER gates green throughout
- Override on "vendor the crate" doctrine paid off cleanly

## Session totals through Iter 11

- 12 commits this session (since `cda48a5`)
- Lib tests: 582 → 662 (+80)
- Cargo: 0.4.19 → 0.7.0
- Apple_Inc Wiki pinned (Stage 3-B complete 10/10)
- Stage 4 readability_fork complete (4 sub-stages)
- Stage 5 jusText complete (4 sub-stages)
- All 4 BLOCKER gates remained green throughout every single commit

Remaining M3 umbrella: Stage 6 (focus-tuned borderline arbiter +
sanitize_tree), Stage 7 (bare_extraction + metadata), Stage 8 (extract
public entry-point), Stage 9 (HLD §7.6 wiring).

### Iter 12 — Stage 6 (full arbiter + sanitize_tree) ✓ RESOLVED

**Agent:** general-purpose. Commit `c8cf5ef`.
**Delivered:** 7-branch `compare_extraction` arbiter (all 7 branches
faithfully ported with line-cites) + `sanitize_tree` (found at
external.py:163-190). Added 3 new pub Options fields (lang/url/source),
new public `compare_extraction`, `sanitize_tree`, `TEI_VALID_TAGS`.
Lib tests: 662 → 669 (+7). Cargo 0.7.0 → 0.8.0 MINOR (Options widened).

**Agent honest-divergence:** brief tests 1, 2, 5 were re-encoded against
Python's actual behaviour (the focus enum has no "prefer shorter/longer
on close" branches in compare_extraction; sanitize_tree strips tag
WRAPPERS not attributes). Anti-inversion respected — agent didn't ship
brief-as-written when it would have inverted Python.

**Deferred:** focus=precision pre-clean of backup_tree (needs
Document::from_node).

## Stage 6 — COMPLETE ✓

### Iter 13 — Stage 7a (metadata extraction) ✓ RESOLVED

**Agent:** general-purpose. Commit `9555eb0`.
**Delivered:** new module `src/trafilatura/metadata.rs` (1108 LOC).
`Metadata` struct + `extract_metadata` orchestrator. OG/meta-name/XPath
fallbacks. Lite `normalize_authors`. Title-separator-split heuristic
(structural port of HTMLTITLE_REGEX without adding regex dep).
Lib tests: 669 → 694 (+25). Cargo 0.8.0 → 0.8.1 PATCH.

### Iter 14 — Stage 7b (JSON-LD parser) ✓ RESOLVED

**Agent:** general-purpose. Commit `013d6f1`.
**Delivered:** new module `src/trafilatura/metadata_jsonld.rs` (~620 LOC).
schema.org Article/NewsArticle/BlogPosting/+8 article types ported.
Publisher mapping for site_name. Author parsing handles
String/Object/List/givenName+familyName. Top-level handles single object,
list, and @graph wrapper. Liveblog liveBlogUpdate carve-out preserved.
Lib tests: 694 → 710 (+16). Cargo 0.8.1 → 0.8.2 PATCH.

**Deferred:** `extract_json_parse_error` regex-rescue path (Python's
serde_json failure handler); full `normalize_authors` regex pipeline.

### Iter 15 — Stage 7d (URL + date stub + cats/tags/license) ✓ RESOLVED

**Agent:** general-purpose. Commit `af9e6ca`.
**Delivered:** new module `src/trafilatura/metadata_url.rs` (~1121 LOC).
`extract_url` (canonical → og:url → default). `extract_domain` (minimal
courlan subset, returns full domain with subdomain — faithful to
Python's `fast=True`). `extract_date` STUB (handles article:published_time,
<time datetime>, meta name="date"; htmldate's dateparser path deferred).
`extract_catstags` + `extract_license` wired into `extract_metadata`.
Lib tests: 710 → 735 (+25). Cargo 0.8.2 → 0.8.3 PATCH.

**Anti-inversion:** Python regex quirk in `extract_catstags`
(`[s|ies]?` as char-class, not alternation) replicated verbatim with
inline comment.

## Stage 7 — COMPLETE ✓

Across 3 commits (9555eb0, 013d6f1, af9e6ca):
- ~3700 LOC across 3 new modules (metadata, metadata_jsonld, metadata_url)
- +66 lib tests (669 → 735)
- Cargo 0.8.0 → 0.8.3 (3 PATCH bumps)
- All 4 BLOCKER gates green throughout

**Deferred:** full `htmldate` port (locale-aware date parsing with
dateutil/dateparser equivalent) — flagged as M4 candidate.

## Session totals through Iter 15

- 17 commits this session (since `cda48a5`)
- Lib tests: 582 → **735** (+153)
- Cargo: 0.4.19 → **0.8.3**
- Stages 3-B + 4 + 5 + 6 + 7 all complete
- All 4 BLOCKER gates remained green through EVERY single commit

Remaining M3 umbrella:
- Stage 8: comments extraction + LRU_TEST deduplication
- Stage 9: `mdrcel::extract` public surface wiring (HLD §7.6) +
  byte-equality push

### Iter 17 — Stage 9 (extract public surface wiring) ✓ RESOLVED — **M3 FINALE**

**Agent:** general-purpose. Commit: pending (no commit, handoff).
**Delivered:** `src/lib.rs` rewrite of `extract_with` to drive the full
Trafilatura cascade. M2 Readability port preserved verbatim under new
public `extract_via_readability(html, base_url, opts) -> Result<Extracted,
ExtractError>` function. Added ONE additive `Extracted.comments: String`
field (defaults to `""` via derive).

**New `extract_with` pipeline** (mirrors `core.bare_extraction`,
core.py:130-358):
1. `metadata::extract_metadata(html, base_url, true, &[])` — OG / meta-name /
   itemprop / JSON-LD / URL / date / cats-tags / license.
2. `readability_fork::bare_extraction_with_cascade(html, &cleaning_opts)` —
   parse + tree_cleaning + convert_tags + own arm (extract_content) +
   compare_extraction (own/readability/jusText, 7-branch arbiter,
   sanitize_tree post-pass).
3. `extract_comments_from_html` — fresh parse + clean + Stage 8 comments
   extractor (the rcdom Drop quirk forbids cross-fn DOM sharing).
4. Assemble `Extracted` with the Metadata→Extracted mapping (title, byline,
   excerpt, site_name, published_time, canonical_url, language).

**Lib tests:** 752 → 761 (+9). All 8 brief-mandated tests plus 1 sanity
for the M2 fallback path. All 4 BLOCKER gates remain byte-identical
green. `cargo clippy --release --all-targets -- -D warnings` clean.

**Cargo:** 0.9.0 → **0.10.0 MINOR**. Behaviour shift on the default path
is the load-bearing MINOR trigger: `extract`'s text output differs from
M2 on real-world inputs (different algorithm).

**Deferred to M4:**
- Mapping `Metadata.{categories, tags, image, pagetype, license, hostname}`
  onto the public `Extracted` (currently accessible via the
  `#[doc(hidden)]` infrastructure surface only).
- Full htmldate locale-aware date parsing.
- Relative-URL resolution.
- Output formats (XML / XMLTEI / CSV / JSON / markdown).

## M3 COMPLETE ✓

Across the full M3 umbrella (Stages 0a-9):
- Cargo: 0.4.0 (M2 end) → **0.10.0** (M3 finale).
- LOC: ~13000 across `src/trafilatura/` modules + dom.rs extensions.
- Lib tests: 582 → 761 (+179 across Stages 0a-9; Stage 9 alone +9).
- 4 BLOCKER gates green through EVERY commit (parser_equivalence_gate,
  trafilatura_equivalence_gate, trafilatura_extract_smoke,
  trafilatura_extract_content_gate).
- Algorithms ported: XPath 1.0 evaluator, cleaning + convert_tags,
  baseline rescue, full `_extract` + `extract_content` + 12 block
  handlers, `prune_unwanted_sections` + `recover_wild_text`,
  `readability_lxml` fork (`Document::summary` + `sanitize` +
  `is_probably_readerable`), jusText (segmentation + classifier + 100
  stoplists), `compare_extraction` arbiter, full metadata (OG / meta /
  itemprop / JSON-LD / URL / date / catstags / license),
  `extract_comments` + LRU_TEST dedup, public `extract` wiring.
