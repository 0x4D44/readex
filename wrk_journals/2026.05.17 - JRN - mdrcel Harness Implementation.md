# JRN — mdrcel Differential Test Harness Implementation

**Started:** 2026-05-17
**Supervisor:** Claude (orchestrating agent teams; not implementing/debugging directly)
**Branch:** `feat/harness-implementation`
**Governing design:** `wrk_docs\2026.05.17 - HLD - mdrcel Differential Test Harness.md`
**Sibling (oracle team):** `wrk_docs\2026.05.17 - HLD - mdrcel Oracle Adapters.md`

---

## Supervision model

- Per stage: **implement** (general-purpose agent, TDD) → **review** (`code-reviewer` skill) → **apply feedback** (`code-review-apply-feedback` skill) → **devil's advocate** challenge → fix loop → **supervisor commit**.
- Supervisor protects context: high-level direction only; agents return concise summaries (decisions, test pass/fail, files, blockers), never file dumps. No direct testing/debugging by supervisor.
- Sequential stages (each builds on the prior). Commit at every stage boundary (atomic, only authored files; user 0x4D44).
- Final: independent verification team compares implementation vs HLD; iterate until 100%.

## Stage plan (from HLD §12)

| # | Stage | Status | Commit |
|---|---|---|---|
| 0+1 | Bootstrap: `.gitattributes`+renormalize, workspace, mdrcel→lib, `benchmark` skeleton, repo `.gitignore` | ✅ done | `.gitattributes` `8541ea2`; bootstrap commit this turn |
| 2 | `metrics.rs` — tokenizer, Jaccard, norm-Levenshtein, precision + property tests (TDD) | ✅ done | this turn |
| 3 | `corpus.rs` — urls.tsv loader, snapshot hashing, `fetch` subcommand, 3 samples | ✅ done | this turn |
| 4 | `oracle.rs` — serde types vs reconciled contract (schema gated; R1 mitigation) | ✅ done | this turn |
| 5 | mdrcel lib API (`extract`/`Extracted`/`Options`/`ExtractError::NotImplemented`) + `crate_run.rs` | ✅ done | this turn |
| 6 | `score.rs` — hierarchy → results.json (host metadata) | ✅ done | this turn |
| 7 | `report.rs` — report.md generation | ✅ done | this turn |
| 8 | `regression.rs` — host-pinned baseline; commit first baseline (the floor) | ✅ done | this turn (code + baseline) |
| V | Verification team: implementation vs HLD, iterate to 100% | ✅ done | this turn |

## Key decisions & risks

- **R1 — Stage 4 blocked:** `benchmark/oracles/contract.schema.json` absent (oracle team not delivered). Mitigation: build `oracle.rs` serde types from the reconciled HLD §5 contract (we co-specified it in detail); add a `#[ignore]`d schema-conformance test enabled when the schema lands. Scoring/report/regression proceed against fixture JSON. Flagged to oracle team.
- **R2 — Cargo.lock byte-stability — CORRECTED (devil's advocate R3, 2026-05-17).** My original framing here was wrong and seeded a false rationale in commit `8541ea2`'s message. **Forensic fact:** `bfeccf4:Cargo.lock` had **zero CR bytes** when first committed; `core.autocrlf=true` strips CR on check-in, so the blob was already LF-clean and `git add --renormalize .` was a **no-op** for it. The commit message's "repairs pre-committed Cargo.lock byte-stability" is **factually false** — the outcome (byte-stable) is correct, but not for the stated reason. The commit is **not** amended (CLAUDE.md). **Accurate rationale, recorded here as the authoritative record:** committing `.gitattributes` before any `-text` artifact is **prophylactic for *future* byte-stable artifacts** — the HTML snapshots (Stage 3) and the oracle `contract.schema.json` (Stage 4), which MUST have the `-text` rule in effect *at `git add` time* or `text=auto` will mangle them irreversibly. Cargo.lock was never at risk. The ordering discipline stands; only the Cargo.lock-specific justification was fiction. Future sessions/oracle team: do not cargo-cult "renormalize repaired the lock."
- **R3 — mdrcel is a binary (`src/main.rs`):** parent brief mandates a library crate. Bootstrap converts to lib; Stage 5 defines the real API surface.
- **D1 — Feature branch:** branched off `main` per CLAUDE.md (default-branch rule) + multi-agent concurrency (oracle team + `wrk_b2_spike/` active).
- **D2 — Scope hygiene:** commits are atomic and contain only files this team authored. `wrk_b2_spike/` and the oracle team's HLD V1–V5 are left untracked (not ours).

## Forward obligations (carried from Stage 0+1 devil's advocate; must be honoured by later stages)

- **O1 (→ Stage 4):** Before the FIRST commit of `benchmark/oracles/contract.schema.json`, verify `git check-attr -a benchmark/oracles/contract.schema.json` reports `-text`. `benchmark/oracles/` is the **oracle team's tree** — Team #1 does NOT create files there (incl. no `.gitkeep`); the `.gitattributes` rule legitimately pre-exists the file by design (this is the point of M0 ordering, not a defect).
- **O2 (→ Stage 6 & 8):** The declared repro host (`Anvil`) currently exists only as prose in `benchmark/README.md`. Stage 6 MUST emit the host identity into `results.json` run metadata (machine-readable); Stage 8's regression comparator MUST read it and print `BASELINE ADVISORY (host X, expected Y)` + skip gating on mismatch (HLD §9). Bootstrap correctly did NOT pre-build a host-detection module (would be premature abstraction).
- **O3 (→ all stages / docs):** Minimum `rustc` is **1.85** (implied by `edition = "2024"`); reproducibility is single-host by design (HLD §2.9). No `rust-toolchain.toml` — both reviewers concur it would imply a cross-machine guarantee the design explicitly disclaims.
- **O4 (→ Stage V) — PARTIALLY discharged; original caveat persists (corrected after Stage-6 verification probe; see honesty note in Stage-6 log).** Stage 6 wired `score.rs` as a real non-test consumer and removed the now-satisfied per-item `#[allow(dead_code)]`. **Accurate final state (verifier probe, not the over-claim):** for the `benchmark` BIN crate under `clippy --workspace --all-targets -D warnings`, unused **`pub` fns ARE now caught** (a non-test consumer exists) — so the *kept* scoped `#[allow]`s on `pub` items (`corpus::load`, `oracle::wire_name`, …) remain genuinely **load-bearing**. BUT unused **private items and never-constructed enum variants** in the bin crate are STILL NOT compiler-caught (the original Stage-2 caveat — unchanged). Therefore O4 is **not fully closed**: dead private code / dead enum variants in `benchmark` rely on convention + review, not the compiler. **Stage V MUST manually audit** that every kept `#[allow(dead_code)]` is still justified and that no private item / enum variant is silently dead. Do NOT re-assert "genuinely enforcing / proven" — that is false for private/variant items.
- **O9 (→ corpus/gold curation & baseline re-bless; ties to O5/§2.7):** The regression gate compares Coverage/word_count MAGNITUDES only — it does NOT detect a change in the *reference basis* (Trafilatura→gold) behind equal numbers (`UrlRecord` does not store which reference was used; storing it is premature at M1 per §3). Mitigation = the manual re-bless ritual: after ANY gold-set change the committed baseline MUST be re-captured (HLD §2.7/§9 cp+commit). Stage V must verify this is documented in `regression.rs`/README; a future milestone may add reference-kind to `UrlRecord` to make it checkable (out of scope now).
- **O8 (→ Stage 7 report.rs — forward-contract):** The agreement-on-disagreement distribution is only sampled when crate + both oracles are all valid Ok + oracles disagree (<0.5) — at M1 and early real runs that is ≈0 samples. Stage 7 MUST render it with its sample N and flag it non-representative below a threshold; it must NEVER print "crate sides with X%" without "(N=k of m)". Stage 6 records honest per-URL `Option<Agreement>` only (no aggregator exists yet — correctly out of scope). Forward-contract comment added in `score.rs`/`report.rs`; Stage V verifies Stage 7 honoured it.
- **O7 (→ crates.io publish milestone, OUT of this harness build's scope; Stage V records only):** `#[non_exhaustive]` on the PUBLIC `mdrcel::ExtractError` is deliberately NOT added now. Reason: it would force the in-workspace harness (`crate_run.rs`) into a wildcard match, defeating the Stage-5 #1 intentional exhaustive Bug-E2 compile-fence. `mdrcel` is pre-1.0 (0.2.0) and the parent brief explicitly permits breaking API changes pre-1.0, so adding `ExtractError` variants now is an allowed breaking change. At the crates.io-publish milestone (parent-brief Milestone 4, not part of this harness deliverable), `#[non_exhaustive]` must be added for external consumers AND at that point the harness consciously adopts a documented wildcard + explicit status mapping. The two are in genuine tension; the compile-fence wins for now.
- **O6 (→ Stage V / known limitation):** On `OracleTimeout`, `std::process::Child::kill()` kills only the direct child; the Trafilatura adapter re-execs into its venv interpreter (sibling §4) and Node spawns workers, so a timeout can orphan the real worker (CPU/RAM/file-handle leak across a corpus run). The VERDICT is still correctly `OracleTimeout` (not a Bug-E2/correctness issue). Tree-kill (Windows Job Object / Unix process-group) needs platform deps — DEFERRED for the v1 single-host dev sandbox; documented as a known limitation in `oracle.rs`. Stage V verifies it is documented, not fixed.
- **O5 (→ corpus curation / §2.7 baseline freeze / Stage V):** The 3 Stage-3 sample snapshots deliberately EXCLUDE the table-heavy `sec_edgar` shape (the consumer breaking case, gold #3/#5/#6). Stages 6–8 passing on the 3 synthetic samples is **NOT evidence** on EDGAR/table extraction. The separately-tracked real ≥50-URL corpus curation MUST include ≥1 table-heavy SEC filing + ≥1 table-heavy regulator page, and that is **blocking for the §2.7 baseline freeze / §12 step-8 first baseline**. Stage V must verify this obligation is legible and unmet-baseline is not frozen without it. A synthetic fake EDGAR was deliberately NOT created (mock-implementation trap).

## Open decisions for Arthur (non-blocking; build continues meanwhile)

- **DEC-3 (Stage 5 `thiserror` — SUPERVISOR DECIDED, reversible, low-stakes).** Devil's advocate flagged `thiserror = "2"` (proc-macro tree) pulled for ONE fieldless `ExtractError::NotImplemented` variant — the DEC-2/minimal-deps lesson. Although the parent brief lists `thiserror` as an allowed lib dep, the brief's governing PRINCIPLE is "minimal & conservative", and the crate is internally inconsistent (defers scraper/html5ever on minimal-deps grounds, pulls a derive-macro tree for one string). **Decision: drop `thiserror` for M1; hand-write `impl Display + std::error::Error` (~8 LOC).** Yields a genuinely **zero-dependency M1 library floor** (a real virtue for a crates.io lib's first cut). Trivially reversible: re-add `thiserror` at the milestone where `ExtractError` actually grows variants/`#[from]`/`#[source]` (noted in `lib.rs` doc), exactly as scraper/html5ever are deferred. Within autonomous scope (small dep/impl choice, principle-aligned, not architectural). Arthur: revert trivially if you prefer the listed dep.

- **DEC-2 (Stage 3 `fetch` HTTP mechanism — SUPERVISOR DECIDED, reversible, veto welcome).** `ureq` pulled 51 of 71 benchmark crates (full rustls + ICU tower + 2× webpki-roots) into the committed lockfile for a developer-only, out-of-band, run-~50-times GET that is provably never on the scoring path. Devil's advocate + code-reviewer both flagged; DA argued decisively it violates HLD §3 "smallest thing that works" and that the "everything in Rust" steer governs the harness's *own logic/report*, not throwaway scaffolding (HLD §6 explicitly calls fetch out-of-band). **Supervisor decision: replace `ureq` with `std::process::Command` shelling `curl` (ships Win10/11 + Git), README-documented.** Rationale: keeps the scoring path hermetic & dep-free; removes 50 crates of supply chain better undone now than carried through Stages 4–8; trades a pinned Rust dep for an unpinned system tool ONLY on the throwaway path; the committed snapshots (the real artifacts) are reproducible regardless of how curl behaved. Touches Arthur's "everything in Rust" steer → flagged. Trivially reversible (re-add `ureq`). **Arthur: veto → I restore `ureq`.**

- **DEC-1 (HLD §8 tokenizer spec — casefold).** Devil's advocate PROVED false-negatives: lowercase+NFC (the literal §8 spec) is NOT Unicode caseless matching — `tokens("ﬁle") != tokens("file")`, MICRO SIGN U+00B5 ≠ Greek mu, ﬀ/ﬃ ligatures split. These occur in our SEC/regulator corpus, so the "English/Western-European v1" scope only partially excuses it; Coverage/Precision get artefactually depressed (measuring-instrument bias). **Mitigation in place:** documented as a known limitation + pinning test `known_limitation_ligature_not_casefolded` (so a future fix is intentional/reviewed). **Decision needed:** change HLD §8 "lowercase, NFC" → **`NFKC_Casefold`** for v1? This touches the shared tokenizer-comparability contract, so it is a spec change, not a Stage-2 patch. **Supervisor recommendation: yes, adopt NFKC_Casefold for v1.** Deferred to Arthur; build proceeds on documented current behaviour.

## Running log

- **2026-05-17 — setup:** Recon done; branch `feat/harness-implementation` created; journal opened; risks R1–R3 logged. Proceeding to design-baseline commit then Stage 0+1.
- **2026-05-17 — Stage 0+1:** Implemented by `impl-stage0`. Code-reviewer (code-reviewer skill): **clean, no required changes**. Devil's advocate found 7 items the reviewer structurally could not: R3 (commit-msg fiction — corrected in R2 above; **DA earned its seat here**), R2 (phantom-file rule = intentional, → O1), R4 (prose-only host → O2), R5 (redundant `resolver="3"` + rustc floor → O3), R6/R7 minor. Apply-feedback agent (code-review-apply-feedback skill): removed redundant `resolver="3"`, added `.gitignore` trailing newline; **all four quality gates PASS** (build/clippy-Dwarnings/fmt/run=`no corpus`). `.gitattributes` committed `8541ea2`; bootstrap committed `edf3501`. R1 post-commit verify: `git ls-files --eol` uniform `i/lf`, `Cargo.lock` `attr/-text` — invariant holds.
- **2026-05-17 — Stage V (final verification):** Independent team — 2 HLD-coverage verifiers + 1 unanchored devil's advocate, run in parallel. **Verifier A (§2/§5/§8 + Bug-E2 core): 100% MET (modulo recorded-deferred).** **Verifier B (§6/§7/§9/§12/§10/§11): 100% MET (modulo recorded-deferred).** **Devil's advocate: the "100% complete vs the HLD for the M1 harness deliverable" claim is DEFENSIBLE — explicitly NOT a rubber-stamp** (anti-Bug-E2 doctrine enforced by executable code with real CI exit-code consequences; no end-to-end laundering path found across oracle→score→report→regression; floor path execution-proven, deterministic; obligation ledger audited item-by-item against code, every deferral labelled a deferral, not done-substituted). Gates independently re-run by all three: 203 bench + 9 lib = 212 tests, 0 failed, 0 ignored; clippy -D warnings clean; fmt clean; `cargo run` ×2 deterministic, gate engages vs committed baseline, M1 self-compare = VACUOUS/not-a-substantive-pass, exit 0. **No functional/MISSING/PARTIAL gaps.** One honesty-hygiene residual (R3-class): stale `--renormalize` Cargo.lock fiction surviving in `.gitattributes` line 4 (commit-msg + journal were corrected; this 3rd copy missed) → SCRUBBED (comment-only, rule lines byte-identical via `git diff`+`git check-attr`, gates green). **R3 honesty correction now fully closed across all 3 artifacts.** No manufactured further iteration — the DA verdict is the deliverable genuinely holds within scope.

## Closeout — scope-honest final statement (per the devil's-advocate discipline)

**DONE: the Milestone-1 harness *instrument* is 100% complete vs the governing HLD.** All §12 build-order steps 0–8 delivered & committed atomically; §2 (9 locked decisions), §5 (oracle contract consumer side), §8 (metrics), §9 (report/regression) MET; the Bug-E2 doctrine is enforced end-to-end as executable code; the M1 floor is HONEST (committed baseline + report + gate show every URL not_scored / oracles oracle_error / zero trusted scores / VACUOUS-not-a-pass — nothing laundered). All quality gates green; output deterministic.

**DELIBERATELY NOT IN SCOPE (honestly deferred, named owners — stating these as "done" would itself be the absence-as-evidence Bug-E2):**
- HLD §6 "≥50 URLs" corpus + real table-heavy SEC/EDGAR snapshots — O5/§11.4 separate curation; BLOCKING for the §2.7 gold-set freeze; the committed baseline is the *M1 documented floor*, NOT the frozen §2.7 baseline.
- Oracle-contract verification (O1) — `contract.schema.json` not yet delivered by the oracle team; the schema-conformance test auto-activates on arrival; fidelity is to the *reconciled HLD §5 restatement*, the *normative* schema is formally UNVERIFIED until O1.
- The `mdrcel` extraction algorithm — M1 floor returns `NotImplemented` by design (parent-brief future milestones).
- O7 `#[non_exhaustive]` (crates.io-publish milestone); O6 timeout-orphan tree-kill (v1+); O4 private-item/enum-variant dead-code (convention-not-compiler-enforced in the bin, accurately documented).

**OPEN DECISIONS FOR ARTHUR (flagged, reversible, non-blocking):** DEC-1 (HLD §8 → NFKC_Casefold tokenizer spec change — supervisor recommends yes; pinning test locks current behaviour); DEC-2 (`fetch` via system `curl` not `ureq`); DEC-3 (`thiserror` dropped, mdrcel zero-dep). All recorded with rationale; revert any trivially.

- **2026-05-17 — Stage 8:** Implemented by `impl-stage8` (`regression.rs`, host-pinned gate, status-transition matrix; 195 bench + 9 lib tests; NO baseline tooling per §9; epsilon-tolerant thresholds well-justified). Code-reviewer: **clean**. Devil's advocate (2 genuine + strong credit): **#2c FIX-NOW** — `Scored→NotScored` gates `ScoreLost` for ALL reasons, but `ReferenceUnavailable`/`ReferenceEmpty` = oracle changed under us, NOT crate; a flaky Trafilatura timeout on the declared host would red-CI "crate score LOST" while the crate is perfect (Bug-E2 conflation — the gate's one job is to distinguish these) → split arm by reason (crate-owned gating; reference/oracle-owned listed-non-gating). **#4b FIX-NOW** — at M1 floor the verdict block (read first) says "clean" having compared NOTHING; vacuous presented as substantive pass (R3/§5 doctrine) → render "clean but VACUOUS". **#2a/#4/#5 DOCUMENT** — reference-basis-change blind spot (→ O9 re-bless), threshold coarseness, non-atomic-prepend-on-scratch. **Credit (DA explicit):** `canonical_host_of` is a TRUE one-line delegate (NOT a drift hazard — unlike Stage-7 #1); word_count recomputed not wire; serde ERRORS not silent-defaults (no `#[serde(default)]` laundering); `should_fail`/advisory-exit/host=None all correct. Consolidated apply-feedback applied ALL 5: #2c `RegressionKind::ReferenceLost` (non-gating) vs `ScoreLost` (gating), `classify` matches `NotScoredReason` EXHAUSTIVELY (no wildcard — Bug-E2 compile-fence), `is_gating()`/`should_fail()` split; #4b `compared_trusted_pair` + "clean but VACUOUS — NOT a substantive pass" in block AND stderr; plus the agent's sound beyond-literal `GateOutcome::ReferenceLostNonGating` so the stderr never falsely says "clean" for a reference-loss-only run (exit-neutral, doctrine-consistent); #2a/#4/#5 docs. 203 bench + 9 lib green. Focused verification: **PASS, clean to commit** — arms hand-verified, compile-fence intact, no regression; **score.rs `M` anomaly investigated & RESOLVED**: it is the REQUIRED `canonical_host_of` pub one-line delegate (#2c host-pin; build fails without it; true single-source delegate, NOT drift) → belongs in the atomic Stage-8 commit (7 files). ver 0.8.0→0.8.1. Code committed; supervisor now performs the deliberate manual M1-floor baseline capture (HLD §9 cp+commit ritual, no tooling) as a SEPARATE commit, then validates the gate engages end-to-end against it.
- **2026-05-17 — Stage 7:** Implemented by `impl-stage7` (`report.rs`, pure `render_report`; 168 tests). Code-reviewer: **clean to commit**. Devil's advocate (3 genuine + explicit credit): **#1 FIX-NOW** two independent `gold.tsv` parsers (`GoldSet`/score.rs ✕ `GoldBands`/report.rs) with ZERO cross-check — count-preserving column-swap silently diverges band-vs-text on the §7 highest-authority signal; spec EXPECTS post-freeze human edits → add a differential cross-check (GoldBands keys ✓ GoldSet keys, loud FAIL on mismatch). **#4 FIX-NOW** honesty inconsistency — O8 non-representative discipline applied to agreement but NOT to the headline Coverage/Precision means (`0.95 (N=3 of 50)` unflagged) → mirror the pattern. **#3b DOCUMENT** disagreement ranking structurally can't surface "both oracles wrong same way, crate faithfully wrong". **#7a FIX** `|` in a legal URL corrupts the human-read table → cell-escape. #5 cheap (comment + debug_assert). **Credit (DA explicit):** O4 honesty NOT over-claimed this time — `wire_name` removal justified by a real traced main-reachable consumer, comment correctly scoped (the Stage-6 correction was learned); M1 "not a result" visibility genuinely well-designed (bold top-line verdict); write-ordering correct. Consolidated apply-feedback applied ALL 5: #1 gold/band cross-check (`&GoldSet` threaded; `GoldSet::urls()` minimal read-only accessor — sole score.rs change, purely additive, `score_url`/gating byte-identical; mismatch → loud FAIL row both directions, deterministic; loud-row not hard-error — a panic would suppress the honest `n/a` evidence = Bug-E2 "hide the evidence"); #4 `SCORED_FRACTION_MIN=0.5` low-fraction banner mirroring O8; #3b blind-spot preamble; #7a `md_cell` escape on all free-text cells; #5 sequential-order comment + `debug_assert!(is_finite)`. Focused verification re-review: **PASS, clean to commit** — all 5 correct, M1 floor honest + byte-deterministic (2 runs identical bar timestamp), core gating provably untouched, **177 tests** (168 bench + 9 lib) + clippy-Dwarnings/fmt green. ver 0.6.1→0.7.1 (whole Stage 7 = one uncommitted unit; reconciled). Committed.
- **2026-05-17 — Stage 6:** Implemented by `impl-stage6` (integrative anti-Bug-E2 stage; 145 tests; explicit `ScoreOutcome::Scored|NotScored` — no sentinel floats; O2 dep-free host; **O4 discharged + clippy dead-code enforcement PROVEN via probe**; `StatusDetail` extension justified). Code-reviewer + devil's advocate **both independently confirmed the core `score_url` gating engine is SOUND and exhaustively tested — DA's attacks on the core explicitly failed (design is right)**. Real issues all at the input boundary / provenance: **Risk 1** `"unknown-host"` shared constant → Stage-8 Bug-E2 (two detection-failure machines collide → false baseline validity) — fix-closed; **Risk 2** empty gold file silently `NotScored` not hard error (asymmetric w/ missing-file; §7 highest-authority signal) — add `ExpectedTextEmpty`; **Risk 7a** `runs/<ts>` 1s-collision silently overwrites a run — uniquifier; **Risk 4/7b** → O8 + Stage-8 host-normalization forward-contract. Risk 5 (hang) measured 489ms M1 run — NON-issue; Risk 6 O4 probe honest (levenshtein genuinely main-reachable); serde anti-sentinel verified on the wire. Consolidated apply-feedback applied ALL 4: Risk 1 host fail-closed (`HostDetectionFailed` hard error, host resolved before any scoring → no poisoned results.json; canonical form trim/strip-FQDN/lowercase; testable seam — agent's sharp honest finding that Windows resolves `hostname.exe` from System32 regardless of PATH, so the seam is the only honest failure-path test); Risk 2 `GoldError::ExpectedTextEmpty` symmetric w/ missing, same `tokens().is_empty()` predicate; Risk 7a atomic run-dir w/ numeric suffix (no silent overwrite); Risk 4/7b forward-contracts (O8 + Stage-8 host-compare). 151 tests green; M1 floor unchanged (`host=anvil`, nothing laundered, sub-second). Version 0.6.0→0.6.1. **Honesty correction (verification probe — verifier earned its seat, R3-class):** the agent's "O4 DISCHARGED / dead-code GENUINELY ENFORCING / PROVEN" claim was OVER-STATED. Probe proved enforcement is *partial*: unused `pub fn` IS caught, but unused **private items / never-constructed enum variants** in the bin crate are STILL NOT caught (original Stage-2 caveat persists). O4 above corrected to the accurate partial state; the false "proven enforcing" wording is being scrubbed from `metrics/crate_run/oracle/corpus.rs` comments (doc-only apply-feedback, no logic). Core `score_url` gating engine independently re-confirmed SOUND & undisturbed; the 4 functional fixes verified correct. Committing after the wording scrub.
- **2026-05-17 — Stage 5:** Implemented by `impl-stage5` (mdrcel M1 API skeleton + `crate_run.rs`; 110 tests = 9 lib + 101 bench; `extract` delegates to `extract_with` so non-divergence is structural; panic isolation via `catch_unwind`). Code-reviewer: **clean to commit**. Devil's advocate (sharp, 2 decisive): **#1** catch-all `Err(_)=>CrateError` + `#[allow(unreachable_patterns)]` is a Bug-E2 vector — parent brief design-COMMITS to `ContentTooShort`; wildcard would silently launder a valid short-content verdict, no compiler signal → **FIX NOW** make match exhaustive (compile-fence). **#2** `thiserror` for one variant → **DEC-3** drop it, hand-write (zero-dep M1 lib floor). #3 forward the `Ok(empty)`→Stage-6 status-gate hazard on producer side (doc). #5a drop `Eq` (unused over-constraint, future float-field breaking fork). #5c `#[non_exhaustive]` tension resolved → O7 (defer to publish milestone; compile-fence wins now). #4/#5b non-issues; panic isolation verified genuinely sound (no statics/OnceLock/thread_local to poison). Apply-feedback applied ALL 4. **#1 compile-fence PROVEN**: temp `ExtractError::ContentTooShort` → exactly `error[E0004] pattern Ok(Err(ContentTooShort)) not covered` at the Bug-E2 site, panic arm unaffected; reverted, gates re-green. **mdrcel is now a true zero-dependency crate** (`cargo metadata`: `deps: []`). #5a `Eq` dropped (PartialEq kept); #3 producer-side hazard doc added; DEC-3 thiserror removed + reversibility recorded. ver mdrcel 0.2.0→0.2.1, benchmark 0.5.0→0.5.1 (patch, in-progress precedent). 9 lib + 101 bench tests green; clippy-Dwarnings/fmt/run green. Agent made a flagged in-scope doc-consistency fix (lib/crate_run docs that contradicted the new fence). Devil's advocate earned its seat again (#1 reviewer-invisible Bug-E2 vector). Committed.
- **2026-05-17 — Stage 4:** Implemented by `impl-stage4` (R1 mitigation: consumer side from reconciled §5; 89 tests, no new deps). **Code-reviewer AND devil's advocate independently found a CRITICAL, empirically-reproduced defect** (DA reproduced on the Windows box, ~64KB threshold): stdout drained only AFTER child exit → any oracle JSON >~64KB (every real SEC 10-K — THE the consumer-critical case) fills the pipe, blocks the child, → false `OracleTimeout`. Invisible to all 89 tests (fixtures <1KB); a doc comment *affirmatively asserted it safe* with false reasoning (atomicity-of-write ≠ non-blocking). This is the exact measuring-instrument Bug-E2 the project exists to prevent — **process earned its keep emphatically.** Also: #6 schema probe verifies field-NAME shape only, over-claims as "verified" (R1 stays unproven); #3 exit0+ok:true+non-null-error laundered to Ok while doc claims "either alone = failure"; #2a timeout-kill orphans venv grandchildren (→ O6). #4 non-UTF-8/truncated/BOM handling genuinely solid (credit). Consolidated apply-feedback dispatched; critical fix = std-only concurrent reader thread + ≥256KB regression test (must fail-before/pass-after). Apply-feedback applied ALL 7 with an honest methodological correction: joining the reader on the timeout path re-stalls (orphaned grandchild holds the write-end) → it **detaches reader on timeout/error, joins only on success** (justified by §4 exit-code propagation; consistent with O6). #1 fix **proven**: ≥256KB regression test deadlocked at 90s (mdtimeout-killed) BEFORE, passes 0.05s AFTER. #3 contradiction guard added; #6 schema probe strengthened (types+nullability) + "UNVERIFIED until O1" caveat; #5 word_count pinning test; #7 debug_assert snapshot-absolute; #2a orphan documented (not fixed, O6). ver 0.4.0→0.4.1 (patch, bugfix on unreleased). Focused verification re-review: **PASS, clean to commit** — fix correct, timeout safe & fast, non-UTF-8→OracleError intact, no regressions, 94 tests green. Hygiene: agent `notes/` scratch not gitignored → added `notes/` to repo `.gitignore` (prevents recurring leakage across remaining stages). Committed.
- **2026-05-17 — Stage 3:** Implemented by `impl-stage3` (67 tests; TSV loader, content-addressing, drift=hard-error, isolated `fetch`, 3 synthetic samples, hand-rolled civil-date; ver 0.1.0→0.2.0). Code-reviewer: fix-then-commit (untested overwrite-refusal; wrong SHA-256 comment). Devil's advocate (sharp): #1 `ureq` = 51/71 crates supply-chain bloat for out-of-band util → DEC-2 (replace w/ curl); #2 **Bug-E2** — missing-snapshot (file absent, filename valid) passes `load` as Ok while module doc falsely claims coverage → add existence check; #3b fetch re-run laundered stale date; #4 no table-heavy EDGAR sample, correctly scoped but silent → O5; #3a/#6 doc-backstop notes; #5 civil-date NON-issue (remove unreachable branch only). Both agree civil-date hand-roll is the *correct* minimal-deps call. Apply-feedback applied ALL 7 items: `ureq`→shelled `curl` (lockfile 141→82 crates, scoring path now HTTP-stack-free); `load_checked` + `CorpusError::SnapshotMissing` closes the Bug-E2 missing-snapshot gap, module doc corrected (no more overclaim); stale-date laundering fixed; EDGAR-gap caveat in `urls.tsv` (→ O5); doc backstops; unreachable civil-date branch deleted; wrong SHA-256 comment fixed. Self-initiated DRY (`read_manifest_with_lines` shared core) — sound. Ver 0.2.0→0.3.0. Focused verification re-review of the two substantive fixes (#1 curl swap, #2 Bug-E2): **PASS, clean to commit** — curl call-graph isolation airtight, no partial file on failure (tested), Bug-E2 closure proven by differential test. **70 tests green**, clippy-Dwarnings/fmt/run all green. DEC-2 (curl) flagged to Arthur. **Verify-at-commit clarification (honesty record):** DA #6b suggested re-deriving `sha256(committed blob)` and comparing to the filename — that check is conceptually WRONG and inconsistent with DA's own #6a: the scheme is content-addressed by **URL** (`sha256(url)[:16]`), not by file content. The real invariants both verified at commit: (i) `git check-attr` → all snapshots `text: unset`/`eol: lf` (verbatim, byte-stable); (ii) independent recompute confirms `filename == sha256(url)[:16]` for all 3 rows (also enforced in-code by the drift hard-error, which the passing `load_checked` proves). Do NOT cargo-cult DA #6b's content-hash check.
- **2026-05-17 — Stage 2:** Implemented by `impl-stage2` (strict TDD, 33 tests). Code-reviewer: **clean to commit**, two optional suggestions. Devil's advocate (sharp): #1 `J(∅,∅)=1.0` documented as feature but un-flagged Bug-E2 vector; #6a missing `precision` primitive → ∅-convention silently delegated to Stage 6; #2 lowercase+NFC ≠ casefold, **proven** ﬁ/µ false-negatives (→ DEC-1, escalated); #4 proptest ASCII-only, blind to the Unicode pipeline (its own stated risk); #5 module-wide `#![allow(dead_code)]` (→ tightened + O4 empirical finding); #3/#6b/#6c doc gaps. Apply-feedback applied ALL: hazard doc + Stage-6 contract; new `precision()` with deliberately-different ∅=0.0 + tests; stronger proptest (smaller alphabet + Unicode + idempotence + empty-inclusive); per-item `#[allow]`+tripwires; limitation doc + pinning test; complexity/cost docs. **47 tests PASS** (33+14), clippy-Dwarnings/fmt/run all green. Devil's advocate again earned its seat (reviewer cleared; DA caught the Bug-E2 vectors). Committing.

---

## PHASE 2 — Oracles + Real Corpus (post-merge; Arthur-directed 2026-05-17)

**Context:** Phase-1 harness merged to `main` (ff `bfeccf4..1eaec6c`). Arthur's premise "we've got oracles" was FALSE — `benchmark/oracles/` is empty (only untracked design drafts V1–V5 + canonical). Methodology is oracle-first/baseline-first: NO extraction code until the instrument is real. Arthur chose: drive **oracles ∥ corpus** in parallel, then meaningful re-baseline, then M2.

**Branch:** `feat/oracles-and-corpus` (off `main`; CLAUDE.md default-branch rule + multi-agent concurrency).

**Ownership change — DEC-6 (supervisor-recorded):** Arthur folded oracle implementation into this team's remit. `benchmark/oracles/**` is now OURS to author (was the oracle design team's per old D2/O1). Canonical oracle HLD `wrk_docs/2026.05.17 - HLD - mdrcel Oracle Adapters.md` (final, converged after V1–V5) is committed at Stage A as the now-governing sibling design (the harness HLD is committed; its sibling should be too). Interim V1–V5 stay UNTRACKED (review history, not deliverables) — do not commit, do not delete.

**Seam recon (differential discipline applied to the design itself — value of looking before building):** concrete mismatches found between the canonical oracle HLD §3.2 and the committed harness consumer `oracle.rs::OracleResult`:

- **DEC-4 — `contract_version` (supervisor-decided, reversible, FYI Arthur):** oracle HLD §3.2 mandates `contract_version: integer` as field #1 (self-describing rows; a contract bump shows as a baseline diff — its whole stated rationale). Harness `OracleResult` (10 fields, NO `deny_unknown_fields`) has NO such field → an emitted `contract_version` is silently dropped, anti-drift intent defeated (latent Bug-E2). Resolution (only one consistent with the HLD's own rationale): adapters EMIT it + schema marks it REQUIRED + **Stage C adds `contract_version` to harness `OracleResult` + a schema-test assertion**. O1 doing its designed job (schema is the normative arbiter; reconciled §5 always provisional). Not architectural; reversible.
- **SEAM-2 — Windows venv invocation:** harness invokes BARE `python`/`node` from PATH (recon-confirmed: python 3.12.10, node 24.12.0). `run.py` MUST self-locate its pinned Trafilatura (re-exec into `./.venv` per oracle HLD §4 / O6) — the harness activates no venv. `run.mjs` fine (Node resolves local `node_modules` from script dir).
- **DEC-5 — `fetch` curl has NO User-Agent (supervisor-decided, reversible, FYI Arthur):** SEC EDGAR fair-access blocks default-UA curl. Capturing that block page as a snapshot = Bug-E2 (poison-as-data). Resolution: real UA w/ contact (`mdrcel-benchmark/0.1 (+martingdavidson@gmail.com)`); Stage C adds a default UA to `curl_download()` (strictly better — default-curl-block is a latent defect for any polite fetch). Stream B may fetch out-of-band with `curl -A` meanwhile (HLD permits out-of-band).

**Risks (Phase 2):**
- **R4 (seam reconciliation):** DEC-4 + SEAM-2 + DEC-5 — resolved in design above; VERIFIED in Stage C (dormant schema test must activate & pass = O1 discharged).
- **R5 (corpus authenticity — Bug-E2, CRITICAL):** a fetched block/captcha/paywall/SPA-shell/error page committed as a "snapshot" is poison presented as data. HARD GATE: every snapshot positively content-verified as the real target; synthetic/fake EDGAR FORBIDDEN (mock trap; O5 line held). Can't capture honestly → drop & repick, never fake.
- **R6 (determinism):** Trafilatura/Readability must emit run-twice byte-identical JSON (HLD determinism clause) — explicit conformance test.
- **R7 (pinning):** unpinned upstream = non-reproducible signal. Locks committed, versions in README + emitted `oracle_version`. Single-host §2.9 unchanged (native libxml2/ICU still unpinnable — documented).
- **R-NET (honest-failure gate):** Streams A & B both need outbound network (PyPI/npm; URL fetch). If the sandbox is offline, agents MUST stop and report — NOT synthesize. "Couldn't" is never laundered into "done" (core project ethos).

**Discharges:** Stage A closes R1/O1 (schema lands, test activates). Stage B closes O5 (real table-heavy EDGAR/regulator in corpus). Stage C closes O9's prerequisite (meaningful re-baseline) — but the §2.7 *frozen* baseline still needs the hand-decided gold set (Arthur-blessed; Stage C only *proposes* it).

**Stage plan:**

| # | Stage | Status |
|---|---|---|
| A | Oracle adapters (Trafilatura+Readability) + `contract.schema.json` + conformance/determinism tests; pinned+locks; harness schema test activates green | ✅ done `e17c4bd` |
| B | Real ≥50-URL corpus incl. ≥1 table-heavy EDGAR 10-K + ≥1 table-heavy regulator; authenticity-gated; urls.tsv caveat rewritten honestly | ✅ done `85ad674` |
| C | Integration: DEC-4/DEC-5 harness reconciliation; end-to-end real run; meaningful M1-floor re-baseline; propose gold set (freeze = Arthur) | pending (after A∥B) |

A ∥ B (independent per oracle brief). Per-stage: implement(TDD) → code-reviewer + devil's-advocate (∥) → apply-feedback → focused verify → supervisor commit (atomic, authored-only, Co-Authored-By trailer). M2 (extraction) explicitly DOWNSTREAM of Stage C + Arthur's gold-set bless — NOT this phase.

### Phase-2 running log

- **2026-05-17 — Phase-2 setup:** Post-merge premise-check found `benchmark/oracles/` EMPTY (Arthur's "we have oracles" false; R1 live). Branch `feat/oracles-and-corpus`. Seam recon (Explore) found DEC-4 `contract_version` mismatch + SEAM-2 venv + DEC-5 UA BEFORE building (differential discipline applied to the design itself). Tasks #10/#11/#12 (#12 blocked by #10,#11). Stage A ∥ Stage B dispatched concurrently with an R-NET honest-failure gate (offline ⇒ stop, never synthesise).
- **2026-05-17 — Stage B (corpus) — committed `85ad674`.** impl: network OK; 51 real URLs, all 7 shapes (8/8/8/8/8/5/6), O5 met (Apple FY2025 10-K 62 tables, FY2019 124, IBM legacy, gov.uk HMRC, Fed H.15/OMO); 8 URLs honestly dropped+replaced (redirect/gzip/404/bot-block) — R5 held; load_checked clean; hashes vs corpus.rs known vectors. Code-reviewer: **PASS** (independently recomputed sha256 drift for ALL 51 rows; perfect 51↔51 bijection; O5 confirmed by opening filings). Devil's-advocate (full 51-byte sweep — **earned its seat**): R5/Bug-E2 genuinely holds (no block/captcha/SPA/gzip/truncation even on 10.7 MB Berkshire; no false-breadth padding) BUT 3 honesty defects the PASS missed — **D1** W3C→WHATWG redirect content-swap (bytes ≠ labelled doc; also non-deterministic), **D2** inflated "table-heavy" notes (IBM legacy non-`<table>`; Fed H.15 = 1 matrix not "dense tables"), **D-systemic** ~15+ news/hub snapshots intrinsically non-reproducible (contract holds; prose oversold). Apply (code-review-apply-changes): D1 → frozen dated `w3.org/TR/2017/REC-html52-20171214/` (verified no-redirect, deterministic, relabelled); D2 reworded to factual markup (agent honestly corrected the finding's OWN imprecise premise); D-systemic disclosure added. Re-verify: load_checked clean, 51↔51, hash + check-attr `-text` green. Atomic commit = urls.tsv(M)+51(A)+3 synthetic(D). **O5 discharged.**
- **2026-05-17 — Stage A (oracles) — committed `e17c4bd`.** impl: network OK; real Trafilatura 2.0.0 / @mozilla/readability 0.6.0 / jsdom 29.1.1, pinned+locks; self-bootstrapping `./.venv` (SEAM-2); the dormant harness `schema_conformance` test **AUTO-ACTIVATED SKIPPED→ok (O1/R1 discharged)**; DEC-4 handled by emitting `contract_version` as a schema *property* NOT in `required` (keeps existing harness test green until Stage C adds the field) — sound, documented ×3. **Instrument defect caught by the REAL corpus (project thesis validated before any extraction code):** Trafilatura crashed `UnicodeEncodeError` writing UTF-8 JSON to the Windows cp1252 console on real Unicode pages → `trafilatura ok:14/51`; ASCII fixtures hid it. TDD fix (separate agent): bug → permanent regression fixture (selftest Step 6 + unicode-article.html); `_emit_json` → `sys.stdout.buffer` UTF-8 on BOTH success+failure paths → **ok:14→51, oracle_error→0**, determinism + schema_conformance still green. Code-reviewer: **PASS** (tri-state; UTF-8 fix both paths; venv honesty — no version-lie, no write-race; full transitive pinning; schema genuinely tight). Devil's-advocate (**earned its seat a 3rd time** — isolation-review blind spot): **D1 MEDIUM** cross-adapter asymmetry — malformed `--base-url` hard-fails Readability (drops oracle #2 from the differential) while Trafilatura tolerates; contained (honest OracleError, not laundered) but a latent instrument bias; + selftests never exercised `--base-url` at all. Supervisor call: contract-correct behaviour = graceful degrade (base_url is auxiliary) → fix-now, FYI Arthur. Apply: run.mjs validates `new URL()` → absent/throw ⇒ JSDOM w/o `url` (about:blank), canonical_url re-keyed byte-symmetric with the absent path (HLD §5); `--base-url` selftest coverage added (valid/absent/malformed, both adapters); README dev-notes. Re-verify (a)–(e) PASS, schema_conformance green. Pre-commit gate caught+fixed: stray `__pycache__/*.pyc` staged (→ `*/__pycache__/`,`*.pyc` added to oracle .gitignore) and the canonical Oracle HLD already tracked via concurrent commit `09b51cf` (**DEC-6 already satisfied by repo reality**; V1–V5 also already tracked — not mine to revert, FYI Arthur). Atomic commit = 16 `benchmark/oracles/**` files.
- **Status:** Stage A ✅ `e17c4bd`, Stage B ✅ `85ad674`. **B1 cross-team gate RESOLVED-BY-DEC-6:** Arthur unified oracle+harness ownership into this team, so the "joint sibling sign-off" is now the machine-checkable Stage-C integration proof (schema_conformance + end-to-end real run), strictly stronger than a prose handshake. O2 (180 s timeout sizing) / O3 (reader accepts string|null) fold into Stage-C verification (task #12). **Process scorecard: the devil's advocate caught a reviewer-invisible defect in BOTH stages — the discipline keeps earning its cost.** Next: Stage C (task #12, now unblocked).
