# JRN — mdrcel M2 (Idiomatic Rust Port of Mozilla Readability 0.6.0)

Supervisor: Claude (agent-team model). Maintained per Arthur's standing instruction.
Branch: `feat/oracles-and-corpus`. Start: 2026-05-18. Predecessor: M1 + gold sub-phase complete & frozen (`7afaabe`).

---

## Supervision model (Arthur's instructions, in force)
- Supervisor gives high-level direction only; does NOT test/debug/read-heavy itself — delegates to agents to protect context.
- Per stage: implement (TDD) → code-reviewer skill → devil's-advocate challenge → code-review-apply-changes skill → focused verify → **supervisor** atomic commit (authored files only; Co-Authored-By trailer; check `git status` first).
- A devil's-advocate team member reviews/challenges the other agents EVERY stage (incl. challenging the supervisor).
- Maintain this journal; commit at stage boundaries. Final stage: independent agent set verifies 100% vs the finalized HLD; iterate to 100%.
- M2 was Arthur-gated; **Arthur greenlit it** this session.

## Methodological precondition (carried from gold sub-phase)
Gold set frozen & human-decided; anti-inversion sacrosanct. The port is a **faithful transcription of Readability.js**, never tuned by copying oracle output. A faithful clean port is *expected* to legitimately beat the oracles on gold-precision (they interleave tables the gold excludes); an oracle-copy could only tie — that delta is itself the anti-inversion proof.

---

## Stage R0 — Plan + ratification (this entry)

### Architect plan (opus Plan agent)
Delivered a rigorous M2 HLD: fidelity anchor (`textContent` = raw depth-first descendant-text concat, zero synthetic separators); dependency `html5ever` + `markup5ever_rcdom` (kuchikiki fallback); 1:1 module map to Readability.js (`regexps/dom/grab_article/scoring/prep/helpers/metadata`); 5-stage breakdown; faithfulness-verification + anti-inversion sections; 8 risks flagged for supervisor/DA.

### Devil's-advocate challenge (opus) — verdict SAFE-WITH-CHANGES; 4 BLOCKER / 6 MAJOR / 4 MINOR
**All 14 findings ACCEPTED by the supervisor.** Summary + adjudication:

| # | Sev | Finding | Supervisor adjudication |
|---|-----|---------|-------------------------|
| B-1 | BLOCKER | Fidelity anchor cited `JSDOMParser.js` — **dead code** here; oracle uses jsdom 29.1.1 (`run.mjs:184`). Conclusion holds but via WHATWG DOM `textContent`, not the cited source. | ACCEPT. Re-anchor to WHATWG DOM; add Stage-0 empirical probe vs jsdom 29.1.1; state JSDOMParser.js unused (`_docJSDOMParser` undefined under jsdom). |
| B-2 | BLOCKER | `_setNodeTag` risk **inverted**: `_docJSDOMParser` falsy ⇒ oracle ALWAYS takes the slow `createElement`/`replaceChild` branch (Readability.js:760-772), not the in-place one. | ACCEPT. Port mirrors the slow branch only (new element, children moved in order, attrs cloned, `readability` score carried). Drop fast-path porting. |
| B-3 | BLOCKER | Stage-3 "≥0.95 Jaccard vs Readability-JS" is **not harness-measurable** (`score.rs:267-279`: gold→Trafilatura; "Readability is never the reference"). BRIEF "~95%" is *descriptive*, not an acceptance gate; promoting it = inversion. | ACCEPT (critical). Acceptance = gold Coverage/Precision + the consumer cases. "~95% vs RJS / ~70-80% vs Trafilatura" = **descriptive expectation only**. RJS-Jaccard = non-gating diagnostic. |
| B-4 | BLOCKER | "Converge to ≥0.95 vs JS oracle" = unmechanised oracle-mimicry back door. | ACCEPT. Mechanised guard: every scoring change commits with exact Readability.js line(s) + JS-behaviour statement; RJS-Jaccard never an exit threshold; divergence closed only by a test asserting JS-specified behaviour; oracle-Jaccard-as-acceptance = explicit ANTI-GOAL; per-stage DA audits for output-matching-without-citation. |
| M-1 | MAJOR | Stage-0 parser-equivalence is a flagged spike, not a gate; 124-table FY2019 EDGAR maximally stresses implied-`<tbody>`/foster-parenting (0 `<tbody>`, 1001 `<tr>`). | ACCEPT. Promote to **BLOCKER gate**: post-tokenizer token-sequence identity jsdom-29.1.1 vs html5ever+rcdom for all 7 gold + table-heavy non-gold snapshots, before Stage 1a. Divergence ⇒ parser changes, not Stage 1. |
| M-2 | MAJOR | `markup5ever_rcdom` is `+unofficial`/"unsupported, test-support"; DA verified it *builds* (html5ever 0.39 + rcdom 0.39 + tendril 0.5, rustc 1.95.0, edition 2024, ~36 crates incl. unused xml5ever/web_atoms). "minimal graph" claim false. | ACCEPT. Record honest status + count; pin Cargo.lock; kuchikiki switch criterion = rcdom borrow-mgmt blocks Stage 0 (same algorithm, no plan change). |
| M-3 | MAJOR | Stage 1 is a monster (~60% of Readability.js) — defeats per-stage review. | ACCEPT. Split → 1a (parse+scoring+single top-candidate; first Ok; gate example.com+gov.uk hub), 1b (sibling append 1415-1535; gate Apple/Morrison Wikipedia prose band), 1c (FLAG_* retry+fallback; gate no regression). |
| M-4 | MAJOR | Supervisor's "metadata→Stage 4" ruling creates hidden Stage-1 dep: `_headerDuplicatesTitle` (1105) uses full `_articleTitle` (=`_getArticleMetadata`/`_getArticleTitle` 572-651, set 2745 before grab 2747) to delete headings from body. | ACCEPT — refines my ruling. `_getArticleTitle`+title-metadata → Stage 1a; Stage 4 = remaining non-body metadata. Metadata stays MANDATORY M2 scope. |
| M-5 | MAJOR | JS `\s` (incl. `﻿`) ≠ Rust `regex` `\s`; JS `\W` ASCII ≠ Rust `\W` Unicode — bites `normalize`/`whitespace`/`hasContent`/`tokenize` (scored surface). No backrefs/lookaround (verified) — that claim holds. | ACCEPT. Every ported regex uses explicit JS-compatible classes (`(?-u:\W)`; explicit JS `\s`); per-regex conformance test table as Stage-0/1a oracle. |
| M-6 | MAJOR | Real fidelity risk is `parseInt`-of-attribute (rowspan/colspan, 2245-2259) → `_getRowAndColumnCount`→`_markDataTables`→scored EDGAR text — not float arithmetic. No `Math.round` exists (verified) so that concern moot. | ACCEPT. JS-`parseInt`-faithful helper (ws-skip, sign, longest digit prefix, NaN→`||1`/`||0`); pin with EDGAR rowspan/colspan classification tests. |
| m-1 | MINOR | Score/data-table maps safe iff point-query-only (verified vs reference). | ACCEPT. HLD invariant + debug-assert/test nothing iterates them in scored path. |
| m-2 | MINOR | Some stage exits not `results.json` fields. | ACCEPT. Each stage names exact field its gate reads. |
| m-3 | MINOR | Retry-loop clone semantics under slow `_setNodeTag`. | ACCEPT. Re-parse from original bytes per attempt; pin with test; decided at 1c review. |
| m-4 | MINOR | Stage-0 must not silently flip baseline. | ACCEPT. Stage-0 exit = compiles + parser gate passes + `cargo test` green + every URL still `not_implemented`. |

### Finalized stage plan (post-adjudication)
- **Stage 0** — DOM facade + parse spine (infra). Hard parser-equivalence BLOCKER gate. Baseline floor unchanged (all `not_implemented`).
- **Stage 1a** — parse + `_grabArticle` scoring + single top-candidate + `_getArticleTitle`/title-metadata; first `Ok`. Gate: example.com + gov.uk hub gold Scored, high precision.
- **Stage 1b** — sibling append. Gate: Apple/Morrison Wikipedia prose-only Coverage band.
- **Stage 1c** — FLAG_* retry/flag-sieve loop + fallback (+ re-parse-per-attempt). Gate: no regression on 1a/1b.
- **Stage 2** — `_markDataTables` + `_cleanConditionally` + `_prepArticle`. Gate: 3 table-heavy EDGAR/HMRC gold Scored; per-URL precision ≥ oracles' (the faithful-port-beats-SOTA delta, evidenced in results.json).
- **Stage 3** — close residual gaps. Gate: gold Coverage/Precision bands + the consumer cases; RJS-Jaccard diagnostic only (NOT a gate).
- **Stage 4** — remaining non-body metadata + Options + deliberate reviewed `crate_run.rs` compile-fence arm. Default `extract()` byte-identical (zero scored-text change).
- **Final** — independent agent set verifies 100% vs HLD; iterate to 100%; DA final challenge; supervisor closeout + baseline re-bless (HLD §9).

### FOR ARTHUR (decisions I made in your stead — all reversible)
1. **Anti-inversion honesty correction (most important):** "≈95% vs Readability-JS" is a *descriptive expectation*, **not** an acceptance gate (the harness can't even score it; Readability is never the reference). M2 is accepted on gold Coverage/Precision + the consumer cases. This corrects the roadmap framing I gave you earlier — the DA caught it; I ratified the correction.
2. Metadata is **in M2 scope** (faithful port; de-risks M5 the consumer shim), sequenced last — but title-resolution moved earlier (it feeds body-text decisions). DA challenged my first cut; I refined it.
3. Dependency `html5ever`+`markup5ever_rcdom` accepted despite its `+unofficial` status (DA verified it builds); kuchikiki is the documented fallback.
All ratifiable/amendable; the HLD records full rationale.

### Artifacts
- Final HLD: `wrk_docs\2026.05.18 - HLD - mdrcel Readability Port (M2).md` (written by writer agent from architect plan + DA amendments + these adjudications).
- Tasks #18 (ratify) … #24 (final verify).

---

## Stage 0 — DOM facade + parser-equivalence BLOCKER gate — COMPLETE

Cycle: implement (opus, TDD) → code-reviewer (opus) → devil's-advocate (opus) → apply (opus, code-review-apply-changes) → independent focused-verify (opus) → supervisor commit.

**Result: parser-equivalence BLOCKER gate PASS 15/15** (7 gold + 8 non-gold incl. 124-table FY2019 EDGAR, 72k-token Berkshire, legacy IBM ~1500 `<font>`, + 2 EDGAR index pages added during apply). `html5ever`+`markup5ever_rcdom` is post-tokenizer sequence-identical to jsdom 29.1.1 across the whole 51-snapshot corpus. **rcdom stands; HLD §6.1 kuchikiki trigger NOT met.** `lib.rs` still `NotImplemented`; harness floor honestly VACUOUS (no laundering).

Findings (all accepted & applied):
- code-reviewer — APPROVE-WITH-NITS; MAJOR M1 = empty-vs-empty (`[]==[]`) laundering path; the gate's own "fails loudly on empty" contract did not exist in code.
- devil's-advocate (highest-value) — **MAJOR-2: proved html5ever≢jsdom on non-whitespace stray text in table parts (foster-parenting: rcdom `FOSTERcell` vs jsdom `cellFOSTER`).** 15/15 PASS is corpus-luck (every corpus snapshot has 0 non-ws stray table text), NOT general substrate proof. Also overrode the reviewer's factually-wrong "2 excluded EDGAR pages are link-lists" — they are tabular and both PASS → included.
- Applied: token-vector non-empty assert (M1); foster divergence pinned in a witness test + a per-snapshot **self-policing guard** that fails loudly if any future snapshot enters the divergent class (+ non-UTF8 rejection) + over-claim wording downgraded (M2); tokenizer-mirror FNV-1a-64 content-hash tripwire over `metrics.rs::tokens` (M3 — `sha2` unavailable in the frozen manifest; FNV-1a documented per the brief's "simplest robust option" licence; multi-agent-safe, touches only the Stage-0 test file); 2 EDGAR index pages included (m1); vacuous `debug_assert` de-theatred (m2); notes hygiene (m3); bounded HLD path-accuracy fix (m4).
- Independent focused-verify — COMMITTABLE: 58 tests green, clippy/fmt clean, gate 15/15 (real Node spawns), FNV hash independently recomputed = pinned constant, FIX-1/FIX-2b prove-out actively demonstrated, all committable files NUL-clean.

**TRACKED STANDING RISK (M2):** html5ever and jsdom *provably* diverge on non-whitespace stray `#text` directly under `table/tbody/thead/tfoot/tr` (foster-parent ordering). Currently **corpus-inert** (0/51 in that class) and **self-policing** (gate guard fails loudly if any future gold/corpus addition enters it → re-triggers the HLD §6.1 rcdom→kuchikiki decision). Not a defect now — a bounded, surfaced, monitored substrate limitation. Later stages / corpus growth must respect the guard, never weaken it.

Version: mdrcel 0.2.1→0.2.2 (patch — internal infra + deps; frozen surface byte-unchanged; `readability` `#[doc(hidden)]`). Deps pinned `=0.39.0`/`=0.5.0`; `markup5ever_rcdom` `+unofficial`/test-support status recorded in `Cargo.toml`. (`notes/m2-stage0.md` is gitignored scratch — durable record is here + the commits.) Tasks: #19 → completed; #20 (Stage 1a) next.

---

## Stage 1a — parse + scoring + single top-candidate (first real Ok) — COMPLETE

Cycle: implement → **recovery (interrupted agent)** → re-implement/complete → code-reviewer (opus) → devil's-advocate (opus) → apply (opus) → independent focused-verify (opus) → supervisor commit.

**Recovery event (honest record):** the first Stage-1a agent's API socket dropped after 38 tool-uses, leaving partial unreviewed work (`regexps.rs` ~18.5 KB + `Cargo.toml` regex dep/version; `Cargo.lock` stale; no other modules; lib.rs untouched so floor intact). `SendMessage` unavailable + `Agent` starts fresh ⇒ could not resume. A fresh agent recovered: validated the partial `regexps.rs` against Readability.js (kept; one correct fix — the `regex` crate rejects `(?-u:\W)` on `&str`, replaced with the exact-equivalent ECMAScript class `[^A-Za-z0-9_]`), regenerated `Cargo.lock`, completed the full Stage-1a scope.

**Result: M2 is off the floor.** Crate `ok:51` (was `not_implemented:51`); **50/51 Scored, ZERO regressions** vs the M1 vacuous baseline. The 1 non-Scored = IBM FY2008 `reference_empty` — Trafilatura's *own* empty output (the DEC-G8 oracle-weakness case), crate is `Ok` with `crate_wc==rjs_wc==11273` — NOT a crate defect; 50 is the max achievable.

**example.com — faithful PASS:** cov 0.75 / prec 0.857; `crate_wc=16 == Readability-JS wc=16` (token-sequence identical). The 0.75 ceiling is the *documented* gold-vs-Readability divergence (`_headerDuplicatesTitle` faithfully removes the `<h1>` at sim 1.0>0.75; `gold.tsv` itself records "both oracles drop the `<h1>`"). Not a defect, not tuned.

**gov.uk-hub — HONEST STOP → principled HLD amendment (supervisor adjudication, DA-validated):** the implementing agent refused to fudge and surfaced that HLD §7.1's "high precision on gov.uk-hub" sub-gate is **methodologically impossible for a faithful port** and contradicts the gold's OWN frozen anti-inversion design (`gold.tsv` curated gov.uk-hub as the case where Readability *fundamentally fails*; RJS itself = 234 nav words, none the gold line; human gold band 6-12). The DA independently ran RJS and proved **RJS ⊆ crate (371 ⊋ 234, 0 RJS-only tokens)**; the 137-word delta is *exactly* the deliberately-deferred Stage-2 `_cleanConditionally` / Stage-1b sibling-append. DA actively hunted for an in-scope Stage-1a mechanism that *should* cut the cruft and found none (nav columns carry a class containing "and" → `okMaybeItsACandidate` → Readability.js itself does not strip them). **Adjudication: PRINCIPLED spec-bug fix, NOT gate-weakening** — demanding 6-12 would *force* the §4 inversion the whole HLD forbids. Applied the DA's exact loophole-closing wording to HLD §7.1: the pass condition is now the structural invariant **crate token-multiset ⊆ Readability-JS token-multiset** (no invented content); reaching 6-12 by out-cleaning RJS is an explicit **ANTI-GOAL** rejected at the §4.3(e) audit; the 6-12 human gold band remains frozen ground truth, reported as the expected Coverage residual, not a pass/fail threshold.

**Anti-inversion record (DA-mandated, §4.3(e)/§10) — why the EDGAR≡RJS identity is faithful, not inversion:** crate token-sequence is *identical* to Readability-JS on the table-heavy golds (AAPL-FY2025 28566, AAPL-FY2019 32102, IBM-FY2008 11273) and example.com (16). This is expected and faithful: EDGAR filings are single-container documents where Stage-1a (single top-candidate; sibling-append/`_cleanConditionally` deferred) and full RJS *necessarily converge*. The **dispositive non-inversion proof is the gov.uk-hub divergence**: a port tuned to mimic the oracle would match RJS=234 there; the faithful port correctly produces 371⊋234, exactly as Stage-1a scope predicts. Crate has zero I/O (cannot observe the oracle at runtime); all test expecteds were hand-traced from Readability.js (3 self-corrected by re-tracing JS — `_replaceBrs` independently confirmed byte-identical to real jsdom, genuine re-trace not back-fitting). Faithfulness was independently verified by reviewer + DA against ~30 combined cited Readability.js ranges; every ported regex/constant is verbatim; `tokenize` `[^A-Za-z0-9_]` proven exactly ECMAScript `\W` via Node.

Review findings: code-reviewer APPROVE-WITH-NITS (no BLOCKER/MAJOR); devil's-advocate SAFE-AFTER-FIXES (no BLOCKER/MAJOR; strengthened conclusions with direct evidence). Fixes applied: `.gitignore` `wrk_gold/` (commit-atomicity, MINOR-1); **`is_js_space` consolidated** to one canonical `pub(crate) dom` predicate + exhaustive conformance pin (MINOR-2, escalated by supervisor to fix-now — it gated scored-path heading deletion; verified PURE refactor: mean Coverage 0.5716 to 4 dp unchanged, all anchors exact); doc-comment tightening (incl. a follow-on stale-comment fix at `helpers.rs` the verify pass flagged); HLD §7.1/§7.5 amendment. Independent focused-verify: COMMITTABLE — 117 lib + 4 gate tests green, clippy/fmt clean, parser-gate 15/15 byte-identical to `149c686`, full harness re-run zero-delta, 13-file set NUL-clean.

Tracked deferred: MINOR-3 `_isProbablyVisible` `display:none !important` → Stage 3 (HLD §7.5 noted); `NodeKey` Rc-ptr ABA observation → re-audit at Stage 1c (retry re-parse) / Stage 2 (conditional removal churn nodes more aggressively).

Version: mdrcel 0.2.2→0.3.0 (minor — first real extraction behaviour behind the signature-unchanged frozen surface). Tasks: #20 → completed; #25 (Stage 1b — sibling append) next.

### FOR ARTHUR (Stage 1a)
- **Contract amendment made in your stead (reversible/amendable):** HLD §7.1 gov.uk-hub sub-gate was mis-specified — it demanded a precision a faithful Readability port provably cannot reach on a nav-hub, which would have *forced* the anti-inversion violation the whole project guards against. I adjudicated it a principled spec-bug fix (devil's-advocate independently validated with RJS⊆crate evidence) and applied the DA's gameable-proof structural-invariant wording. Full rationale + evidence above; ratify or revert at will.
- **M2 genuinely extracts now** — the crate is off the floor, faithfully (independently proven non-inverted). 50/51 Scored; the 1 gap is Trafilatura's own known weakness, not ours.

---

## Stage 1b — sibling append (`Readability.js:1415-1535`) — IMPLEMENTED; §7.2 gate → HONEST-STOP for adjudication

Cycle: TDD (failing tests first, hand-traced from JS) → implement → focused verify → (review/DA/apply per supervision model) → supervisor commit.

**What was built.** `grab_article.rs::grab_article` now completes the sibling-append block it deliberately STOPPED before at Stage 1a (`Readability.js:1418-1535`), faithfully line-cited:
- `articleContent = createElement("DIV")` (1418); `isPaging` always false ⇒ the `readability-content` id (1419-1421) is skipped (score-invisible).
- `siblingScoreThreshold = max(10, topCandidate.readability.contentScore·0.2)` (1423-1426) — read from the side table (the finalized candidateScore from 1283, or the fake-div init score; Stage-1a leaves exactly that).
- `parentOfTopCandidate = topCandidate.parentNode` (1428); `siblings = parentOfTopCandidate.children` (1429).
- **Explicit index walk over a refetched children `Vec`** (HLD §7.2 critical fidelity point) — NOT a Rust iterator. `s`/`sl` are `i64` (the JS `s -= 1` at `s==0` yields `-1`, then `s++` → `0` — a `usize` would panic on the underflow; `i64` mirrors JS Number semantics exactly). `sl` mirrors the JS *variable* (decremented on append, 1504), not a re-read of `.length`; the refetched `Vec` (1498) is used only for element access at `siblings[s]`.
- All branches ported with citations: `sibling === topCandidate` (1447-1448); same-non-empty-className `contentBonus += topCandidate.contentScore·0.2` (1453-1458); score branch `sibling.readability && score+bonus >= threshold` (1460-1465); the `nodeName==="P"` clause A `nodeLength>80 && linkDensity<0.25` (1471-1472) and clause B `nodeLength<80 && >0 && linkDensity===0 && /\.( |$)/` (1473-1480); `_setNodeTag(sibling,"DIV")` for non-`ALTER_TO_DIV_EXCEPTIONS` siblings (1487-1493); `articleContent.appendChild` (1495); refetch + `s-=1;sl-=1` (1498-1504).
- The inline `/\.( |$)/` regex added to `regexps.rs` as `period_space_or_end()` (literal `.` + literal U+0020 space or `$`; JS `search != -1` ⇔ `is_match`; dialect note: Rust `$` ≡ JS `$` without `/m` on the trimmed inner_text these see) + 8 conformance-table rows.
- **NOT ported (deliberate, recorded):** the 1517-1532 `readability-page-1`/`page` wrap — id/className are score-invisible (HLD §2) AND wrapping articleContent's children in an extra `<div>` adds ZERO `#text` characters, so it is provably `text_content`-invariant. `neededToCreateTopCandidate` (1314) gated only that block ⇒ intentionally removed at 1b (returns when Stage 1c needs `_attempts` bookkeeping). 1538+ retry/`charThreshold` loop = Stage 1c (STOP here exactly as Stage 1a STOPPED at 1415).

**NodeKey ABA re-audit (DA observation carried forward) — result: SOUND.** Stated invariant: *no node owning a live score-table entry is dropped during the sibling walk; `set_node_tag` is the only address change and it transfers the entry old→new atomically before the old handle can be dropped.* `appendChild` moves (never drops) a node — its `Rc` is held by `articleContent.children`; its score entry is untouched. `set_node_tag` (Stage-0/1a-correct) allocs a new `Rc`, transfers score+data-table entry old→new, detaches old; the old `Rc` stays alive via the local `sibling` binding through the iteration, so the address is not reused while referenced, and callers use the returned new handle. The whole `Dom` outlives the walk. Pinned by `sibling_append_nodekey_aba_setnodetag_transfer_holds` (set_node_tag of a scored TABLE→DIV mid-sequence: score follows the pointer, old handle has none).

**TDD evidence.** 7 new hand-traced tests (expected values traced from `Readability.js:1415-1535`, NOT an oracle), all green: `sibling_append_preamble_content_trailing_all_included` (clause A both sides + `===topCandidate`), `_short_p_clause_b_period_rule` (clause B + the no-period negative), `_link_dense_long_p_excluded` (linkDensity≥0.25 ⇒ clause A fails, unscored ⇒ excluded), `_setnodetag_to_div_and_alter_exception` (SECTION∈ALTER kept as-is; FORM∉ALTER → DIV; full score hand-trace in the `tc_score·0.2≥10` regime where same-className contentBonus exactly equals the threshold's computed arm), `_index_walk_visits_every_sibling_once_in_order` (5 siblings around the top candidate, order + no-dup), `_threshold_floor_and_content_bonus` (the `max(10,…)` FLOOR regime, full score trace), `_nodekey_aba_setnodetag_transfer_holds`. Two test expecteds were self-corrected by re-tracing the JS (the SECTION/threshold scenarios — my first hand-trace under-estimated scores; re-derived from the cited arithmetic, NOT back-fitted to output — anti-inversion discipline working as designed). Counts: **`cargo test -p mdrcel` 124 lib + 4 gate green** (was 117+4; +7); **`cargo clippy -p mdrcel --all-targets -- -D warnings` clean**; **`cargo fmt --check` clean**. Parser-equivalence gate still 15/15 (the `parser_equivalence_blocker_gate` + foster-divergence witness + tokenizer tripwire untouched, all 4 green).

**Differential gate (§7.2) — supervisor-corrected after the devil's-advocate independently reproduced the load-bearing claims with the FROZEN oracle. The implementing agent's first draft contained (a) a wrong gold-set enumeration and (b) a non-existent "oracle U+FFFD" cause; the DA caught both; both corrected below. (Process fix recorded at end.)**

**Code result: SAFE.** Stage-1b sibling-append is a faithful transcription of `Readability.js:1418-1535` — DA-confirmed byte-identical to *full Readability-JS* on two adversarial sibling-heavy fixtures the gold cannot provide (adv1 Jaccard 1.000000, 289==289; adv2 `diff`-empty byte-identical, 170==170 — the maximally load-bearing 4×back-to-back `_setNodeTag` index-walk case). `git diff d426df1`: scoring/helpers/lib/mod/dom byte-identical, regexps additive-only, **no constant/threshold altered** (anti-inversion confirmed).

**The 7 gold URLs (correct enumeration per `gold.tsv`): Apple-WP, Morrison-WP, example.com, gov.uk-hub, EDGAR Apple-FY2025-10K, gov.uk-HMRC-2025-26 (gold #6), EDGAR Apple-FY2019-10K. IBM FY2008 is NOT gold — DEC-G8 explicitly rejected it; it is a non-gold corpus oracle-weakness case.** Stage 1b vs 1a (harness + DA reproduction):

| Gold URL | 1a wc | 1b wc | RJS wc | band | note |
|---|---|---|---|---|---|
| example.com | 16 | 16 | 16 | 14–20 | crate==RJS; **IN band (faithful pass)** |
| gov.uk-hub | 371 | 371 | 234 | 6–12 | RJS⊊crate (§7.1 amended); RJS 19.5× band |
| Apple-WP | 28104 | 28104 | 27915 | 12200–14912 | RJS **1.87×** band → §7.2 honest-STOP |
| Morrison-WP | 7573 | 7573 | 7514 | 3906–4774 | RJS **1.57×** band → §7.2 honest-STOP |
| EDGAR FY2025 | 28566 | 28566 | 28566 | 2063–2523 | crate token-seq==RJS; RJS 11.3× band |
| **gov.uk-HMRC #6** | **2430** | **2564** | 2548 | 135–167 | **CHANGED — faithful sibling-append →RJS 2548; band already failing ~15–17× at BOTH stages, honest-STOP unchanged** |
| EDGAR FY2019 | 32102 | 32102 | 32102 | 1678–2052 | crate token-seq==RJS; RJS 15.6× band |

**6 of 7 golds byte-unchanged; gold #6 (gov.uk-HMRC) faithfully changed 2430→2564** (sibling-append firing, converging toward RJS 2548). NOT a regression, NOT a defect: gold #6's 135–167 band was already unreachable (~15–17×) at Stage 1a and remains so. Every Stage-1a *anchor* (example.com==RJS, gov.uk-hub RJS⊊crate, the 3 EDGAR token-seq==RJS) holds exactly; non-gold convergences (browse-edgar 701→771==RJS771, W3C-spec 275→348==RJS348, HMRC-2024-25, Wikipedia-Main) confirm sibling-append fires faithfully via the cited lines — additional non-inversion signal (an oracle-copy could not produce 771==771/348==348 through independently hand-traced spec lines; dispositive gov.uk-hub 371⊋RJS234 §10 anchor still stands).

**Wikipedia structural anti-inversion — CORRECTED (DA reproduced with the FROZEN `run.mjs`/jsdom-29.1.1; the implementer's & code-reviewer's "U+FFFD/mojibake" claims were a non-existent console/dossier encoding artifact — the *same* artifact the gold-curation journal already corrected twice, recurring here and struck):**
- TRUTH: the crate is a **strict multiset SUPERSET of RJS** — **RJS-only = 0 tokens** on both Wikipedia golds (independently reproduced). 0 U+FFFD bytes in *any* RJS or crate output; `£`=`c2a3` byte-identical & equinumerous RJS==crate==snapshot (26/26 Apple; 39/39 Morrison), `é`/en-dash likewise. **No oracle mojibake exists with the frozen oracle.** The earlier "491/252, 300 U+FFFD" figures are the artifact, struck.
- crate-only = Apple 32 token-types / Morrison 30, **100% attributable to deferred `_cleanConditionally` chrome** (Wikipedia `[edit]` links, disambiguation hatnotes, "See also/Main article/Further information" cross-refs, "External links"/Authority-control navbox) — **ZERO unexplained residual**. The crate retains *more* chrome than RJS and invents *nothing*: the §7.1 "crate ⊇ RJS, never invent different" pattern in its strongest, cleanest form.

**MAJOR-3 — strategic, NEEDS ARTHUR (the supervisor will NOT self-rule this; top open item):** the DA independently confirmed **full Readability-JS — the complete algorithm with `_cleanConditionally`/`_markDataTables` running — cannot reach 6 of the 7 gold bands, ever**: example.com 0.80× (in band), but Apple-WP 1.87×, Morrison-WP 1.57×, gov.uk-hub 19.5×, EDGAR-FY2025 11.3×, gov.uk-HMRC 15.3×, EDGAR-FY2019 15.6× the band max. RJS's Apple-WP output ends with the entire Bibliography; `_cleanConditionally` provably does NOT strip well-formed reference lists/infoboxes/navboxes. **Therefore "convergence expected at Stage 2" is FALSE for these golds — Stage 2 cannot close what complete RJS does not.** The bands are unreachable by ANY faithful Readability port at ANY stage — *by the gold sub-phase's deliberate anti-inversion design* (gold = human truth that SOTA/Readability fails; that delta IS the non-inversion proof). The DA also found a real **internal HLD contradiction**: §7.2 ("Wikipedia reaches the prose-only band", hard pass/fail) vs §7.4 (beat-the-oracle mechanism is *table* boilerplate; its Stage-2 gate is the EDGAR/HMRC golds; for Wikipedia §7.4 only claims "precision rises") and §4's ANTI-GOAL. Resolving this **redefines the explicitly-normative §4.1 acceptance model** — broader than the single §7.1 gate the supervisor amended in Arthur's stead, so it is **Arthur's to ratify**. DA's recommended falsifiable restatement (supervisor endorses surfacing it; NOT self-applied):

> M2 (a *faithful Readability port*) is accepted iff, per gold URL: **(1) structural-faithfulness invariant** — `tokens(crate)` ⊇ `tokens(RJS v0.6.0)`, every crate-only token attributable to a named, cited-`Readability.js`-line, deferred cause (e.g. not-yet-ported `_cleanConditionally`/`_markDataTables`), with **zero** unexplained tokens, and **zero** RJS-only tokens except a *separately documented & independently reproduced* oracle/parser residual; **(2) the named acceptance cases pass**; **(3)** gold Coverage/Precision **reported as the by-design Readability-vs-human residual** (the SOTA-gap baseline a later milestone must beat) — explicitly **not** a pass/fail band for a Readability-faithful milestone. Any divergence not closed by a cited line or a reproduced oracle-residual is a BLOCKER (§4.3(c)).

**Operative decision (avoid blocking, per Arthur's "push on"):** Stages 1c/2 proceed measured by this DA-validated, falsifiable, currently-zero-residual structural invariant (the work is identical either way: faithfully port Readability, measure vs RJS, record the gold residual). HLD §4.1/§7.2 normative text is **left UNCHANGED pending Arthur's ratification**; final M2 verification (#24) uses whatever Arthur ratifies. This recognises what M2 (a *port*, not a gold-beater — that is M3+) was always measured by; it is not gold-erosion (the invariant is falsifiable and gates the consumer + zero-residual faithfulness).

Version: mdrcel 0.3.0 → **0.3.1 PATCH** (matures already-shipped extraction; no public API/type/signature change; `extract == extract_with(default)` & Bug-E2 empty-`Ok` preserved; DA noted borderline-but-acceptable).

Tracked deferred (carried): `_isProbablyVisible` `display:none !important` → Stage 3; `_cleanConditionally`/`_markDataTables` → Stage 2 (closes the *non-gold* sibling/table convergence — NOT the Wikipedia/gov.uk *human-gold* gap; see MAJOR-3); FLAG_* retry + page-wrap (1517-1532) + `_attempts` → Stage 1c. NodeKey ABA: **SOUND at 1b** (DA-confirmed via the adversarial 4×`_setNodeTag` fixture); re-audit at 1c retry-reparse / Stage-2 conditional-removal.

**Process fix (DA MINOR-1, supervisor-owned):** the implementing agent must NOT author the supervisor's adjudication journal — it produced the wrong gold enumeration (IBM-as-gold; gold #6 omitted/misclassified as non-gold) and the false U+FFFD cause the DA had to catch. Henceforth implementing/review agents supply EVIDENCE only; the supervisor authors ALL journal adjudication prose. This entry was supervisor-rewritten post-DA; the structural conclusions survived only because independent DA re-derivation reproduced the truth despite the implementer's wrong stated reasons — the discipline working as designed.

### FOR ARTHUR (Stage 1b)
- **Code is faithful & safe** — sibling-append byte-identical to full Readability-JS on adversarial fixtures (the gold can't exercise it; DA-verified independently). 6/7 golds byte-unchanged; gold #6 HMRC faithfully changed 2430→2564 (already-failing band, no regression). Committed.
- **TOP OPEN ITEM — NEEDS YOUR RATIFICATION (MAJOR-3):** full Readability-JS itself cannot reach 6 of 7 gold bands *ever* (1.57×–19.5×; `_cleanConditionally` does not strip refs/infoboxes/navboxes). The HLD has an internal §7.2-vs-§7.4 contradiction. M2 acceptance should be reframed to a falsifiable **structural-faithfulness invariant + the consumer; gold Coverage = by-design diagnostic residual** (exact wording above). Principled (M2 is a faithful *port*; beating the human gold is the M3+ thesis), DA-validated — but it redefines normative §4.1, so it is your call, not mine. I have NOT changed §4.1/§7.2; I am proceeding on the operative invariant to avoid blocking per your "push on" instruction. Ratify, amend, or redirect when back. The §7.1 gov.uk-hub amendment is the narrow precedent; this is its principled generalization across the whole gold set.

---

## Stage 1c — FLAG_* retry/flag-sieve loop + fallback + page-wrap + re-parse-per-attempt — COMPLETE

Cycle: implement → code-reviewer (resolved a PRIORITY-0 anchor scare) → devil's-advocate (independently strengthened every load-bearing claim) → supervisor-authored journal → focused-verify → commit. (Journal-authorship process-fix in force: agents supply evidence; supervisor authors all adjudication prose.)

**Code result: SAFE-TO-PROCEED** — code-reviewer APPROVE-WITH-NITS, devil's-advocate SAFE-TO-PROCEED; **no BLOCKER, no MAJOR in code**.

Built (faithful, line-cited): `_grabArticle` outer retry/flag-sieve machinery (`Readability.js:1043`,`1538-1576`) — `CHAR_THRESHOLD=500` verbatim (`:133`/`:54`), `textLength<_charThreshold` (`:1546`), flag sieve STRIP_UNLIKELYS→WEIGHT_CLASSES→CLEAN_CONDITIONALLY (`:1556-1561`), `_attempts`, **stable** longest-text fallback via `sort_by_key(Reverse)` (`max_by_key`'s last-max would be unfaithful), `None` when longest attempt has 0 text (`:1568-1571`); page-wrap both arms (`:1517-1532`, deferred from 1b); re-parse-from-original-bytes-per-attempt (HLD §m-3, finalized at this review). Version 0.3.1→0.3.2 PATCH.

**PRIORITY-0 scare — RESOLVED (verify-don't-trust worked exactly as designed).** The implementing agent's handoff anchor table cited `gov.uk-income-tax` numbers (817==RJS817, cov 0.938) in the **gov.uk-hub** slot — internally inconsistent with the 3-agent-verified Stage-1b record. The code-reviewer built committed `ca01c54` in a clean worktree (md5-verified-identical snapshots); the devil's-advocate ran 3 fully-independent harness runs. **Truth: gov.uk-hub = 371 / RJS 234 / cov 0.033, UNCHANGED 1b→1c** — the dispositive `crate ⊋ RJS` §10 anti-inversion anchor stands bit-identically. A handoff *mislabel*, NOT a code defect, NOT an anchor break, NOT nondeterminism.

**DETERMINISM — bulletproof (this underpins ALL M2 differential evidence).** DA ran the harness 3 fully-independent times → **0 mismatches across 51 URLs × 9 fields**, mean Coverage `0.610548850278` identical to 12 decimals; + source audit: side tables point-query-only by construction under per-attempt rebuild; `NodeKey=Rc::as_ptr` only hashed/`ptr_eq`'d, never ordered; the only sort is stable on a deterministic `usize`; `string_cache` atoms compared as strings. No nondeterminism, latent or otherwise.

**Re-parse ≡ JS `pageCacheHtml`:** faithful at the scored (token) level — DA's 10 adversarial fixtures all token-identical to RJS; the sole residual is the pre-existing, score-invisible, retry-INDEPENDENT html5ever↔jsdom whitespace gap (proven present in the no-retry baseline too — re-parse adds ZERO new divergence). NodeKey ABA safe by construction (per-attempt `Dom` owned & dropped; only owned `String`/`usize` escape).

**Bug-E2 `None`→`Ok("")`:** exactly faithful to RJS null (`:1569-1571`→`:2748-2750`). DA enumerated all 51 corpus URLs: crate non-empty for every one; zero asymmetric-empty vs RJS; corpus `None`-set is empty; `lib.rs` maps `None`→`Ok(Extracted{text:""})`, never `Err`/`NotImplemented`; `extract==extract_with(default)` by construction; harness `crate ok:51`. No content laundered.

**Anti-inversion / no tuning:** `git diff ca01c54` for scoring/regexps/helpers/prep/dom/metadata = **0 lines**; only retry transcription in grab_article/mod/lib. Mean Coverage 0.5765→0.6105 (+0.034) is exactly 3 URLs where the faithful retry fired (attempt-0 <500 chars): aljazeera 55→269 (RJS 264), bankofengland 0→954 (RJS 800), cnbc 5→645 (RJS 115 — the 645-vs-115 residual is the documented Stage-2 `_cleanConditionally` over-inclusion class, not a retry defect). DA hand-traced bankofengland: convergence is structurally produced by the faithfully-ported flag-sieve (77 unlikely-class containers; STRIP cleared on retry → content survives), 954≠RJS 800 (does not invert). Page-wrap↔`_prepArticle` order-swap proven token-invariant on adversarial fixtures (both arms); must be re-confirmed when order-sensitive Stage-2 `_cleanConditionally` lands.

Anchors all hold: example.com 16==RJS16; gov.uk-hub 371⊋RJS234; 3 EDGAR token-seq==RJS; gold #6 gov.uk-HMRC 2564; Wikipedia unchanged; 50/51 Scored. Parser-equivalence gate 15/15; `tests/parser_equivalence_gate.rs`+`body_text.mjs` byte-identical to `149c686`. 136 lib + 4 gate green; clippy/fmt clean. Frozen `benchmark/src/`/gold/score/metrics/oracle/crate_run zero diff.

Findings (resolved/recorded): **MINOR-1** HLD §m-3 line-cite imprecision (cited `:2306`; actual reset `page.innerHTML=pageCacheHtml` `:1549`, cache `:1043`) — **supervisor-corrected in the HLD this commit**. **MINOR-2** the 1b `Some("")`→1c `None` retry-exhaustion boundary is faithful & contract-preserving (re-pinned) — no action. **Pre-existing tracked:** `benchmark/src/crate_run.rs` has stale M1-`NotImplemented`-floor assertion tests (not run by `cargo test -p mdrcel`; benchmark/src frozen this stage) → **must be updated at Stage 4** (which already touches `crate_run.rs` for the `ExtractError` compile-fence).

**Process tightening (DA+reviewer concur — adopted):** the gov.uk-hub mislabel was a near-miss (coincidentally-correct conclusion via wrong cited evidence). Henceforth implementing/review agents hand off load-bearing numbers as **raw `results.json` rows keyed by exact URL** (not prose), so the supervisor can mechanically verify URL↔number binding before journaling. Baked into future stage prompts.

Tracked deferred (carried): `_cleanConditionally`/`_markDataTables` → Stage 2; `_isProbablyVisible !important`/full clause set → Stage 3; remaining metadata + `crate_run.rs` stale-test fix + `ExtractError` compile-fence → Stage 4. NodeKey ABA SOUND under retry churn (re-audit at Stage-2 conditional-removal).

**Stage 1 (1a+1b+1c) is COMPLETE.** `_grabArticle` is fully ported (scoring → single-top-candidate → sibling-append → retry/flag-sieve/fallback → page-wrap). The faithful Readability *selection* core is done. Stage 2 adds `_cleanConditionally`/`_markDataTables` — the table-precision delta that lets a faithful clean port legitimately beat both oracles on the EDGAR/HMRC gold-precision (HLD §7.4: the legitimate, non-circular "beats SOTA" mechanism).

### FOR ARTHUR (Stage 1c)
- Code faithful & safe; **M2's faithful Readability selection core (Stage 1 = 1a/1b/1c) is COMPLETE**. Determinism independently proven bulletproof (underpins every M2 differential claim). Committed.
- No new NEEDS-ARTHUR item. The Stage-1b **MAJOR-3 acceptance-model question remains the single open ratification item** — unchanged; engineering continues on the operative structural invariant pending your call (it only binds at the final M2 verification).

---

## Stage 2 — `_markDataTables` + `_cleanConditionally` + full `_prepArticle` — COMPLETE

Cycle: implement (recovered from a usage-cap interrupt — tree was clean; fresh dispatch) → code-reviewer (APPROVE-WITH-NITS, independent line-cite audit) → devil's-advocate at maximum rigor (SAFE-AFTER-FIXES — overrode the reviewer on a load-bearing headline number and on the `_unwrapNoscriptImages` deferral safety claim) → apply (ported `_unwrapNoscriptImages` + MINOR cleanups) → independent focused-verify (COMMITTABLE) → supervisor commit. Process-fix in force: agents supply evidence; supervisor authors all journal adjudication prose.

**Result: SAFE-TO-PROCEED. M2's `_prepArticle` is now the full faithful order.** No BLOCKER, no MAJOR-in-code. Stage 2 ports `_markDataTables` + `_getRowAndColumnCount` + `_cleanConditionally` (the complete shadiness checklist incl. the data-table/ancestor-table/ancestor-code KEEP clauses) + `_cleanHeaders` + `_cleanStyles` + `_cleanMatchedNodes` + `_getTextDensity` + general 4-arg `_hasAncestorTag` + single-cell-`<table>` unwrap + `<br>`-before-`<p>` removal + `<h1>`→`<h2>` retag + share-strip + the JS-faithful `parse_int_js` helper (HLD §9 / M-6) + `_unwrapNoscriptImages` (Readability.js:1892-1968, JS-pipeline position :2733). Every branch line-cited. Version 0.3.2 → **0.3.3 PATCH**.

**Headline measurement — corrected by the DA (sharpest catch of M2 so far):** crate matches Readability-JS on **50/51 corpus URLs by post-tokenizer word-count** (the harness's *scored* metric — `crate_wc == readability_wc`) and on **29/51 by raw `textContent` byte-equality**. The 21 word-count-equal-but-not-byte-equal URLs differ only in whitespace serialization (2-93 bytes/URL, score-invisible per the harness tokenizer which collapses whitespace runs). Bankofengland is the **1/51** with a real content divergence (crate 796 vs RJS 800, 4 words). The code-reviewer's prior "byte-equal 50/51" framing was tokenizer-collapsed-equal, not raw-byte-equal — corrected here so the permanent record states the precise truth.

**THE NEW ANTI-INVERSION ANCHOR (DA-provided; replacing the now-dissolved gov.uk-hub `crate ⊋ RJS` divergence).** With crate now == RJS on 50/51 URLs by word-count, the prior dispositive "crate diverges from RJS, oracle-copy cannot" anchor is gone (gov.uk-hub is now 234==RJS 234 because Stage-2 `_cleanConditionally` correctly removed the nav-chrome the Stage-1 port was retaining). Faithfulness is now anchored by **three orthogonal witnesses, not byte-equality**: (a) **Independent line-cite audit** — both the code-reviewer and the devil's-advocate independently re-derived ~30 combined constants/branches/regexes from Readability.js (DIV+5/PRE+3/TD+3/.../H1-6/TH-5 `_initializeNode` defaults, `_getClassWeight` ±25, `_textSimilarity` 0.75, `_getCharCount`, `_getLinkDensity` 0.3-hash coefficient, the rowspan/colspan formula, ≥10-rows / >4-cols / rows·cols>10 data-table thresholds, `_cleanHeaders` weight<0, the unbounded ancestor-table walk `-1`, `li-100`, embed-contentLength 75, share-strip 500, PRESENTATIONAL_ATTRIBUTES 12-list, DIV_TO_P_ELEMS 9-list, the `adWords`/`loadingWords` regex codepoints down to `广=U+5E7F` and `Реклама=U+0420…`) — every constant matches its cited JS line; no constant was adjusted to chase output. (b) **EDGAR table preservation** — crate matches RJS word-count exactly on apple-FY2025 (28566), apple-FY2019 (32102), IBM-FY2008 (11273); the data-table KEEP clauses (JS:2461,:2466) fire correctly, so the EDGAR financial tables are preserved as RJS preserves them. (c) **Hand-traced unit tests verified oracle-independent** — `grep` of the 210 lib tests for `results.json`/`RJS`/`trafilatura`/oracle/benchmark output finds zero references; every expected value was hand-derived by tracing Readability.js arithmetic at the cited lines. The **directional residual on bankofengland** is what a faithful port *should* produce: crate strips cookie-banner chrome RJS keeps AND keeps body content RJS strips — a faithful port should NOT match RJS on every URL because RJS itself has bugs (the dead-branch `tagName==="object"` at JS:2527 is the on-the-record example, faithfully transcribed dead in the port; firing it would be KEEP-direction inversion). Edge-case divergence in *mixed* directions is the *signature* of faithfulness-under-disagreement.

**Page-wrap ↔ `_prepArticle` order — REVERTED to JS-exact (Stage 1c's swap is no longer invariant under Stage 2):** Stage 1c ran the page-wrap BEFORE `_prepArticle` (a near-noop), arguing observational invariance for the Stage-1a-slice. Under full Stage 2, `_cleanConditionally` calls `_hasAncestorTag(node, "code", maxDepth=3)` (JS:2470) and `_hasAncestorTag(node, "figure", maxDepth=3)` (JS:2552) — the page-wrap interposes one extra `<div>` ancestor, shifting depths by +1 → a `<code>`/`<figure>` ancestor at JS-depth 3 becomes port-depth 4 → KEEP misses → port out-cleans RJS = §4 inversion. **Reverted to JS-exact order** (`_prepArticle` BEFORE page-wrap, inside `grab_article`). The devil's-advocate independently enumerated ALL 5 `_hasAncestorTag` call sites in `_cleanConditionally` and confirmed only the two with default maxDepth=3 are vulnerable (the others use unbounded `-1` or are pre-wrap in `_grabArticle`) — the revert is **complete**, not partial.

**`_unwrapNoscriptImages` PORTED in Stage 2 (was originally deferred; DA proved the deferral was unsafe-by-construction):** the apply agent's prior framing "score-invisible because html5ever drops noscript content" was MECHANICALLY WRONG (html5ever with `scripting_enabled:false` matches jsdom inert: it KEEPS noscript content as real children). The function `_unwrapNoscriptImages` (Readability.js:1892-1968, JS-pipeline at :2733) actively *removes* placeholder `<img>` elements lacking `src`/`srcset`/`data-src`/`data-srcset`/image-extension URLs — that changes the `<img>` count consumed by ≥3 `_cleanConditionally` shadiness clauses (img==0||img>2; p/img<0.5; gallery exception img==li_count). DA constructed an adversarial fixture proving the deferral risk. Supervisor adjudication: **port it now in Stage 2** (faithful ~80-line transcription with 13 hand-traced unit tests) — eliminates an unsafe-by-construction claim before Stage 3+ builds on it. Result: ZERO corpus-observable change (the corpus happens not to contain placeholder-`<img>` patterns that would flip `_cleanConditionally` verdicts), but now genuinely safe by construction.

**§7.4 HONEST-STOP — confirmed as the MAJOR-3 generalization (reinforces the still-open NEEDS-ARTHUR ratification):** the 3 table-heavy golds (apple-FY2025 28566, apple-FY2019 32102, gov.uk-HMRC #6 2548) are crate==RJS word-count-equal AND both ~10-15× the human "narrative-only" gold band. RJS itself runs the complete `_cleanConditionally`/`_markDataTables` and still keeps the data tables — by deliberate Readability design (the `:2461-2463`/`:2466-2468` data-table-KEEP clauses). The DA enumerated ALL data-table-affecting branches in `_cleanConditionally` and confirmed there is **no port-only faithful mechanism** by which the crate could legitimately strip these tables — any such "fix" would be out-cleaning Readability = §4 inversion. So HLD §7.4's "faithful port beats SOTA on EDGAR/HMRC gold-precision" thesis is **unreachable by a faithful port at any stage** — the same MAJOR-3 class as §7.1 gov.uk-hub and §7.2 Wikipedia. Stage 2's evidence reinforces (does not introduce a new) NEEDS-ARTHUR item.

**Determinism — INTACT.** DA ran the harness twice on Stage-2 code → results.json byte-identical. The new `HashSet<NodeKey>` and `HashMap<NodeKey,_>` (data-table flag + content_score) remain point-query-only by construction; the only new sort is over `Vec` snapshots (deterministic). Combined with Stage 1c's 3-run determinism proof, M2 differential evidence remains bulletproof.

**Bankofengland 4-word residual (the only non-word-count-parity URL):** crate 796 vs RJS 800. LCS analysis (DA): symmetric *but directional* — crate strips 31 token-types of cookie-banner/policy chrome RJS keeps AND keeps 12 token-types of real body content (rate-decision, inflation, etc.) RJS strips. ACCEPTED for Stage 2: directional mixed (not uniformly worse than RJS); chasing it risks inversion-tuning; revisit at Stage 3+ only if the residual widens or recurs on new URLs.

**Doctrine all green:** 210 lib tests + 4 parser-equivalence gate tests = 214 total, 0 failed; `cargo clippy -p mdrcel --all-targets -- -D warnings` exit 0; `cargo fmt --check` exit 0; parser-equivalence gate file + `body_text.mjs` byte-identical to commit `149c686`; `extract == extract_with(default)`; Bug-E2 `None`/empty → `Ok("")` never `Err`; harness `crate ok:51` no `crate_error`; `unsafe`-free; sync/no-I/O; frozen `benchmark/src/`/gold/score/metrics/oracle/crate_run zero diff.

Findings recorded: **MAJOR-1** (DA): byte-equal headline was 50/51 word-count-equal, not 50/51 raw-byte-equal — **corrected in this entry**. **MAJOR-2** (DA): `_unwrapNoscriptImages` was unsafe-by-construction — **ported faithfully in Stage 2** (supervisor's "port now" adjudication). **MINOR-1/3/4** (clean.rs `let _ =` doc-anchor; mod.rs deferral note; verbose JS:2527 comment) — all applied. **MINOR-2** (semver 0.3.3 PATCH vs 0.4.0 MINOR): kept PATCH (dev sandbox; consistent series with 1b/1c PATCH; only Stage 1a was MINOR as the first real extraction). **Tracked deferred (carried):** `_isProbablyVisible !important`/full clause set → Stage 3; remaining metadata + `crate_run.rs` stale-test fix + `ExtractError` compile-fence → Stage 4; bankofengland residual → Stage 3+ if it widens.

### FOR ARTHUR (Stage 2)
- **`_prepArticle` is now the full faithful order; the Readability *cleaning* layer is COMPLETE.** Crate matches Readability-JS on 50/51 corpus URLs by word-count (29/51 by raw-byte). The 1 residual is bankofengland (4 words, directional-mixed, accepted). Committed.
- **The dispositive anti-inversion anchor has shifted** (correctly): the prior "crate ⊋ RJS on gov.uk-hub" anchor dissolved when Stage 2 correctly removed the nav-chrome; the **new line-cite-based anchor** is in force (independent constant audit + EDGAR table preservation + oracle-independent hand-traced tests + bankofengland mixed-directional edge-case divergence). Documented above.
- **MAJOR-3 reinforced, not multiplied:** Stage 2's EDGAR evidence is the *generalization* of the §7.1 gov.uk-hub and §7.2 Wikipedia honest-STOPs to the §7.4 EDGAR/HMRC class. Same single open NEEDS-ARTHUR ratification item; engineering continues on the operative structural invariant. Stages 3 and 4 remain.

---

## Stage 3 — close residual gaps (`_isProbablyVisible` !important + `_fixLazyImages` + `_simplifyNestedElements`) — COMPLETE

Cycle: implement → code-reviewer (APPROVE-WITH-NITS, line-cited audit) → devil's-advocate at maximum rigor (SAFE-TO-PROCEED — **independently root-caused the bankofengland residual, OVERRODE both the implementer's and the reviewer's diagnostic mechanism**) → supervisor-authored journal → focused-verify → commit. Journal-authorship process-fix in force.

**Result: SAFE-TO-PROCEED. No BLOCKER, no MAJOR in code.** 3 Stage-3 HLD §7.5 candidates ported faithfully (every branch line-cited):
- `_isProbablyVisible` CSSOM `!important` faithfulness (helpers.rs `strip_css_important` + `inline_style_prop` — CSSOM's `getPropertyValue` returns just the value; `getPropertyPriority` is a separate accessor).
- `_fixLazyImages` (`Readability.js:2332-2412`) wired into `_prepArticle:790` slot, all 3 branches incl. the score-affecting empty-`<figure>`/`<img>` create (`:2398-2407`).
- `_simplifyNestedElements` (`Readability.js:537-565`) wired into a new `_postProcessContent` wrapper at the JS-faithful post-grab slot per `Readability.js:281-291` called at `:2754` (the implementer's brief had a slot-mistake claiming pre-grab; agent correctly followed JS).

The brief's `_postProcessContent.aria-modal/role=dialog` was already ported in Stage 1 (no work needed; honest acknowledgement).

**Headline result: raw-byte equality crate↔RJS jumped 29/51 → 50/51** (DA independently verified by running the byte-eq probe at both `7d211d4` and HEAD). The 21 newly-byte-equal URLs are dominated by Wikipedia/EDGAR/gov.uk (deep `<div>`/`<section>` nesting) — exactly the pattern `_simplifyNestedElements` cleans. Token sequence is byte-identical Stage 2 / Stage 3 / RJS on 3 representative URLs (Apple_Inc Wikipedia, Wm_Morrison Wikipedia, SEC EDGAR index) — convergence is **genuine `_simplifyNestedElements`-driven whitespace normalization, NOT false-symmetric masking**. Zero `crate_wc`/`coverage`/`edit_sim` change on any of 51 URLs (the new ports are score-invisible structural improvements; the harness tokenizer collapses whitespace runs).

Word-count parity stays 50/51 (unchanged). Bankofengland 4-word residual persists (crate 796 vs RJS 800).

**Bankofengland mechanism — CORRECTLY DIAGNOSED AT LAST (DA OVERRIDE; verify-don't-trust working at its peak).** Three prior diagnostic attributions were ALL wrong on the structural cause: (1) the implementer claimed "`<section class="page-banner">` `_cleanConditionally` parser-equivalence whitespace at `Readability.js:2566-2601`"; (2) the reviewer claimed "top-candidate ancestor-walk diverges at `<main>` vs `<div class="wrap">`"; (3) my own Stage-2 journal said "directional-mixed at cookie-banner top + body content tail" (the split was correct, but I had no mechanism). The DA's independent investigation found the real cause is a **`markup5ever_rcdom-0.39.0+unofficial` `Drop` cascade artifact**, pre-existing in Stages 1-2 (NOT introduced by Stage 3):

- `<html class="no-js menu-closed">` matches `unlikelyCandidates` regex (the "menu" alternation, `Readability.js:140-141`). Both RJS and the crate strip `<html>` on attempt-1.
- **RJS path** (`Readability.js:1117-1129` then body-fallback `:1314-1327`): JS `Element.remove()` is non-recursive; body's children survive html-removal; body-fallback fires and moves all body.children into a new DIV; returns ~5660-char articleContent with cookie banner + H1 + everything. **One attempt; one harness invocation.**
- **Crate path:** `dom::remove(html)` triggers `markup5ever_rcdom-0.39.0+unofficial/lib.rs:268-285` `impl Drop for Node`, which iterates ALL descendants and `mem::take`s their `children` RefCells (a stack-overflow guard against deep recursive Drop). Even though `body` is held alive via `page = body.clone()`, its **children are emptied** by the cascade. The fallback runs against an empty `page` → `inner_text_len=0 < 500` → retry. Attempt-2 (with `FLAG_STRIP_UNLIKELYS` cleared) parses the original bytes fresh, doesn't strip `<html>`, picks `<main class="main" id="main-content">` as top-candidate. **That's why crate output starts at the breadcrumb-inside-main, missing the pre-`<main>` cookie banner AND the pre-`<header>` `<h1>`.**
- DA-verified token-count split (LCS-aligned, 800-RJS vs 796-crate): **58 cookie-notice + 8 `<h1>`-title tokens only-in-RJS**; **45 hero-card + 4 "Bank Rate over time" + 12 MPC-tail + 1 "target" tokens only-in-crate**; LCS=734; net −4. ✓ (The original 4-word delta we tracked from Stage 1 through Stage 2 reflects this exact directional split.)

**The port is faithful to Readability.js**; the residual is a Rust DOM-library memory-management artifact, NOT a JS-fidelity gap. A fix would require reworking `dom::remove` to detach children from `node` before dropping `node` (decouples drop from cascade so externally-held descendants survive). Substantive structural change; **deferred as a post-M2 maintenance item** — impact is 1/51 URL, 4 words, no scored-metric movement (cov 0.905 / prec 0.908 already excellent; well within edit_sim 0.871). NOT closable by porting more Readability behavior.

**Determinism INTACT** (DA: 2 independent harness runs byte-identical across all 51 URLs; `_simplifyNestedElements` + `_fixLazyImages` tree mutations are deterministic; new `HashSet<NodeKey>` / `HashMap<NodeKey,_>` remain point-query-only by construction).

**Anti-inversion / no tuning:** `git diff 7d211d4 -- src/readability/scoring.rs clean.rs mark_data_tables.rs parse_int.rs dom.rs` = **empty** (the constant-bearing files all byte-unchanged); only retry/post-process/lazy-image transcription added. Zero `crate_wc`/`coverage`/`edit_sim` change across 51 URLs (a tuned port would chase either the bankofengland residual or the gold bands — neither happened).

Doctrine all green: 231 lib + 4 gate = 235 tests; clippy/fmt clean; parser-equivalence gate 15/15 byte-identical to `149c686`; `extract == extract_with(default)`; Bug-E2 `None`/empty → `Ok("")`; `crate ok:51` no `crate_error`; `unsafe`-free; sync/no-I/O; frozen `benchmark/src/`/gold/score/metrics/oracle/crate_run zero diff. Version 0.3.3 → **0.3.4 PATCH** (token-output invariant on every scored URL; only raw-byte textContent whitespace normalises — faithful convergence to RJS).

Findings recorded: **MAJOR (informational, pre-existing, deferred): the bankofengland 4-word residual is `markup5ever_rcdom` `Drop` cascade behavior**, NOT the implementer's "_cleanConditionally / parser-equivalence" nor the reviewer's "ancestor-walk" claim. Now correctly diagnosed and named in the permanent record. **MINOR (pre-existing): `inline_style_prop` returns FIRST match** (CSS cascade is LAST-wins); 1 corpus pattern hits (Wikipedia infobox `display:block;;display:inline`, no `none`/`hidden` involved → no visibility change). **MINOR (pre-existing): `_fixLazyImages` branch 1 uses `get_attribute(elem,"src")` for `<picture>`/`<figure>` not JS IDL `src`** — zero corpus hits, pathological. Tracked deferred: `dom::remove` rcdom-Drop fix (post-M2 maintenance; closes bankofengland); deferred `_postProcessContent` halves (`_fixRelativeUris` text-invariant by construction in both single-text and multi-child branches; `_cleanClasses` attribute-only) → Stage 4 IF `include_html` becomes a scored field.

### FOR ARTHUR (Stage 3)
- **3 new ports landed faithfully; raw-byte equality crate↔RJS now 50/51 (was 29/51 at Stage 2).** `_simplifyNestedElements` faithfully removes the empty/whitespace-only nodes RJS also removes, normalizing textContent serialization without changing the scored output. **Token output unchanged on every URL.** Committed.
- **Bankofengland residual mechanism CORRECTLY DIAGNOSED at last:** it's a `markup5ever_rcdom` `Drop` cascade artifact (rcdom's stack-overflow guard `mem::take`s descendant `children` RefCells when an ancestor drops, even if those descendants are held externally — which is exactly what happens when `_grabArticle` strips `<html class="...menu-closed">` while holding `page = body.clone()`). The port is faithful to Readability; the divergence is a Rust DOM-library artifact, not a JS-fidelity gap. **Three prior diagnostic attributions (implementer's, reviewer's, my own Stage-2 framing) were all wrong on the structural cause** — verify-don't-trust working as designed; the permanent record now states the truth. Deferred as a post-M2 maintenance item (1/51 URL, 4 words, no metric impact).
- **No new NEEDS-ARTHUR item.** The Stage-1b MAJOR-3 acceptance-model question remains the single open ratification item. Only Stage 4 remains (#23: metadata + Options + the deliberate `crate_run.rs` compile-fence) before final 100% verification.

---

## Stage 4 — Metadata + Options + `crate_run.rs` compile-fence arm — COMPLETE (M2 code-complete)

Cycle: implement → code-reviewer (APPROVE-WITH-NITS; branch-by-branch line-cited audit of `_getArticleMetadata` 1757-1863 + `_getJSONLD` 1632-1747 incl. `@graph`) → devil's-advocate at maximum rigor (SAFE-TO-PROCEED; 12 independent attacks all passed) → no code apply needed (no BLOCKER/MAJOR) → supervisor-authored journal → focused-verify → commit. Journal-authorship process-fix in force.

**Result: SAFE-TO-PROCEED. M2 is CODE-COMPLETE.** Full faithful port of the metadata pipeline (every branch line-cited): `_getArticleMetadata` (`Readability.js:1757-1863`), `_getJSONLD` (`:1632-1747` incl. `@graph` resolution `:1674-1678` that Stage 1a deferred), `_unescapeHtmlEntities` (`:1605-1625`), `_isUrl` (`:441-448`), CDATA-marker stripping (`:1644-1647`), `name`/`headline` similarity tie-break (`:1690-1713`). Plus: `<link rel="canonical">` + `<html lang>` capture; `_grabArticle` extended to capture in-tree byline text via `find_descendant_item_prop_name` (`Readability.js:1087-1100`) and `article_dir` ancestor walk (`:1580-1592`).

**Public API additions** (additive, behind the unchanged frozen surface): `Extracted` gains 5 `Option<String>` fields (`byline`/`excerpt`/`site_name`/`published_time`/`dir`) + `#[derive(Default)]`; new `ExtractError::ContentTooShort { word_count, threshold }` variant; `Options.include_html` + `Options.min_word_count` wired; `Readability::include_html(bool)` builder; `dom::serialize_html` (via html5ever's serializer with `TraversalScope::ChildrenOnly(None)` mirroring JS `el.innerHTML`).

**The deliberately-reviewed `crate_run.rs` compile-fence arm landed** (the HLD-documented Stage-4 fence-firing event): `ExtractError::ContentTooShort` maps to `CrateStatus::CrateError("content_too_short: N < M")` — carries both numbers; cannot fire on the default-`Options` corpus path (`min_word_count==0` short-circuits, DA-verified adversarially); harness `crate ok:51` no `crate_error` preserved. Stage-1c-tracked stale M1-floor tests in `crate_run.rs` faithfully rewritten to post-M2 reality + 2 new Stage-4 tests pinning `ContentTooShort` mapping and default-options-never-fires.

**Default `extract()` is BYTE-IDENTICAL to Stage 3 across all 51 URLs** (DA independently re-ran the harness 3 times — content hash `dfadea1699a9939f40eb1ac346147334` byte-identical across all 3 runs; diff vs Stage-3 baseline `runs/2026-05-18T18-37-18Z` = **0 cells changed** on `crate_wc`/`coverage`/`precision`/`edit_sim`/`crate_status`/`status_detail`). Metadata population is score-invisible (the harness scores `.text`, not `.byline`/`.lang`/etc.). The 50/51 word-count parity is unchanged (bankofengland 796 vs 800 persists per Stage-3's correctly-diagnosed rcdom-Drop-cascade mechanism). Parser-equivalence gate 15/15 byte-identical to `149c686`.

**DA's important discovery (silent fix to a Stage-3 unfaithfulness):** Stage 4's cross-attempt threading of `byline_found_state` is actually a JS-faithful correction. Stage 3 reset the byline-detect state per-attempt; `Readability.js:1082-1085`'s gate uses `this._articleByline` which is INSTANCE STATE persisting across retry attempts. Stage 4 threads `byline_found_state` correctly across attempts. The corpus does not contain a URL whose retry-loop-with-byline interaction exercises a measurable delta (so the 0-diff vs Stage 3 holds), but the fix is structurally correct and removes a latent regression surface.

**Anti-inversion / no tuning:** `git diff 997bff5 -- src/readability/scoring.rs src/readability/clean.rs src/readability/regexps.rs src/readability/mark_data_tables.rs src/readability/parse_int.rs` = **empty** (no constants/regex/thresholds touched). DA confirmed 12 ports/branches all faithful with line citations.

**Determinism INTACT** (DA: 3 independent harness runs byte-identical; new `serde_json` parsing is deterministic; metadata `HashMap`/`HashSet` iteration is never read into scored output — score still flows only from `.text`).

**Honest disclosures (DA-validated, accepted):**
- **1-line touches to `benchmark/src/score.rs:1058` and `benchmark/src/crate_run.rs:332-345`** adding `..Extracted::default()` to two test-helper struct-literals — Rust-mechanical consequence of additive `Extracted` fields. Scoring logic and oracle adapter logic both byte-unchanged. KEEP-AS-IS (DA: alternative `pub fn for_test()` constructor would be MORE invasive, adding new public API surface for a test-only need; the `..Default::default()` pattern is the least invasive).
- **Version 0.3.4 → 0.4.0 MINOR.** Adding an enum variant to a non-`#[non_exhaustive]` `pub enum ExtractError` is technically breaking (Rust SemVer guide). Defensible for a pre-1.0 crate (0.X breaks land on 0.Y.0); post-1.0 would mandate MAJOR. DA AGREE. Documented inline in `Cargo.toml`.
- **`serde_json="=1.0.149"`** added as the single new dep — pinned, pure-CPU, no I/O, no `unsafe` in hot path, used ONLY in `_getJSONLD`. Min-viable; a hand-rolled JSON parser for the JSON-LD subset would be ~200 lines of new divergence surface (Unicode escapes, surrogate pairs).

Doctrine all green: **269 lib + 4 parser-equivalence gate + 208 benchmark = 481 tests, 0 failed**; clippy/fmt clean across the workspace; parser-equivalence gate byte-identical to `149c686`; `extract == extract_with(default)`; Bug-E2 `None`/empty → `Ok("")`, never `Err`; `crate ok:51` no `crate_error`; `unsafe`-free; sync/no-I/O; frozen `benchmark/src/metrics.rs`/`oracle.rs`/parser-gate/gold byte-unchanged; `benchmark/src/score.rs` and `benchmark/src/crate_run.rs` touched only with the audited mechanical/deliberate edits.

Findings recorded: **3 MINORs (all test-coverage polish, non-blocking)** from the DA — recommend a unit test for cross-attempt `byline_found_state` threading + a `metadata_byline_short_circuits_in_tree_byline_detect` adversarial dual-presence test + an observation that `apnews.com/hub/technology` produces `byline = "11"` (in-tree detect fires on a time-template node) and `github.blog` produces `byline = Some("")` (JS-faithful `parsed.author.name.trim()`). All JS-faithful; cosmetic, not defects. **Deferred to #24 final-verification phase as polish candidates.**

### M2 STATUS — code-complete

Stage 0 (DOM facade + parser-equivalence BLOCKER gate) → Stage 1a (parse + scoring + single top-candidate) → Stage 1b (sibling-append) → Stage 1c (FLAG_* retry/flag-sieve + page-wrap) → Stage 2 (`_markDataTables` + `_cleanConditionally` + full `_prepArticle`) → Stage 3 (`!important` + `_fixLazyImages` + `_simplifyNestedElements`) → **Stage 4 (metadata + Options + fence-arm + stale-test fix). The faithful Readability port is COMPLETE.**

The operative structural-faithfulness invariant holds: **crate matches Readability-JS on 50/51 corpus URLs by post-tokenizer word-count AND by raw textContent byte-equality**; the 1/51 residual (bankofengland 796 vs 800) is correctly diagnosed as a `markup5ever_rcdom` `Drop` cascade artifact (NOT a Readability fidelity gap; deferred as a post-M2 maintenance item). Anti-inversion anchors: line-cite audit (30+ constants independently re-derived by reviewers and DAs across all stages), EDGAR table preservation (crate ≡ RJS on all 3 table-heavy golds), oracle-independent hand-traced tests (zero `results.json`/oracle references in any test), the directional-mixed bankofengland residual as the faithfulness-under-disagreement signature.

Tracked open items at M2 close: **MAJOR-3 acceptance-model reframe (NEEDS-ARTHUR ratification)** — still the single open normative item; engineering ran continuously on the operative structural invariant pending the call. Post-M2 maintenance candidate: rework `dom::remove` drop semantics to close the bankofengland residual.

### FOR ARTHUR (Stage 4 + M2)
- **M2 IS CODE-COMPLETE.** The faithful Mozilla Readability port lands at byte-equality with the Readability-JS oracle on 50/51 corpus URLs (29/51 → 50/51 raw-byte after Stage 3's `_simplifyNestedElements`); the 1 residual is correctly diagnosed and accepted as a Rust DOM-library artifact. 481 tests green; the harness still runs `crate ok:51`; the deliberate `crate_run.rs` compile-fence fired and was resolved by-design. Committed.
- **Only #24 remains: independent agent-set final 100%-vs-HLD verification.** That stage will: (i) cross-check every HLD section against the implementation; (ii) iterate to true 100% completion if any gap; (iii) make a final supervisor commit closing the milestone.
- **NEEDS-ARTHUR (carried, unchanged):** the MAJOR-3 acceptance-model reframe (structural-faithfulness invariant; gold Coverage = by-design diagnostic residual) — your call when back. M2's engineering ran clean on this operative interpretation; final acceptance (#24) is whatever you ratify.

---

## #24 final 100%-vs-HLD verification — COMPLETE; M2 closed

Cycle: 18 sequential read-only verifier-agent iterations under the /goal self-paced loop, each cross-checking exactly one HLD section against the as-built code. Supervisor (main ctx) reviewed each agent's gap report and dispatched the next iteration; verifiers were forbidden from suggesting constant/threshold/regex changes (anti-inversion). Process-fix in force: agents supplied line-cited evidence, supervisor authored all adjudication.

**Result: M2 IS CLOSED.** All 18 §-verifications passed. 5 HLD doc-drift findings surfaced (all documentation-only; zero code-behaviour changes; all line-cite anchors and anti-inversion guarantees preserved). 2 stale code-comments fixed at close-out.

Verification ledger (18/18 cleared):
- §1 Design in one paragraph — VERIFIED no gaps (15 claims, file:line cites).
- §2 Fidelity anchor (`textContent` jsdom semantics + `_setNodeTag` slow-branch-only) — VERIFIED no gaps.
- §3 Dependency decision (rcdom + html5ever + tendril + serde_json pins) — VERIFIED no gaps.
- §4 Anti-inversion invariants (keystone section — success metric, Jaccard non-gating, mechanised guard B-4) — VERIFIED no gaps.
- §5 + §5.1 Module layout + side-table invariant — **VERIFIED via HLD amendment `b29691a`** (5 doc-drift fixes: `clean.rs`/`mark_data_tables.rs`/`parse_int.rs` added to §5 table; `_hasAncestorTag` relocated helpers→clean; `_getTextDensity` relocated scoring→clean; `NodePtr`→`NodeKey`; §5.1 `debug_assert` claim replaced with the as-built structural guarantee at `dom.rs:649-664`).
- §6 + §6.1/§6.2 Stage 0 DOM facade + parser-equivalence BLOCKER — VERIFIED no gaps (gate byte-identical to `149c686`; 15 snapshots: 7 gold + 8 EDGAR/HMRC).
- §7.1 Stage 1a parse + scoring + single top-candidate — VERIFIED no gaps (12 constants line-cited; `NB_TOP_CANDIDATES=5` + `charThreshold=500` pinned by dedicated tests).
- §7.2 Stage 1b sibling-append — VERIFIED no gaps (11 sibling-append claims; refetched-`Vec` snapshot at :615/:706; i64 signed-index walk; ALTER_TO_DIV_EXCEPTIONS matches `Readability.js:199`).
- §7.3 Stage 1c FLAG_* retry / flag-sieve / fallback — VERIFIED no gaps (4 constraint-tests pin invariants: `charThreshold`, ABA-isolation, longest-attempt stable-sort, page-wrap order).
- §7.4 Stage 2 `_markDataTables` + `_cleanConditionally` + full `_prepArticle` (the heaviest port) — VERIFIED no gaps (all 24 `_prepArticle` steps, all 6 KEEP clauses, all 8 shadiness clauses, all 9 `_markDataTables` branches with thresholds 10/4/10).
- §7.5 Stage 3 residual text-affecting gaps — **VERIFIED via residual annotation `4ca57ee`** (1 gap: `inline_style_prop` FIRST-match vs CSS-cascade LAST-wins; doc-comment annotation at `helpers.rs:122-136` + HLD §7.5 named-residual bullet; 1 corpus pattern hits, no visibility-decision change).
- §7.6 Stage 4 metadata + Options + fence-arm — VERIFIED no gaps (`_getArticleMetadata` + `_getJSONLD` incl. `@graph` + `_unescapeHtmlEntities` + `_isUrl`; 5 `Extracted` `Option<String>` fields; `ContentTooShort` fence-arm pinned by 2 tests).
- §7.7 Deferred / out-of-M2 boundary — VERIFIED boundary intact bidirectionally (8 named-deferred items all Grep-absent; in-scope items all present).
- §8 Regex dialect fidelity — **VERIFIED via HLD amendment `0e6925e`** (5 gaps: HLD `(?-u:\W)` claim corrected to `[^A-Za-z0-9_]` literal-class substitution; 4 score-invisible JS REGEXPS entries `replaceFonts`/`srcsetUrl`/`nextLink`/`prevLink` added to §7.7 deferred list; conformance-table at `regexps.rs:539-688` pins all 31 ported patterns).
- §9 Integer coercion fidelity — VERIFIED no gaps (`parse_int_js` ECMA-262 §21.1.3.18; saturating overflow documented; 4 conformance tests pass; zero native Rust `parse`/`from_str` on DOM-attribute numerics in scored paths).
- §10 Faithfulness verification yardstick — VERIFIED no gaps (`git diff 149c686 HEAD` on constant-bearing files = pure additions, 2927 insertions / **0 deletions**; oracle-independent tests; `score.rs:267-279` reference resolution is gold-if-present-else-Trafilatura, never RJS).
- §11 Critical files — **VERIFIED via HLD amendment `716f374`** (9 inventory gaps: §11 was 5 bullets for Stage-1a scope; expanded to 17 bullets grouped Crate / Tests+pins / Harness+oracle; `prep.rs` role-drift corrected; BLOCKER gate + frozen jsdom probe + `Cargo.toml`/`Cargo.lock` + Stage-2/3/4 files all enumerated).
- §12 Top risks dispositions — VERIFIED no gaps (all 9 risk rows confirmed: textContent separators, RegExp dialect, live snapshotting, numeric divergence, retry-loop reset, _setNodeTag slow-only, scope-minimization, compile-fence by design, Jaccard non-gating).

Five HLD amendments landed this verification session:
- `b29691a` — §5/§5.1 file table refresh + structural-guarantee text
- `4ca57ee` — §7.5 `inline_style_prop` named residual + in-code annotation
- `0e6925e` — §8 `\W` substitution claim + §7.7 score-invisible REGEXPS deferrals
- `716f374` — §11 critical-files inventory expansion (5 → 17 bullets)
- this commit — M2-CLOSE journal + 2 stale code-comment fixes (`prep.rs:407-409` `_fixLazyImages` "DEFERRED"→"wired at Stage 3"; `regexps.rs:21-23` `adWords`/`loadingWords` "deferred"→"ported in Stage 2")

**Anti-inversion preserved across all 18 iterations.** Verifiers cited Readability.js line numbers for every claim. Constant/threshold/regex changes: ZERO. `git diff 149c686 HEAD` on `scoring.rs`/`clean.rs`/`regexps.rs`/`mark_data_tables.rs`/`parse_int.rs`: 2927 insertions / 0 deletions (pure JS-faithful growth). Frozen surfaces intact: `tests/parser_equivalence_gate.rs`, `benchmark/src/metrics.rs`, `benchmark/src/oracle.rs`, `benchmark/oracles/readability-js/body_text.mjs` all byte-identical to `149c686`. Doctrine green at close: clippy + fmt clean; `unsafe`-free; sync/no-I/O; `extract == extract_with(default)` and Bug-E2 `Ok("")` invariants pinned by their tests.

**Three brief-side process artifacts noticed during the verification arc** (recorded honestly): (a) two of the supervisor's verifier briefs contained factual errors that the verifier agents caught and corrected against the source-of-truth — `ALTER_TO_DIV_EXCEPTIONS` was conflated with `DIV_TO_P_ELEMS` in the §7.2 brief; one `_cleanConditionally` shadiness clause was paraphrased imprecisely in the §7.4 brief. Both times the agents verified against the HLD + Readability.js themselves and reported the brief mismatch — this is anti-inversion working as designed. (b) A stale-state `/loop` wake-up fired several times AFTER state had already advanced via task-notification; on each occurrence the supervisor consulted `TaskList` for ground-truth before proceeding, never duplicating completed work. (c) The clippy-on-contended-system delay (Arthur's other Claude sessions running parallel cargo builds) triggered a one-time 5+ minute wait on the §7.5 amendment; the loop survived via the cache-warm fallback wake.

**Carry-forward to M3** (single open normative item, unchanged from §M2-code-complete entry above):
- **MAJOR-3 acceptance-model reframe — NEEDS-ARTHUR ratification.** Structural-faithfulness invariant; gold Coverage on table-heavy URLs (gov.uk-hub §7.1, Wikipedia §7.2, EDGAR/HMRC §7.4) is by-design diagnostic residual, NOT a defect. Engineering ran continuously on this operative interpretation. Arthur's call when back.
- **Post-M2 maintenance candidate** (deferred, not blocking): rework `dom::remove` so children detach before parent drops — decouples Drop from rcdom's stack-overflow-guard cascade and closes the bankofengland 4-word residual (1/51 URL, no scored-metric movement, not closable by porting more Readability behaviour).

### FOR ARTHUR (#24 + M2 closed)
- **M2 IS CLOSED.** The Readability port is faithful to Readability.js at every cited line; the HLD is faithful to the as-built code at every section; the BLOCKER gate is frozen at `149c686`; doctrine green at close. The 5 HLD amendments landed during #24 are all descriptive reconciliation (no code behaviour changed; no constant tuned; anti-inversion preserved).
- **The loop's self-pacing pattern worked.** 18 verifier-agent iterations + 5 supervisor-authored HLD amendments + 2 close-out code-comment fixes + this journal entry, all under the /goal loop with cache-warm 90s cadence. Main context stayed lean (heavy file reads delegated to subagents); doctrine doctrine-red never triggered; NEEDS-ARTHUR carry remained the single open normative item.
- **Next operative move pending your direction:** ratify (or amend) the MAJOR-3 acceptance-model reframe, then either (a) close M2 milestone formally + auto-pivot to M3 per the loop's open-ended stopping policy, or (b) pause and discuss M3 scope before the loop resumes engineering. Task #20 (M3 pivot) is queued and blocked-by-#19 in the ledger; engineering is ready to proceed when you say go.
