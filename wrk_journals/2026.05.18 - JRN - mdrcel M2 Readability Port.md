# JRN — mdrcel M2 (Idiomatic Rust Port of Mozilla Readability 0.6.0)

Supervisor: Claude (agent-team model). Maintained per Arthur's standing instruction.
Branch: `feat/oracles-and-corpus`. Start: 2026-05-18. Predecessor: M1 + gold sub-phase complete & frozen (`7afaabe`).

---

## Supervision model (Arthur's instructions, in force)
- Supervisor gives high-level direction only; does NOT test/debug/read-heavy itself — delegates to agents to protect context.
- Per stage: implement (TDD) → code-reviewer skill → devil's-advocate challenge → code-review-apply-changes skill → focused verify → **supervisor** atomic commit (authored files only; Co-Authored-By trailer; check `git status` first).
- A devil's-advocate team member reviews/challenges the other agents EVERY stage (incl. challenging the supervisor).
- Maintain this journal; commit at stage boundaries. Final stage: independent agent set verifies 100% vs the finalized HLD; iterate to 100%.
- M2 was Arthur-gated; **Arthur greenlit it** this session.

## Methodological precondition (carried from gold sub-phase)
Gold set frozen & human-decided; anti-inversion sacrosanct. The port is a **faithful transcription of Readability.js**, never tuned by copying oracle output. A faithful clean port is *expected* to legitimately beat the oracles on gold-precision (they interleave tables the gold excludes); an oracle-copy could only tie — that delta is itself the anti-inversion proof.

---

## Stage R0 — Plan + ratification (this entry)

### Architect plan (opus Plan agent)
Delivered a rigorous M2 HLD: fidelity anchor (`textContent` = raw depth-first descendant-text concat, zero synthetic separators); dependency `html5ever` + `markup5ever_rcdom` (kuchikiki fallback); 1:1 module map to Readability.js (`regexps/dom/grab_article/scoring/prep/helpers/metadata`); 5-stage breakdown; faithfulness-verification + anti-inversion sections; 8 risks flagged for supervisor/DA.

### Devil's-advocate challenge (opus) — verdict SAFE-WITH-CHANGES; 4 BLOCKER / 6 MAJOR / 4 MINOR
**All 14 findings ACCEPTED by the supervisor.** Summary + adjudication:

| # | Sev | Finding | Supervisor adjudication |
|---|-----|---------|-------------------------|
| B-1 | BLOCKER | Fidelity anchor cited `JSDOMParser.js` — **dead code** here; oracle uses jsdom 29.1.1 (`run.mjs:184`). Conclusion holds but via WHATWG DOM `textContent`, not the cited source. | ACCEPT. Re-anchor to WHATWG DOM; add Stage-0 empirical probe vs jsdom 29.1.1; state JSDOMParser.js unused (`_docJSDOMParser` undefined under jsdom). |
| B-2 | BLOCKER | `_setNodeTag` risk **inverted**: `_docJSDOMParser` falsy ⇒ oracle ALWAYS takes the slow `createElement`/`replaceChild` branch (Readability.js:760-772), not the in-place one. | ACCEPT. Port mirrors the slow branch only (new element, children moved in order, attrs cloned, `readability` score carried). Drop fast-path porting. |
| B-3 | BLOCKER | Stage-3 "≥0.95 Jaccard vs Readability-JS" is **not harness-measurable** (`score.rs:267-279`: gold→Trafilatura; "Readability is never the reference"). BRIEF "~95%" is *descriptive*, not an acceptance gate; promoting it = inversion. | ACCEPT (critical). Acceptance = gold Coverage/Precision + the consumer cases. "~95% vs RJS / ~70-80% vs Trafilatura" = **descriptive expectation only**. RJS-Jaccard = non-gating diagnostic. |
| B-4 | BLOCKER | "Converge to ≥0.95 vs JS oracle" = unmechanised oracle-mimicry back door. | ACCEPT. Mechanised guard: every scoring change commits with exact Readability.js line(s) + JS-behaviour statement; RJS-Jaccard never an exit threshold; divergence closed only by a test asserting JS-specified behaviour; oracle-Jaccard-as-acceptance = explicit ANTI-GOAL; per-stage DA audits for output-matching-without-citation. |
| M-1 | MAJOR | Stage-0 parser-equivalence is a flagged spike, not a gate; 124-table FY2019 EDGAR maximally stresses implied-`<tbody>`/foster-parenting (0 `<tbody>`, 1001 `<tr>`). | ACCEPT. Promote to **BLOCKER gate**: post-tokenizer token-sequence identity jsdom-29.1.1 vs html5ever+rcdom for all 7 gold + table-heavy non-gold snapshots, before Stage 1a. Divergence ⇒ parser changes, not Stage 1. |
| M-2 | MAJOR | `markup5ever_rcdom` is `+unofficial`/"unsupported, test-support"; DA verified it *builds* (html5ever 0.39 + rcdom 0.39 + tendril 0.5, rustc 1.95.0, edition 2024, ~36 crates incl. unused xml5ever/web_atoms). "minimal graph" claim false. | ACCEPT. Record honest status + count; pin Cargo.lock; kuchikiki switch criterion = rcdom borrow-mgmt blocks Stage 0 (same algorithm, no plan change). |
| M-3 | MAJOR | Stage 1 is a monster (~60% of Readability.js) — defeats per-stage review. | ACCEPT. Split → 1a (parse+scoring+single top-candidate; first Ok; gate example.com+gov.uk hub), 1b (sibling append 1415-1535; gate Apple/Morrison Wikipedia prose band), 1c (FLAG_* retry+fallback; gate no regression). |
| M-4 | MAJOR | Supervisor's "metadata→Stage 4" ruling creates hidden Stage-1 dep: `_headerDuplicatesTitle` (1105) uses full `_articleTitle` (=`_getArticleMetadata`/`_getArticleTitle` 572-651, set 2745 before grab 2747) to delete headings from body. | ACCEPT — refines my ruling. `_getArticleTitle`+title-metadata → Stage 1a; Stage 4 = remaining non-body metadata. Metadata stays MANDATORY M2 scope. |
| M-5 | MAJOR | JS `\s` (incl. `﻿`) ≠ Rust `regex` `\s`; JS `\W` ASCII ≠ Rust `\W` Unicode — bites `normalize`/`whitespace`/`hasContent`/`tokenize` (scored surface). No backrefs/lookaround (verified) — that claim holds. | ACCEPT. Every ported regex uses explicit JS-compatible classes (`(?-u:\W)`; explicit JS `\s`); per-regex conformance test table as Stage-0/1a oracle. |
| M-6 | MAJOR | Real fidelity risk is `parseInt`-of-attribute (rowspan/colspan, 2245-2259) → `_getRowAndColumnCount`→`_markDataTables`→scored EDGAR text — not float arithmetic. No `Math.round` exists (verified) so that concern moot. | ACCEPT. JS-`parseInt`-faithful helper (ws-skip, sign, longest digit prefix, NaN→`||1`/`||0`); pin with EDGAR rowspan/colspan classification tests. |
| m-1 | MINOR | Score/data-table maps safe iff point-query-only (verified vs reference). | ACCEPT. HLD invariant + debug-assert/test nothing iterates them in scored path. |
| m-2 | MINOR | Some stage exits not `results.json` fields. | ACCEPT. Each stage names exact field its gate reads. |
| m-3 | MINOR | Retry-loop clone semantics under slow `_setNodeTag`. | ACCEPT. Re-parse from original bytes per attempt; pin with test; decided at 1c review. |
| m-4 | MINOR | Stage-0 must not silently flip baseline. | ACCEPT. Stage-0 exit = compiles + parser gate passes + `cargo test` green + every URL still `not_implemented`. |

### Finalized stage plan (post-adjudication)
- **Stage 0** — DOM facade + parse spine (infra). Hard parser-equivalence BLOCKER gate. Baseline floor unchanged (all `not_implemented`).
- **Stage 1a** — parse + `_grabArticle` scoring + single top-candidate + `_getArticleTitle`/title-metadata; first `Ok`. Gate: example.com + gov.uk hub gold Scored, high precision.
- **Stage 1b** — sibling append. Gate: Apple/Morrison Wikipedia prose-only Coverage band.
- **Stage 1c** — FLAG_* retry/flag-sieve loop + fallback (+ re-parse-per-attempt). Gate: no regression on 1a/1b.
- **Stage 2** — `_markDataTables` + `_cleanConditionally` + `_prepArticle`. Gate: 3 table-heavy EDGAR/HMRC gold Scored; per-URL precision ≥ oracles' (the faithful-port-beats-SOTA delta, evidenced in results.json).
- **Stage 3** — close residual gaps. Gate: gold Coverage/Precision bands + the consumer cases; RJS-Jaccard diagnostic only (NOT a gate).
- **Stage 4** — remaining non-body metadata + Options + deliberate reviewed `crate_run.rs` compile-fence arm. Default `extract()` byte-identical (zero scored-text change).
- **Final** — independent agent set verifies 100% vs HLD; iterate to 100%; DA final challenge; supervisor closeout + baseline re-bless (HLD §9).

### FOR ARTHUR (decisions I made in your stead — all reversible)
1. **Anti-inversion honesty correction (most important):** "≈95% vs Readability-JS" is a *descriptive expectation*, **not** an acceptance gate (the harness can't even score it; Readability is never the reference). M2 is accepted on gold Coverage/Precision + the consumer cases. This corrects the roadmap framing I gave you earlier — the DA caught it; I ratified the correction.
2. Metadata is **in M2 scope** (faithful port; de-risks M5 the consumer shim), sequenced last — but title-resolution moved earlier (it feeds body-text decisions). DA challenged my first cut; I refined it.
3. Dependency `html5ever`+`markup5ever_rcdom` accepted despite its `+unofficial` status (DA verified it builds); kuchikiki is the documented fallback.
All ratifiable/amendable; the HLD records full rationale.

### Artifacts
- Final HLD: `wrk_docs\2026.05.18 - HLD - mdrcel Readability Port (M2).md` (written by writer agent from architect plan + DA amendments + these adjudications).
- Tasks #18 (ratify) … #24 (final verify).

---

## Stage 0 — DOM facade + parser-equivalence BLOCKER gate — COMPLETE

Cycle: implement (opus, TDD) → code-reviewer (opus) → devil's-advocate (opus) → apply (opus, code-review-apply-changes) → independent focused-verify (opus) → supervisor commit.

**Result: parser-equivalence BLOCKER gate PASS 15/15** (7 gold + 8 non-gold incl. 124-table FY2019 EDGAR, 72k-token Berkshire, legacy IBM ~1500 `<font>`, + 2 EDGAR index pages added during apply). `html5ever`+`markup5ever_rcdom` is post-tokenizer sequence-identical to jsdom 29.1.1 across the whole 51-snapshot corpus. **rcdom stands; HLD §6.1 kuchikiki trigger NOT met.** `lib.rs` still `NotImplemented`; harness floor honestly VACUOUS (no laundering).

Findings (all accepted & applied):
- code-reviewer — APPROVE-WITH-NITS; MAJOR M1 = empty-vs-empty (`[]==[]`) laundering path; the gate's own "fails loudly on empty" contract did not exist in code.
- devil's-advocate (highest-value) — **MAJOR-2: proved html5ever≢jsdom on non-whitespace stray text in table parts (foster-parenting: rcdom `FOSTERcell` vs jsdom `cellFOSTER`).** 15/15 PASS is corpus-luck (every corpus snapshot has 0 non-ws stray table text), NOT general substrate proof. Also overrode the reviewer's factually-wrong "2 excluded EDGAR pages are link-lists" — they are tabular and both PASS → included.
- Applied: token-vector non-empty assert (M1); foster divergence pinned in a witness test + a per-snapshot **self-policing guard** that fails loudly if any future snapshot enters the divergent class (+ non-UTF8 rejection) + over-claim wording downgraded (M2); tokenizer-mirror FNV-1a-64 content-hash tripwire over `metrics.rs::tokens` (M3 — `sha2` unavailable in the frozen manifest; FNV-1a documented per the brief's "simplest robust option" licence; multi-agent-safe, touches only the Stage-0 test file); 2 EDGAR index pages included (m1); vacuous `debug_assert` de-theatred (m2); notes hygiene (m3); bounded HLD path-accuracy fix (m4).
- Independent focused-verify — COMMITTABLE: 58 tests green, clippy/fmt clean, gate 15/15 (real Node spawns), FNV hash independently recomputed = pinned constant, FIX-1/FIX-2b prove-out actively demonstrated, all committable files NUL-clean.

**TRACKED STANDING RISK (M2):** html5ever and jsdom *provably* diverge on non-whitespace stray `#text` directly under `table/tbody/thead/tfoot/tr` (foster-parent ordering). Currently **corpus-inert** (0/51 in that class) and **self-policing** (gate guard fails loudly if any future gold/corpus addition enters it → re-triggers the HLD §6.1 rcdom→kuchikiki decision). Not a defect now — a bounded, surfaced, monitored substrate limitation. Later stages / corpus growth must respect the guard, never weaken it.

Version: mdrcel 0.2.1→0.2.2 (patch — internal infra + deps; frozen surface byte-unchanged; `readability` `#[doc(hidden)]`). Deps pinned `=0.39.0`/`=0.5.0`; `markup5ever_rcdom` `+unofficial`/test-support status recorded in `Cargo.toml`. (`notes/m2-stage0.md` is gitignored scratch — durable record is here + the commits.) Tasks: #19 → completed; #20 (Stage 1a) next.

---

## Stage 1a — parse + scoring + single top-candidate (first real Ok) — COMPLETE

Cycle: implement → **recovery (interrupted agent)** → re-implement/complete → code-reviewer (opus) → devil's-advocate (opus) → apply (opus) → independent focused-verify (opus) → supervisor commit.

**Recovery event (honest record):** the first Stage-1a agent's API socket dropped after 38 tool-uses, leaving partial unreviewed work (`regexps.rs` ~18.5 KB + `Cargo.toml` regex dep/version; `Cargo.lock` stale; no other modules; lib.rs untouched so floor intact). `SendMessage` unavailable + `Agent` starts fresh ⇒ could not resume. A fresh agent recovered: validated the partial `regexps.rs` against Readability.js (kept; one correct fix — the `regex` crate rejects `(?-u:\W)` on `&str`, replaced with the exact-equivalent ECMAScript class `[^A-Za-z0-9_]`), regenerated `Cargo.lock`, completed the full Stage-1a scope.

**Result: M2 is off the floor.** Crate `ok:51` (was `not_implemented:51`); **50/51 Scored, ZERO regressions** vs the M1 vacuous baseline. The 1 non-Scored = IBM FY2008 `reference_empty` — Trafilatura's *own* empty output (the DEC-G8 oracle-weakness case), crate is `Ok` with `crate_wc==rjs_wc==11273` — NOT a crate defect; 50 is the max achievable.

**example.com — faithful PASS:** cov 0.75 / prec 0.857; `crate_wc=16 == Readability-JS wc=16` (token-sequence identical). The 0.75 ceiling is the *documented* gold-vs-Readability divergence (`_headerDuplicatesTitle` faithfully removes the `<h1>` at sim 1.0>0.75; `gold.tsv` itself records "both oracles drop the `<h1>`"). Not a defect, not tuned.

**gov.uk-hub — HONEST STOP → principled HLD amendment (supervisor adjudication, DA-validated):** the implementing agent refused to fudge and surfaced that HLD §7.1's "high precision on gov.uk-hub" sub-gate is **methodologically impossible for a faithful port** and contradicts the gold's OWN frozen anti-inversion design (`gold.tsv` curated gov.uk-hub as the case where Readability *fundamentally fails*; RJS itself = 234 nav words, none the gold line; human gold band 6-12). The DA independently ran RJS and proved **RJS ⊆ crate (371 ⊋ 234, 0 RJS-only tokens)**; the 137-word delta is *exactly* the deliberately-deferred Stage-2 `_cleanConditionally` / Stage-1b sibling-append. DA actively hunted for an in-scope Stage-1a mechanism that *should* cut the cruft and found none (nav columns carry a class containing "and" → `okMaybeItsACandidate` → Readability.js itself does not strip them). **Adjudication: PRINCIPLED spec-bug fix, NOT gate-weakening** — demanding 6-12 would *force* the §4 inversion the whole HLD forbids. Applied the DA's exact loophole-closing wording to HLD §7.1: the pass condition is now the structural invariant **crate token-multiset ⊆ Readability-JS token-multiset** (no invented content); reaching 6-12 by out-cleaning RJS is an explicit **ANTI-GOAL** rejected at the §4.3(e) audit; the 6-12 human gold band remains frozen ground truth, reported as the expected Coverage residual, not a pass/fail threshold.

**Anti-inversion record (DA-mandated, §4.3(e)/§10) — why the EDGAR≡RJS identity is faithful, not inversion:** crate token-sequence is *identical* to Readability-JS on the table-heavy golds (AAPL-FY2025 28566, AAPL-FY2019 32102, IBM-FY2008 11273) and example.com (16). This is expected and faithful: EDGAR filings are single-container documents where Stage-1a (single top-candidate; sibling-append/`_cleanConditionally` deferred) and full RJS *necessarily converge*. The **dispositive non-inversion proof is the gov.uk-hub divergence**: a port tuned to mimic the oracle would match RJS=234 there; the faithful port correctly produces 371⊋234, exactly as Stage-1a scope predicts. Crate has zero I/O (cannot observe the oracle at runtime); all test expecteds were hand-traced from Readability.js (3 self-corrected by re-tracing JS — `_replaceBrs` independently confirmed byte-identical to real jsdom, genuine re-trace not back-fitting). Faithfulness was independently verified by reviewer + DA against ~30 combined cited Readability.js ranges; every ported regex/constant is verbatim; `tokenize` `[^A-Za-z0-9_]` proven exactly ECMAScript `\W` via Node.

Review findings: code-reviewer APPROVE-WITH-NITS (no BLOCKER/MAJOR); devil's-advocate SAFE-AFTER-FIXES (no BLOCKER/MAJOR; strengthened conclusions with direct evidence). Fixes applied: `.gitignore` `wrk_gold/` (commit-atomicity, MINOR-1); **`is_js_space` consolidated** to one canonical `pub(crate) dom` predicate + exhaustive conformance pin (MINOR-2, escalated by supervisor to fix-now — it gated scored-path heading deletion; verified PURE refactor: mean Coverage 0.5716 to 4 dp unchanged, all anchors exact); doc-comment tightening (incl. a follow-on stale-comment fix at `helpers.rs` the verify pass flagged); HLD §7.1/§7.5 amendment. Independent focused-verify: COMMITTABLE — 117 lib + 4 gate tests green, clippy/fmt clean, parser-gate 15/15 byte-identical to `149c686`, full harness re-run zero-delta, 13-file set NUL-clean.

Tracked deferred: MINOR-3 `_isProbablyVisible` `display:none !important` → Stage 3 (HLD §7.5 noted); `NodeKey` Rc-ptr ABA observation → re-audit at Stage 1c (retry re-parse) / Stage 2 (conditional removal churn nodes more aggressively).

Version: mdrcel 0.2.2→0.3.0 (minor — first real extraction behaviour behind the signature-unchanged frozen surface). Tasks: #20 → completed; #25 (Stage 1b — sibling append) next.

### FOR ARTHUR (Stage 1a)
- **Contract amendment made in your stead (reversible/amendable):** HLD §7.1 gov.uk-hub sub-gate was mis-specified — it demanded a precision a faithful Readability port provably cannot reach on a nav-hub, which would have *forced* the anti-inversion violation the whole project guards against. I adjudicated it a principled spec-bug fix (devil's-advocate independently validated with RJS⊆crate evidence) and applied the DA's gameable-proof structural-invariant wording. Full rationale + evidence above; ratify or revert at will.
- **M2 genuinely extracts now** — the crate is off the floor, faithfully (independently proven non-inverted). 50/51 Scored; the 1 gap is Trafilatura's own known weakness, not ours.
