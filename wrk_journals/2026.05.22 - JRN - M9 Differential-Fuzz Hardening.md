# JRN — M9 Differential-Fuzz Hardening

**Started:** 2026-05-22
**Role:** Supervisor / team-lead. Legwork delegated to agents; I tie-break, verify, serialise commits.
**Spec:** `wrk_docs/2026.05.22 - HLD - mdrcel M9 Differential-Fuzz Hardening.md` (V4, ratified)
**Arthur's ruling:** HARDEN FIRST ("build from a strong base … far cheaper to test with our oracles in
this repo than once we're integrated into the consumer"). This satisfies HLD §14 step 2 → run Stage 0,
honour its go/no-go.

## Goal (this loop)
Complete all M9 stages sequentially & methodically; iterate each to 100% before advancing.
Stage gate order (HLD §6): **Stage 0 (falsification spike)** → on GO → Stage 1 (shared-helpers
refactor; land early as M8 seam) → Stage 2 (batch oracle) → Stage 3 (harvester) → Stage 4 (diff/group)
→ Stage 5 (triage + fix/promote, measured by §8 KPI).

Supervisor refinement (recorded in chat, accepted): **Stage 1 graduates to "do anyway"** — it is pure
behaviour-preserving de-dup and the M8 shared seam, so worth landing regardless of Stage-0 outcome.
Everything else stays gated on a Stage-0 GO.

## Pre-registered Stage-0 thresholds (fixed BEFORE looking at results)
- NO-GO iff fewer than **X = 3** distinct *new-class* root-cause loci across the sample.
- GO iff ≥ 3 new-class loci, OR any single new-class locus recurs across ≥ **Y = 5** pages.
- Known clusters (U+2063/Cf, blake2b fingerprint, TEI header) are DISCOUNTED before counting.
- Defaults per HLD §6.0; Arthur did not override at ratification.

## Defaults locked for this run (Arthur green-lit, no objection)
- WARC slice: latest monthly crawl, arbitrary segment.
- `warcio` pinned into the existing oracle venv.
- Promotion cap: none (minimised + legitimate-content only); revisit only if gate slows.

---

## Log

### 2026-05-22 — Session start
- M8 confirmed landed on `main` (HEAD `9b8e059`, version 0.18.0, teiHeader unmasked, gate mask
  narrowed to fingerprint-only). Metadata-coordination risk for M9 is therefore retired.
- Read the ratified HLD in full to ground the stage spec.
- Committed M9 HLD lineage + journal (`272c2b7`).
- Recon GREEN: oracle venv present (`.venv/Scripts/python.exe`), `run.py` intact, Common Crawl
  reachable (HTTP 200 from `data.commoncrawl.org`, LHR3 edge).
- Learned invocation surface: mdrcel `extract_to_{txt,markdown,xml}(html, base_url, &Options)`;
  oracle `python run.py <abs.html> --txt|--markdown|--xml [--base-url URL]`; NFC both sides.

### 2026-05-22 — Stage 0 spike (delegated to agent `a295edd7389e6b28f`) → **GO**
Agent harvested **300** pages from CC-MAIN-2026-17 (one streamed WARC, 329 records scanned), ran the
oracle (batch script verified byte-identical to run.py) vs mdrcel in txt/markdown/xml, masks OFF,
NFC-compared. Artifacts under `benchmark/fuzz/stage0/` (throwaway, uncommitted); report at
`benchmark/fuzz/stage0/STAGE0_REPORT.md`.

Headline (NFC, masks off): txt 209/300 equal, markdown 199/300, xml(fp-masked) 214/300.

**Supervisor classification (anti-inversion applied):**
- Known classes discounted: Class 1 Cf-leak (U+00AD/U+FEFF, 15 pp), Class 2 fingerprint (xml, 297 pp),
  Class 3 TEI (N/A).
- **Family B (Cc control-byte leak, 39 pp) folded INTO the known class** — Python's
  `remove_control_characters` (`isprintable or isspace`) strips BOTH Cf and Cc; same unported function,
  same root cause as the known deferred defect. NOT counted as new-class (conservative).
- **New-class loci (after discount):** A) htmldate **PANIC** (28 pp) — confirmed at
  `src/htmldate/extractors.rs:304`, byte-slice `string[..4]` lands mid-char on CJK date prefixes;
  C) extraction-shape (38 pp) — distinct layers: selection (table-vs-`<p>` top-candidate), table-walk
  (cell grouping), `<lb/>` spacing, trailing-space serialization; D) whitespace placement (26 pp).

**Pre-registered gate:** NO-GO iff <3 new-class loci; GO iff ≥3 OR any recurs ≥5. → **≥3 distinct
new-class loci AND recurrence 26–38 ≫ 5. DECISIVE GO.** Premise validated: real-world pages surface
new fidelity-bug classes (panic, selection, serialization) well beyond the three known.

### 2026-05-22 — Stage-0 follow-up: fix the htmldate char-boundary panic (highest-severity find)
Fixed `custom_parse` Block 1 (`extractors.rs:304`): replaced `string[..4]`/`string[4..8]` byte-slices
with a code-point char-prefix (`string.chars().take(8)`), mirroring Python's `string[:4]` (code-point
slice). For ASCII input it is byte-identical to the prior form (so the 51 gates are unaffected); for
non-ASCII it falls through exactly as Python's `False` `.isdigit()` does, instead of panicking. Block 2
(regex-matched ASCII digits) and Block 3 (named groups) were already safe. Added 2 regression unit
tests (CJK date prefix; short multi-byte string). All 7 `custom_parse` lib tests pass.

Rationale for fixing now (vs deferring to Stage 5): it is a confirmed, root-caused crash that aborts
ALL extraction (every entry point calls `extract_metadata` → `custom_parse`), so leaving it would force
`catch_unwind` plumbing through the Stage-4 diff harness and lose ~9% of pages. A crash is the
highest-severity bug class; clearing it first de-risks every later stage. M8 has landed, so the §10
metadata-coordination carve-out (don't fight a live M8 over date semantics) is moot.

Validation in flight: full markdown + txt corpus gates (51 fixtures each) running to confirm no ASCII
regression. Next: commit; then Stage 1 (shared-helpers refactor).

### 2026-05-22 — Stage-0 fix landed (commit `4a9421c`)
Both corpus gates green (51/51 markdown + 51/51 txt). Panic fix + 2 regression tests committed.
Stage 0 closed; M9 cleared to Stage 1.

### 2026-05-22/23 — Stage 1 (delegated to agent `a14212e2e563e4307`) → green after TEI flake retire
**Scope decision** (deviation from HLD §5.1, recorded): the HLD proposed a single
`normalize(output, format)` folding masking; M8 had since reshaped the masking landscape (TEI mask
narrowed to fingerprint-only; teiHeader unmasked), and the masking is a 2-input, assertion-bearing
transform that doesn't fit a one-arg `normalize`. Per "don't overengineer / smallest change," scoped
Stage 1 to extract ONLY the verbatim-shared format-agnostic helpers; all format-specific masking stays
in its own gate, untouched. This also makes the refactor safe-by-construction (no new masking → no
false-green path).

**Extracted into `tests/common/mod.rs`:** `Bucket` enum + `label()` + `classify`, `collapse_ws`,
`first_diff_index`, `window_around`, `escape`, `workspace_path`, the unified `run_oracle(format_flag,
snapshot_path)` (replaces six `python_<fmt>` fns differing only in the flag string), `nfc(s)`, and the
five shared self-tests. All confirmed byte-for-byte identical across gates before unification. Format-
specific masking (`neutralise_fingerprint`, `neutralise_header`, `mask_fingerprint_note`, csv-column
masking and their self-tests) stayed in their gates verbatim. 6 gates refactored to `mod common;`,
−927/+48 lines.

**Validation:** 5/6 gates green on the agent's first pass (csv 15/15 pass, json 6/6, markdown 6/6, txt
6/6, xml 14/14). The 6th (tei) failed with 38 1-byte diffs on `<date type="download">2026-05-22` vs
`2026-05-23`. Diagnosed via vendored source: mdrcel's `today_utc()` (`metadata.rs:872`) disagrees with
Python's `datetime.now().date()` (local) during the ~1h UTC-23:00–00:00 window in BST. M8 had
explicitly accepted this as a residual flake (`metadata.rs:866-871`). Re-ran TEI post-window: 14/14
pass, `GREEN 51 = 38 substantive + 11 allowlist + 2 deferred`. Refactor is genuinely
behavior-preserving.

**Known M9 residual recorded:** the today_utc-vs-today_local timezone gap is a real fidelity defect,
not present in M9 substantive scope but worth flagging for Stage-5 triage consideration. Fixing
cleanly needs FFI or a new dep (chrono isn't currently in `Cargo.toml`); M8's choice to keep std-only
+ accept the flake is the documented status. Not addressed in Stage 1.

Next: commit; Stage 2 (batch oracle + cache integrity).

### 2026-05-23 — Stage 1 committed (`83c49c8`) → Stage 2 (delegated to agent `adb0d814282b80ee9`)
**Agent delivered run.py refactor (+306/-77 lines):**
- Single shared `run_oracle(raw, base_url, output_format)` at lines 281-308. Single place builds the
  extract() kwargs (`with_metadata=False, deduplicate=False, include_comments=False, config=cfg`) — the
  HLD §5.3 anti-drift invariant.
- `_locked_kwargs_descriptor` (lines 234-252) hashes the boolean posture PLUS the SHA-256 of
  `trafilatura.cfg` — better than the literal HLD ask (which only specified the kwargs hash). Now any
  cfg edit invalidates the cache.
- `--batch <corpus_dir> <manifest>` mode (`_run_batch`, line 333). Imports trafilatura once, loops
  manifest, emits header + per-doc JSONL.
- `_BATCH_OUTPUT_FORMATS = (txt, markdown, xml)` — matches HLD's "start with txt+markdown; widen if
  Stage 0 shows signal." Stage 0 showed xml signal, so xml included. xmltei/json/csv excluded (xmltei
  carries the BST residual; json/csv are metadata-domain).
- `with_metadata=True` xmltei forcing: agent verified (and existing run.py already documented) that
  trafilatura's `Extractor.__init__` (settings.py:144-149) auto-promotes with_metadata for xmltei, so
  the explicit override the HLD asked for is redundant. Existing TEI gate green proves equivalence.

**Validation (supervisor took over from agent's incomplete report):**
- **Batch ≡ per-doc equivalence (9/9 byte-equal):** alphabetical first/middle/last fixtures × txt /
  markdown / xml. Sizes 112b–79513b, all exact. `--batch`'s outputs are byte-identical to per-doc
  mode's outputs.
- **Cache-integrity header present** with all 4 keys: `traf_version`, `cfg_sha`, `run_py_sha`,
  `manifest_sha`. All non-empty hex.
- **6 format gates** running in background (~20 min) to confirm the shared-fn refactor preserved
  per-doc behavior across all 51 fixtures × 6 formats.

Synthetic 51-manifest under `benchmark/fuzz/stage2_validation/` (51 lines, source_url=null) +
`batch_out.jsonl` (52 lines = 1 header + 51 docs) — throwaway, will NOT be committed.

### 2026-05-23 — Stage 2 committed (`841d9b3`) → Stage 3 (agent `a59f61fd5a5fbe114`)
**Stage 3 delivered:**
- `benchmark/fuzz/harvest.py` (387 lines): canonical, CLI-driven, FROZEN-filter WARC streamer.
  Module-level `_FILTER_FROZEN_AT = "M9 Stage 3 — 2026-05-23"` + docstring NOTE that changing the
  filter re-baselines the §8 KPI. CLI: `--crawl-id / --warc-index / --target-count / --output-dir
  / --manifest / --verbose`. Streams via `requests(stream=True)` + `resp.raw.decode_content=False`;
  stops at target_count (only tens of MB pulled, not the full 1GB WARC).
- **Working slice**: 1500 pages from `CC-MAIN-2026-17` segment 1775805908305.14 WARC 00000.
  Scanned 1626 records, kept 1500 (126 dropped by filter).
- **Manifest schema verified** matches HLD §5.2 exactly: 5 keys
  `{sha, source_url, warc_path, warc_offset, length}` with types `(str, str, str, int, int)`.
- **`sha` = sha256(file-on-disk UTF-8 bytes)** (agent upgrade from Stage 0's sha-of-WARC-payload):
  makes the corpus self-verifying. Round-trip full-corpus sweep: **1500/1500 length match + 1500/1500
  sha256 match**.
- **Stratification (observational)**: non-English 35.6% (above ~30% aim), table-heavy 28.6% (above
  ~20% aim). Distribution skews toward bug-rich shapes — good.
- `.gitignore` updated (3 new entries + comment block): `benchmark/fuzz_corpus/`,
  `benchmark/fuzz/oracle_cache.jsonl`, `benchmark/fuzz/divergences.jsonl`. `manifest.jsonl` stays
  committed (reproduces the corpus from the WARC offsets without storing the bulk).
- **Overlap note**: Stage 3's slice probably contains all 300 Stage-0 spike pages (same WARC, both
  iterators start from the beginning) — fine for the working slice; future held-out slice should use
  `--warc-index=1` for independence.

Next: commit; Stage 4 (diff/classify/group harness).

### 2026-05-23 — Stage 3 committed (`596e6b4`) → Stage 4 (agent `a622c85bbac60e346` + supervisor follow-up)
**Agent built `tests/fuzz_diff.rs` (~1025 lines)** with cache-integrity check, sha2 dev-dep + version
bump 0.18.0 → 0.18.1. Verifies header (traf_version + cfg_sha + run_py_sha + manifest_sha) before
reading entries. Diff window + shape classification + `diff_shape_hash` (K=1 dedup, HLD §5.5) +
read-time grouping. Two stray C0 control bytes in a comment block (literal NUL + US, not `\x00`/`\x1f`
escapes) — cleaned to escape literals; clean compile preserved.

**Supervisor follow-up — run.py hardening (necessary prerequisite):** First batch oracle run died at
doc 25/1500 with no stderr (lxml C-extension on a malformed-namespace page, hard crash). Two issues:
(1) `use_config()` called per-extract → ~20s/doc effective overhead at the 4500-call scale (would
take ~8h); (2) no per-doc error handling. Lifted `_CACHED_EXTRACT` + `_CACHED_CFG` to module level
(initialise once, reuse), wrapped each `run_oracle` call in try/except with stderr logging and a
`__TRAFILATURA_ORACLE_ERROR__` sentinel string the harness can recognise, added per-100-doc progress
logging. Rerun: **1500/1500 docs in ~25 min, 1 per-format error** (`084e1677…` xml — the original
killer page, now recorded as sentinel).

**Mandatory mask fix (HLD §1/§3 "the pipeline masks known classes"):** initial fuzz run showed xml
0/1500 equal — every doc diverged on the blake2b-fingerprint attribute (mdrcel emits bare `<doc>`,
Python emits `<doc fingerprint="…">`). Per HLD this is one of the three known-deferred classes and
the harness MUST mask it. Added `apply_known_masks(s, format) -> String` + `strip_xml_fingerprint`
(pure-strip, no shape assertion; the xml gate already enforces shape on the 51 corpus). Applied
before byte-compare. (Cf and TEI masks NOT applied — TEI isn't a format we diff; Cf surfaces
naturally and clusters on its own `diff_shape_hash` for Stage 5 triage.)

**Post-mask harness result (the actual Stage 4 deliverable, 1500-page CC slice):**
| format | pages | equal | divergent |
|---|---:|---:|---:|
| markdown | 1500 | 1079 | 421 |
| txt | 1500 | 1139 | 361 |
| xml | 1500 | 1045 | 455 |

- **Total divergences: 1237 (was 2282 pre-mask).**
- **43 distinct (fmt, shape_class, near_tag) groups** (was 14 pre-mask — the mask uncovered real
  structural diversity that fingerprint noise was hiding).
- **1065 distinct `diff_shape_hash` values** — the K=1 triage units Stage 5 will process per HLD §5.5.

Top groups (decision-useful signal): markdown/text-content 361 (Family C-like selection /
serialization), txt/text-content 320 (same), xml/extra-element/doc 71, xml/text-content/{doc,p,lb} 69
+ 68 + 29, xml/missing-element/doc 41, markdown/whitespace 36. Each row reports both member count
and distinct-hash count — distinct=members in most groups confirms the K=1 split is naturally there
(every member is its own shape).

Artifacts (uncommitted, regenerated per run, gitignored): `benchmark/fuzz/oracle_cache.jsonl` (1501
lines = header + 1500), `benchmark/fuzz/divergences.jsonl` (1237 lines). Harness runtime ~15 min.

Next: commit; Stage 5 (triage + guarded fix/promote loop), held-out slice for KPI baseline.

### 2026-05-23 — Stage 4 committed (`d6af5de`) → Stage 5 (supervisor + agent `a7118896889502606`)

**Slice partition (supervisor decision, frozen pre-Stage-5).** Rather than harvest a separate
held-out slice (would have required new files + env-var path plumbing), partitioned the existing
1500-page manifest by index: **first 1000 = working slice (triage target); last 500 = held-out
(KPI substrate, never used for triage)**. Same CC distribution; partition frozen at Stage 5 entry
per HLD §8 anti-gaming rule. Modified `tests/fuzz_diff.rs`: added `const WORKING_SLICE_SIZE = 1000`,
split `per_format` stats into `per_format_working` + `per_format_heldout`, gated divergence-recording
on `is_working` (held-out is "look but don't peek"). Clean compile; verified gates not re-run since
the change is internal to the fuzz harness only.

**Baseline KPI (FROZEN, the pre-Stage-5 substrate):**
| Format   | Pages | Equal | Diverge | KPI(%eq) |
|----------|------:|------:|--------:|---------:|
| markdown |   500 |   345 |     155 |   69.00% |
| txt      |   500 |   370 |     130 |   74.00% |
| xml      |   500 |   335 |     165 |   67.00% |

Working slice: 787 divergences across 37 (fmt, cls, tag) groups; 688 distinct diff_shape_hashes
(K=1 triage units).

**Triage round 1 (agent + supervisor synthesis).** 5 groups inspected (top-3 by count + 2 narrow-
context); 15 representatives analysed; every hypothesis verified against vendored Python source.
Identified **two cross-group root causes**:
- **A — trailing-space-before-inline-tag** (~36 records, ~4.6% of working slice). mdrcel emits
  `text. <lb/>` where Python emits `text.<lb/>`. Hits `xml/text-content/lb` (16/18) +
  `xml/text-content/p` (~20/48). Vendored: `htmlprocessing.py:222-265`, `utils.py:282-300`,
  `xml.py:159-175`. Rust mirrors at `cleaning.rs:1318-1410` and `output.rs:805-957` look
  *algorithmically faithful* on inspection — the bug is therefore EITHER upstream in tree-build
  (`main_extractor.rs:712-864`) OR in the DOM-text-storage layer (rcdom tail-trap territory).
- **B — cell/head text-vs-tail allocation** (~17 records, ~2.2%). `Forest |\n` (oracle) vs
  `Forest\n|\n` (mdrcel). Hits `markdown/whitespace` (~10/21) + `txt/whitespace` (~7/9). Mirror
  at `output.rs:585-603` looks faithful; divergence is likely upstream in
  `convert_tags`/table-handling (cell with `<p>`-child vs leaf cell).

**Allowlist:** none this round (no rep cleared the §7 anti-inversion bar).

**Fix discipline:** the agent explicitly recommended "DO NOT blind-fix" — both root causes have
MEDIUM-confidence diagnoses (pattern is clear, fix locus is not). The 51-fixture gates are
byte-equal-strict; a wrong patch breaks the established corpus. Supervisor honoured the
recommendation — **no fix applied this round**. The disciplined next step is a targeted empirical
reproduction (dump mdrcel's DOM tree state vs Python's lxml tree at the four pipeline stages on
one of the 18 xml/lb fixtures) to localise the bug site. That is its own focused investigation
and is deferred to a future session.

**Stage 5 status: PARTIAL — loop demonstrated end-to-end, baseline locked, triage round complete,
fix-and-measure rounds deferred per anti-inversion.** Per HLD §8 honest-out clause this is a
legitimate outcome — the loop runs, the data is sound, the disciplined finding is two
high-confidence-pattern / medium-confidence-locus root causes with cited vendored source. KPI
delta this round: zero (no fix applied). Full triage report at
`wrk_docs/2026.05.23 - REPORT - M9 Stage 5 round 1 triage.md`.

**M9 SUMMARY (Stages 0–5):**
- Stage 0 (GO) committed `4a9421c`. Decisive go: ≥3 distinct new-class loci, recurrence 26–38 ≫ Y=5.
  Surfaced + fixed an htmldate char-boundary panic (CJK date prefixes) — the highest-severity find.
- Stage 1 committed `83c49c8`. tests/common/mod.rs shared helpers; −657 net lines de-dup.
- Stage 2 committed `841d9b3`. Shared config-locked oracle fn + --batch + cache-integrity header.
- Stage 3 committed `596e6b4`. FROZEN-filter CC harvester + 1500-page slice + manifest.
- Stage 4 committed `d6af5de`. Diff/classify/group harness + run.py hardening (cached cfg + per-doc
  error handling). Post-mask fuzz: 1237 divergences, 1065 K=1 units, real signal isolated.
- Stage 5 (partial) — this commit. Slice partition + baseline KPI + triage round 1 report.

M9 delivered the apparatus and one disciplined round; the apparatus is sound and re-runnable. Future
sessions can pick up at the "Step A (reproduction)" recommendation in the triage report to land
fixes and chase KPI plateau.
