# M4 forward push — /goal loop journal

**Started:** 2026-05-21 (Arthur off to bed after M3 finale)
**Loop type:** /goal — supervisor + agent team. Aim: drive as far forward
into M4 as possible while protecting Claude's context.
**Approval:** Arthur picked "Full M4 umbrella" via AskUserQuestion.

## Scope (full M4 umbrella)

Stage ordering decided up-front; can re-order if dependencies surface:

0. **Push 21 unpushed commits** — *NO-OP, HEAD == origin/main already*.
1. **htmldate port** — biggest faithfulness debt (~3000 LOC + dateparser).
   1a. Scoping agent: map htmldate.py + dateparser into Rust stages.
   1b–1n. Cited port, one sub-module per dispatch.
2. **Relative-URL resolution** — wire courlan-style URL join into `extract()`.
3. **Output formats** — XML / XMLTEI / CSV / JSON / markdown.
4. **Metadata → Extracted mapping** — categories, tags, image, pagetype,
   license, hostname on public `Extracted` struct.
5. **focus=precision pre-clean of backup_tree** — deferred from M3
   Stage 6; needs Document::from_node.
6. **Simhash + content fingerprint + is_similar_domain**.
7. **extract_json_parse_error regex-rescue path**.

## Baseline (commit `3f4f4b7`)

- Lib tests: **761 passing**
- BLOCKER gates: **4 / 8 / 5 / 1 = 18 passing**
  - parser_equivalence_gate: 4/4
  - trafilatura_equivalence_gate: 8/8
  - trafilatura_extract_content_gate: 5/5
  - trafilatura_extract_smoke: 1/1
- Cargo: 0.10.0
- Working tree clean apart from `.claude/`.
- 0 commits ahead of origin/main (M3 was pushed).

## Supervisor doctrine (carried from M3)

- **Anti-inversion**: any test that "inverts Python" is wrong; agents
  may correct briefs that mis-encode Python behaviour.
- **Line-cite everything**: every port must cite the Python source
  line(s) it ports.
- **Keep dispatches one level deep**: agents may not spawn sub-agents.
- **Pipe minimal context**: send the brief, not the whole codebase.
- **Atomic commits**: one logical stage per commit, gate-green or
  reverted.
- **Journal after every iteration** before the next dispatch — protects
  my context.
- **Never throw away or rewrite without explicit permission** — apply
  smallest-change principle.

## Iteration log

### Iter 1 — htmldate scoping (agent `a0c45722c3d55fc36`)

**Deliverable:** stage plan for M4 Stage 1 (htmldate port).

**Key findings:**
- htmldate 1.9.4 installed at `C:\Users\marti\AppData\Roaming\Python\Python314\site-packages\htmldate\` (8 .py files, 2068 LOC in-scope excluding `cli.py`).
- `find_date()` call graph fully mapped to leaf `try_date_expr`.
- **dateparser deferral**: agent recommends option (c) — port `regex_parse` + month tables only, stub `external_date_parser` returning `None`. Anti-inversion clean; minor coverage hit on non-ISO free-text dates. **Supervisor agrees.**
- **regex catalogues**: ~95% verbatim port (~165 LOC Python → ~200 LOC Rust). One `(?<!w3.org)` lookbehind needs 3-line post-filter (Rust `regex` crate has no lookarounds).
- **Total estimate**: ~2030 Rust LOC, ~110 unit tests, 8 sub-stages (A–H).

**Sub-stages:**
| ID | Source | Rust LOC | Tests | Deps |
|----|--------|----------|-------|------|
| A | settings.py + utils.py::{Extractor,trim_text} | ~80 | 6 | — |
| B | validators.py | ~250 | 18 | A |
| C | extractors.py regex catalogues | ~220 | 25 | — |
| D | extractors.py non-dateparser layer | ~450 | 22 | A,B,C |
| E | core.py header + element walkers | ~550 | 18 | A–D |
| F | core.py::search_page cascade | ~280 | 12 | A–E |
| G | core.py::find_date entrypoint + wire into metadata_url.rs | ~200 | 8 | A–F |
| H | corpus parity gate | 0 | — | G |

**Supervisor decisions:**
- Module location: new `src/htmldate/` sibling to `src/trafilatura/`.
- No mirror to `benchmark/python_refs/`: cite site-packages directly. Stable enough for sandbox use.
- Dispatch order: serial A → C in parallel? No — A sets up module skeleton; do A first, then can run C in parallel with B.

### Iter 2 — M4-S1-A landed (`b19fcee`)

- Agent: `a543e28777ba30585`. ~290 LOC + 10 unit tests.
- Anti-inversion correction: brief named 7 Extractor fields; Python has 5 (`__slots__`). Python wins.
- `chrono` not a dep → MIN_DATE as `(i32, u32, u32)` tuple.
- Cargo 0.10.0 → 0.11.0 MINOR (new module surface).
- Lib tests 761 → 771.

### Iter 3 — M4-S1-B landed (`b04fb1e`)

- Agent: `a4f4cbd6bcde82e66`. 900 LOC code + 545 comments (35% comment density = strong line-cite discipline).
- All 11 Python public functions ported with citations.
- Built `DateTime` struct + `DateInput` enum + minimal `strptime`-style format parser + Howard Hinnant `civil_from_days` algorithm — all without adding `chrono` dep.
- Anti-inversion: brief claimed `compare_values` returns `Tuple[int, datetime]`; Python returns `int`. Python wins.
- `get_max_date(None)`: Python uses `datetime.now()`; Rust returns 9999-12-31 sentinel + `get_max_date_with(opt, now)` for injection. Documented divergence.
- `@lru_cache` ignored (perf-only).
- Cargo 0.11.0 → 0.11.1 PATCH.
- Lib tests 771 → 813 (+42, vs 18 minimum).

### Iter 4 — M4-S1-C landed (`d8d0f41`)

- Agent: `a0967d621a9bcb528`. 1113 LOC.
- 32 `OnceLock<Regex>` accessors + 1 HashMap + 8 string/array constants.
- Three Rust-regex divergences:
  - SIMPLE_PATTERN `(?<!w3.org)` lookbehind → strip + emit `simple_pattern_post_filter(haystack, start) -> bool` helper.
  - `{,20}` shorthand → `{0,20}` (Rust regex requires explicit lower bound).
  - `match.lastgroup` → deferred to sub-stage D (`// TODO sub-stage D: lastgroup helper`).
- Agent's report said "789 → 881"; real baseline was 813. +68 actual delta. Still 2.7× the 25-minimum.
- Cargo 0.11.1 → 0.11.2 PATCH.
- Lib tests 813 → 881.

### Iter 5 — M4-S1-D landed (`0a1e5ee`)

- Agent: `a149eff586c504fac`. 841 LOC.
- All 12 Python functions ported (216-508). `last_named_group` helper landed (S1-C TODO closed).
- `external_date_parser` STUB returns None (dateparser deferred per scoping).
- `dateutil.parser.parse(fuzzy=False)` substitute: explicit 8-format list using B's `format_parse`.
- `datetime.fromisoformat` substitute: `try_fromisoformat` handles common shapes.
- Anti-inversion: brief double-listed line range 257-283; agent verified actual ranges (252-254 = try_swap_values; 257-283 = regex_parse).
- Cargo 0.11.2 → 0.11.3 PATCH.
- Lib tests 881 → 916 (+35).

### Iter 6 — M4-S1-E landed (`35f3d27`)

- Agent: `a17f7475b4b4dbc2d`. 1280 LOC (840 production + 440 tests).
- All constants + 9 walker functions ported (core.py:80-571).
- DATE_ATTRIBUTES = 65 entries, pre-lowercased.
- XPath engine gaps: **NONE**. Every path within existing operator catalog.
- Anti-inversion: `Counter` insertion-order tie-break (CPython) ≠ HashMap; Rust tie-breaks by (count DESC, key ASC).
- Cargo 0.11.3 → 0.11.4 PATCH.
- Lib tests 916 → 942 (+26).

### Iter 7 — M4-S1-F landed (`56bf5fa`)

- Agent: `a9cf27a5765153a0b`. 560 LOC.
- search_page 10-arm cascade ported verbatim from core.py:574-805.
- Anti-inversion: brief mis-described THREE_COMP_REGEX_A/B as scanner arms; Python uses them as normalisers inside SELECT_YMD and SLASHES arms. Python wins.
- SIMPLE_PATTERN lookbehind manually rebuilt via captures_iter + simple_pattern_post_filter + inline tally.
- Cargo 0.11.4 → 0.11.5 PATCH.
- Lib tests 942 → 955 (+13).

### Iter 8 — M4-S1-G landed (`3f44874`) — INTEGRATION MOMENT

- Agent: `a2b85c35205b33350`. 380 LOC production + 70 LOC tests.
- `find_date` orchestrator ported (core.py:861-983), 11-step control flow.
- `clean_html` ported (utils.py:249-255).
- `extract_date` rewritten to drive the full htmldate pipeline.
- Test `extract_date_returns_none_for_pure_text_date` FLIPPED → `extract_date_from_pure_text_date_works_with_htmldate` returning Some("2024-01-15"). 3 other positive tests remain green.
- **All 4 BLOCKER gates remain green** (no fixture flips — find_date wires into metadata, not body extraction).
- Cargo 0.11.5 → **0.12.0 MINOR** (visible behaviour change).
- Lib tests 955 → 965 (+10).

### Iter 9 — M4-S1-H landed (`3371222`) — STAGE 1 COMPLETE

- Supervisor-direct (no agent dispatch — verification-only).
- Created `tests/htmldate_parity_gate.rs` as **5th BLOCKER gate**.
- Python expected output captured for 10 corpus snapshots via `htmldate==1.9.4`.
- **Result: 10/10 byte-equivalent match** between mdrcel and Python.
- 10 fixtures exercise every major `find_date` path (header, time element, JSON-LD, URL date, free-text regex, search_page cascade).
- No library code touched → no Cargo version bump.

## Stage 1 totals

| | Start | End |
|---|---|---|
| Commits | 0 | 8 |
| Lib tests | 761 | 965 |
| Cargo | 0.10.0 | 0.12.0 |
| BLOCKER gates | 4 | 5 |
| New module | — | `src/htmldate/` (7 .rs files, ~5300 LOC) |

**One dispatch failure: zero.** Every sub-stage A–G landed first-try clean.

**Anti-inversion corrections by agents: 5+** (one per sub-stage: A field count, B return type, C lookbehind+`{,20}`, D line range, E `Counter` tie-break, F cascade arm description, G default-helper API). Agents corrected the brief in every case; Python source always won.

## Iter 10 — M4 Stage 2 landed (`1b8409f`)

- Agent: `ab1cd1b1e2c1d73e4`. ~235 LOC.
- Hand-rolled URL parsing (no `url` crate dep) — anti-inversion-clean for in-tree port.
- Ports: `courlan/urlutils.py:110-123` (fix_relative_urls), `htmlprocessing.py:369-378` (convert_link), `urlutils.py:76-84` (get_base_url widened to pub(crate)).
- Wiring: `cleaning.rs::convert_tags` `options.links=true` branch (was STUB) now drives the full pipeline.
- Anti-inversion: brief described protocol-relative `//host/x` as "inherit baseurl scheme"; Python uses LITERAL "http". Python wins.
- Cargo 0.12.0 → 0.12.1 PATCH.
- Lib tests 965 → 975 (+10).

## Stage 2 totals
| | Start | End |
|---|---|---|
| Lib tests | 965 | 975 |
| Cargo | 0.12.0 | 0.12.1 |

## Iter 11 — M4 Stage 3 scoping (agent `a3e7adf7024c96131`)

Recommends: **Option C** (new top-level `extract_to_*` functions) over option B (Options field). 5 sub-stages, ~1040 LOC total.

Risks noted: rcdom Drop quirk; lxml tail-text semantics; NFC normalisation; TEI DTD validation deferred; HTML output already covered by Extracted.html.

## Iter 12 — M4-S3-A landed (`33c6e3b`)

- Agent: `ac11b8529a0679799`. 1240 LOC (600 implementation + 640 tests).
- 7 shared helpers + Document struct + 6 constants ported from xml.py.
- Anti-inversion catches: head defaults to h2 (not h1) per Python's `int(...)[1]` quirk; `<ref>` markdown wrapper runs regardless of include_formatting; merge_with_parent space-prefix branches both pinned; remove_empty_elements single-iter quirk; strip_double_tags reverse-iter required.
- Brief vs source: clean_attributes whitelists by TAG (WITH_ATTRIBUTES set), not by attr name.
- Cargo 0.12.1 → 0.12.2 PATCH.
- Lib tests 975 → 1017 (+42).

## Iter 13 — M4-S3-B landed (`0066db1`) — FIRST PUBLIC OUTPUT FORMAT

- Agent: `a48fe12380fafde30`. ~620 LOC.
- `pub fn extract_to_markdown` lands.
- `Options::with_metadata: bool` (additive, default false).
- Promoted unicode-normalization from dev-dep to regular dep.
- Anti-inversion: initial UTF-8 byte iteration corrupted multi-byte chars (NFC test caught it); tag-list "split on comma" was wrong (Python normalize_tags doesn't split).
- Cargo 0.12.2 → 0.13.0 MINOR (first new public function on output surface).
- Lib tests 1017 → 1033 (+16).

## Iter 14 — M4-S3-C landed (`ed40ae8`)

- Agent: `a7992e797a81b696a`. ~676 LOC.
- `pub fn extract_to_json` + `pub fn extract_to_csv` lands.
- JSON: 19-key insertion order preserved (serde_json::Map is BTreeMap — would invert).
- CSV: 11 columns, tab-delim, \r\n line term, QUOTE_MINIMAL.
- Anti-inversion: brief recommended (a) always emit metadata; Python uses opts.with_metadata flag (option b). Python wins.
- Anti-inversion: categories/tags in JSON are JOINED STRINGS (Python `';'.join(...)`), not arrays.
- Cargo 0.13.0 → 0.13.1 PATCH.
- Lib tests 1033 → 1050 (+17).

## Iter 15 — M4-S3-D landed (`043427d`)

- Agent: `a005b4d6a78765b4b`. ~857 LOC.
- `pub fn extract_to_xml` lands.
- Hand-rolled `serialize_xml_pretty` (~80 LOC) — verified against lxml ground truth via `python3 -c`.
- Anti-inversion: META_ATTRIBUTES order is NOT alphabetical (Python source order matters); commentsbody=None synthesises empty `<comments/>`; lxml escapes > in attrs (some libs don't).
- Cargo 0.13.1 → 0.13.2 PATCH.
- Lib tests 1050 → 1073 (+23).

## Iter 16 — M4-S3-E landed (`201a0f6`) — STAGE 3 COMPLETE

- Agent: `a0533c84d5c47317e`. ~1500 LOC (the biggest sub-stage of Stage 3).
- `pub fn extract_to_tei` lands.
- 6 TEI helpers + write_fullheader + write_teitree + check_tei (3-pass walker) + build_tei_output.
- OutputFormat enum dispatch in control_xml_output (replaces 3-D's TODO).
- `restore_tei_case` post-processor (rcdom lowercases HTML tags; TEI needs camelCase).
- Anti-inversion: `_define_publisher_string` combines sitename + hostname (brief said "prefer sitename"); `_move_element_one_level_up` only inserts when non-empty.
- Deferred: `validate_tei` (no Rust DTD validator).
- Cargo 0.13.2 → 0.13.3 PATCH.
- Lib tests 1073 → 1104 (+31).

## Stage 3 totals
| | Start | End |
|---|---|---|
| Commits | 0 | 5 |
| Lib tests | 975 | 1104 |
| Cargo | 0.12.1 | 0.13.3 |
| Public functions added | 0 | 5 |
| New `Options` fields | 0 | 1 |

## M4 progress so far (Stages 1–3)
| | Start | After Stage 3 |
|---|---|---|
| Commits | 0 | 14 |
| Lib tests | 761 | 1104 (+343) |
| Cargo | 0.10.0 | 0.13.3 |
| BLOCKER gates | 4 | 5 |
| Public functions | 1 (`extract`) | 6 (`extract`, `extract_to_markdown/json/csv/xml/tei`) |

Remaining stages: 4 (Metadata→Extracted mapping), 5 (focus=precision), 6 (Simhash), 7 (json regex rescue).

## Iter 17 — M4 Stage 4 landed (`b3e1d65`)

- Agent: `a17b902a1912b2ded`. ~300 LOC.
- 6 new additive fields on Extracted: categories, tags, image, pagetype, license, hostname. All types match Metadata exactly.
- Three exhaustive struct literals extended (would've broken cargo build otherwise).
- No double-call to extract_metadata — consumed existing binding.
- Cargo 0.13.3 → 0.14.0 MINOR.
- Lib tests 1104 → 1114 (+10).

## Iter 18 — M4 Stage 5 landed (`a1a6f2e`)

- Agent: `a67306bdfde7e6987`. ~242 LOC.
- Wired focus=precision pre-clean: re-parse raw HTML → prune_unwanted_nodes(tree, OVERALL_DISCARD_XPATH) → serialize → Document::new (option b from brief).
- Anti-inversion: `Focus::Default` doesn't exist — it's `Focus::Balanced`. Recall bypass (external.py:49-50) still wins ahead of precision branch.
- Anti-inversion: first test used `<div class="footer">` which readability strips under balanced focus too — switched to `<div class="viral">` for clean differential.
- Cargo 0.14.0 → 0.14.1 PATCH.
- Lib tests 1114 → 1119 (+5).

## Iter 19 — M4 Stage 6 landed (`a1c7961`)

- Agent: `a71ec59563c6b1d07`. ~442 LOC.
- Simhash + content_fingerprint + is_similar_domain ported from deduplication.py:22-143.
- **Zero new deps**. Hash function: hand-rolled FNV-1a 64-bit (Python uses blake2b — anti-inversion divergence honestly documented; Simhash properties hold).
- SequenceMatcher.ratio() hand-rolled (~30 LOC, textbook find_longest_match DP).
- Anti-inversion: STRIP_EXTENSION is single-pass (`www.example.com` → `www.example`, NOT `www`). `example.com` vs `example.org` is TRUE in Python (both strip to "example" → ratio 1.0).
- Cargo 0.14.1 → 0.15.0 MINOR (new public types).
- Lib tests 1119 → 1139 (+20).

## Iter 20 — M4 Stage 7 landed (`4fd6ea3`) — M4 COMPLETE

- Agent: `a16ef157469e6cc15`. ~424 LOC.
- Closed deferred regex-rescue at metadata_jsonld.rs:182-184. 10 regex constants ported (JSON_AUTHOR_REMOVE / _1 / _2, JSON_PUBLISHER / _TYPE / _CATEGORY, JSON_NAME, JSON_HEADLINE, JSON_REMOVE_HTML, JSON_UNICODE_REPLACE).
- Wiring: `Err(_) => extract_json_parse_error(&raw_text, metadata)` replaces silent skip.
- Anti-inversion: Python's extract_json_author has a latent crash on `mymatch[1]` when group 1 is None (JSON_AUTHOR_1 has 2 alternations). Faithful Rust prefers group 1, falls back to group 2.
- Anti-inversion: `normalize_json` lone-surrogate strip (0xD800..0xDFFF) so `\uD800` escapes don't produce invalid Rust char.
- Module header faithful-divergence note flipped from "skipped" → "ported".
- Cargo 0.15.0 → 0.15.1 PATCH.
- Lib tests 1139 → 1149 (+10).

# M4 GRAND TOTALS

| | M3 finale (start) | M4 finale |
|---|---|---|
| Commits this session | — | **18** |
| Lib tests | 761 | **1149** (+388) |
| Cargo version | 0.10.0 | **0.15.1** (1 MAJOR-equivalent + 4 MINORs + 12 PATCHes) |
| BLOCKER gates | 4 | **5** (+htmldate_parity_gate) |
| Public `extract*` functions | 1 (`extract`) | **6** (`extract`, `extract_to_markdown/json/csv/xml/tei`) |
| `Options` fields | 2 | **3** (+with_metadata) |
| `Extracted` fields | 12 | **18** (+6: categories, tags, image, pagetype, license, hostname) |
| New modules | — | `src/htmldate/` (7 .rs files, ~5300 LOC) |
| New gate files | — | 1 (htmldate_parity_gate) |

## Per-stage summary

| Stage | Sub-stages | Commits | Lib tests |
|---|---|---|---|
| 1: htmldate port | 8 (A–H) | 8 | 761 → 965 |
| 2: relative URLs | 1 | 1 | 965 → 975 |
| 3: output formats | 5 (A–E) | 5 | 975 → 1104 |
| 4: Metadata → Extracted | 1 | 1 | 1104 → 1114 |
| 5: focus=precision | 1 | 1 | 1114 → 1119 |
| 6: Simhash + fingerprint | 1 | 1 | 1119 → 1139 |
| 7: json regex rescue | 1 | 1 | 1139 → 1149 |

## Agent dispatch record

- **Iterations: 20** (1 scoping for Stage 1, 8 sub-stages, then iter 9 supervisor-direct for parity gate, then 10–20 = 11 more)
- **Agent dispatch failures: 0**
- **Supervisor-direct iterations: 1** (Stage 1-H parity gate spot-check + gate file creation)
- **Anti-inversion corrections by agents: ~20+** — every sub-stage caught something my brief got wrong; Python source always won.

## Key supervisor decisions

1. **Stage 1 dateparser deferral**: scoping agent recommended option (c) — port `regex_parse` + month tables only, stub `external_date_parser`. Supervisor agreed. ~10k LOC of dateparser saved; coverage hit minor.
2. **Stage 2 hand-rolled URL parsing**: no `url` crate dep — anti-inversion-clean for in-tree port.
3. **Stage 3 surface choice**: Option C (new `extract_to_*` functions) over Option B (Options field). Preserves `Extracted` byte-identical semantics.
4. **Stage 6 hash function**: hand-rolled FNV-1a 64-bit over Python's blake2b — saves a sha2/blake2 crate dep.
5. **Stage 5 wiring option**: serialize-then-reparse (b) over `Document::new` API widening (a).

## State for next session

- Working tree clean apart from `.claude/` (untracked) and the journal (untracked, ready to commit).
- 18 commits ahead of origin/main (M4 not pushed per CLAUDE.md no-push-without-permission).
- Suggested next steps for Arthur:
  1. Push M4 to origin.
  2. Decide M5 scope. Candidates: corpus-wide differential gate (extract output vs Python byte-equivalence at scale), property-based testing harness, performance pass (mdrcel is currently functional but unprofiled).
- M4 left no deferred work beyond what the scoping report explicitly tagged (dateparser, sanitize_tree NFC pass deferred for Stage 3-D-E, TEI DTD validation).




