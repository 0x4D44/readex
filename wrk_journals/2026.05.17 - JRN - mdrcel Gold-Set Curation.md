# JRN — mdrcel Gold-Set Curation (Phase-2 → §2.7 freeze → M2 prep)

Supervisor journal. Continues from `2026.05.17 - JRN - mdrcel Harness Implementation.md`
(Phase-2 closeout: instrument live + meaningful; commits `c561148` Stage C,
`611e0a9` §9 re-baseline, `700aecd` journal). Branch `feat/oracles-and-corpus`,
NOT merged/pushed (no Arthur request).

## Mandate / autonomy
Arthur directed **gold-set first, with him**; then went to bed (2026-05-17)
with: "continue to push on; make sensible decisions to avoid getting blocked;
maintain a journal; save regularly to wrk_journals/". So: I act as the
decision-maker in his stead, **document every judgement call** for his review,
and keep the integrity gates (anti-inversion, code-reviewer, devil's-advocate,
per-stage discipline) NON-negotiable. The gold-set freeze (§2.7) and M2 were
the two Arthur-gated items; he has explicitly authorised me to proceed on gold
unblocked. M2 (extraction algorithm) is still DOWNSTREAM of the full gold
freeze and is NOT started here.

## Locked decisions (Arthur, via AskUserQuestion before bed)
- **DEC-G1 — Gold truth method:** *scoped excerpt* for the hard table-heavy
  cases (EDGAR/regulator) — bounded, human-verifiable correct extraction +
  word bands + why_critical. Wikipedia/example.com = full adjudicated body
  (later refined, see DEC-G3). HLD §7 already permits characterisation-style
  gold (e.g. hub = "near-empty body").
- **DEC-G2 — Freeze scope/sequencing:** *lean-4 first* (2 Wikipedia +
  example.com + 1 hub) → prove the gate goes substantive / de-risk workflow →
  then the 3 hard EDGAR/regulator entries as tranche-2, ALL before any M2
  crate tuning (§2.7 only requires all gold committed pre-tuning; phasing
  within that is fine).
- **DEC-G3 — Wikipedia gold = ARTICLE PROSE ONLY** (lead + section prose;
  EXCLUDE references/bibliography, infobox, nav, category bar, footer).
  Decisive because references alone are ~41% of the Apple page; HLD §7 word
  floors are met by prose alone so they did not settle it — Arthur did.
- **DEC-G4 — gov.uk hub gold = MINIMAL GENUINE BODY** (page's real
  lead/headline only; nav-card grid excluded; tight near-empty band). NOT an
  empty string (harness Bug-E2 forbids empty/whitespace gold .txt — hard
  error by design) and NOT a synthesized sentinel — genuine page text.
- **example.com** (supervisor routine call, stated to Arthur, override
  welcome): gold = `<h1>` "Example Domain" + the descriptive sentence;
  exclude the "More information" link.

## Gold mechanism (mapped, Explore agent)
ZERO new code needed — `GoldSet::load()` (`benchmark/src/score.rs`) reads
`benchmark/corpus/gold/gold.tsv` (6 TSV cols: url, snapshot_filename,
expected_text_file, min_words, max_words, why_critical) + per-URL `.txt`.
Absent gold/ = empty "pre-freeze" set; empty/whitespace .txt or malformed row
= hard Bug-E2 error. Hierarchy gold > Trafilatura > Readability. HLD §2.7
freeze = procedural (curate+commit before any crate tuning; post-freeze edits
need written justification). HLD anti-inversion explicit: gold is hand-decided,
NEVER oracle-derived, else "exceeds SOTA" is circular.

## Progress
- **#13 dossiers (agent, done):** neutral exhibits at `wrk_gold/tranche1/`
  (untracked). Oracle text reproduced by deterministic adapter replay (word
  counts match results.json). Surfaced the references-≈41% crux + mojibake in
  oracle output (reinforces: gold ≠ oracle copy).
- **#15 assembly (agent, done — artifact in working tree, NOT committed):**
  `benchmark/corpus/gold/gold.tsv` + 4 `.txt`; +1 test in `score.rs`
  (`committed_gold_set_is_consistent_with_the_real_corpus`). Gold derived
  ONLY from committed snapshot bytes (build script opens only
  `benchmark/corpus/snapshots/<hash>.html`); oracles read ONLY to compute the
  independence exhibit. Snapshots are valid UTF-8, 0 U+FFFD — the `�` is the
  ORACLES' corruption; gold carries correct Unicode (opposite of inversion).

  | # | URL | gold n | band | rule |
  |---|---|--:|---|---|
  | 1 | Apple en.wikipedia | 13556 | 12200–14912 | ⌊0.9n⌋..⌈1.1n⌉ |
  | 2 | Morrison en.wikipedia | 4340 | 3906–4774 | ⌊0.9n⌋..⌈1.1n⌉ |
  | 3 | example.com | 17 | 14–20 | abs n±3 |
  | 4 | gov.uk | 9 | 6–12 | abs n±3 |

  Independence (token-set Jaccard, harness tokenizer): Apple J(gold,Traf)=0.54
  / J(gold,Read)=0.54; Morrison 0.62/0.60; gov.uk 0.05/0.04; example 0.81/0.75.
  gold-only-vs-oracle vocab = 0 on Wikipedia (clean prose subset; the ~1.7k/0.5k
  oracle-only words are exactly the excluded refs/infobox). No suspicious
  closeness (threshold 0.97). End-to-end (stale binary): summary changed
  `no gold (pre-freeze)` → `4 gold`; 4 URLs now reference-based on GOLD;
  still NotScored(CrateNotImplemented) at M1 floor (correct — crate produces
  nothing, so anti-Bug-E2 gate fires before reference comparison).

## Supervisor adjudications made in Arthur's absence (FYI — review when back)
1. **example.com rule-vs-snapshot discrepancy (ENDORSED agent's call).**
   Arthur's rule quoted the OLD IANA sentence; the committed snapshot has the
   NEWER short one ("This domain is for use in documentation examples without
   needing permission. Avoid use in operations."). Agent used the SNAPSHOT
   text. This is correct and the only honest resolution: the immutable
   snapshot is the source of truth; Arthur's intent ("h1 + the descriptive
   sentence, drop the link") is satisfied by the snapshot's actual sentence.
   No blocker. Arthur may re-word rationale; gold text stands.
2. **Wikipedia cut point = first end-matter `<h2>` ("See also" on both).**
   Lead + body sections through the last section before "See also"; in-body
   `Main article:`/`See also:` hatnote cross-refs dropped as boilerplate
   (running prose only). Faithfully implements DEC-G3. ENDORSED.
3. **Infobox fully excluded** (DEC-G3 explicit) — biggest band lever; correct.
4. **gov.uk = real `<h1>` hero line (9 words), non-empty genuine text** —
   exactly DEC-G4. ENDORSED.
Rationale basis: the independence numbers + 0 gold-only vocab + 0 U+FFFD make
a strong, auditable anti-inversion case. Subject to code-reviewer + DA before
freeze (locked discipline; DA must independently attack the inversion claim).

## Environment finding / decision
- Assembly agent reported "disk 100% full 3.7T/3.7T" + MSVC `LNK1140`.
  **Misdiagnosis:** C: actually has **90.9 GB free**; target/=0.9 GB;
  runs/=34 dirs/1 MB. `LNK1140` is the MSVC linker PDB-size limit on large
  Rust **debug** binaries — unrelated to free disk. **DEC-G5 (supervisor,
  reversible):** run all verification with `CARGO_PROFILE_DEV_DEBUG=0`
  (drops PDB bloat; behaviour/tests identical, only debug symbols change).
  No committed files touched; no `cargo clean` (keeps warm deps). Verifies
  the freeze on a TRUSTWORTHY fresh build, not the agent's stale binary.

## Honest correction (state plainly per Bug-E2 ethos)
Task #16 was framed "verify gate stops reporting VACUOUS". At the M1 floor the
crate is NotImplemented ⇒ every URL NotScored ⇒ the gate is *correctly*
VACUOUS **even with gold present**. Gold does NOT make the gate substantive;
**M2 does**. The value of freezing gold now is METHODOLOGICAL INDEPENDENCE
(§2.7: gold committed before crate tuning), not a substantive gate today.
Re-bless still done so the committed baseline honestly records the gold-era
floor (clean future M2 diffs). #16 reframed accordingly.

## Plan / next
1. DEC-G5 build sanity (fresh, debuginfo=0) → trustworthy gates.
2. code-reviewer + devil's-advocate (∥) on gold artifact + the new test.
   DA focus: independently attack anti-inversion (gold ≠ rebadged oracle),
   band honesty (not gamed), Bug-E2 gates intact, snapshot-as-source-of-truth.
3. apply-feedback (if any) → focused re-verify.
4. **§2.7 freeze**: supervisor atomic commit of `benchmark/corpus/gold/**` +
   the score.rs test; freeze justification in commit message (pre-M2-tuning).
5. **§9 re-bless** post-gold (separate commit); document gate stays VACUOUS
   at M1 by design (honesty).
6. **Tranche-2 (#17):** 3 hard EDGAR/regulator scoped-excerpt (DEC-G1).
   Dossiers → supervisor scoped-excerpt adjudication in Arthur's stead
   (apply DEC-G1, document each call) → assemble → review/DA → §2.7 freeze.
   Completes HLD §7; ALL gold committed before M2.
7. Final closeout; M2 remains Arthur-gated (do NOT start).

## 2026-05-17 — dual sign-off, honesty corrections, tranche-1 §2.7 freeze

**Integrity gate PASSED (locked discipline).**
- code-reviewer: **APPROVE**, no blocking issues; gates independently green
  (debuginfo=0, 206/9/0); parses both real loaders; policy-faithful; honest
  bands; new test strengthens suite; zero collateral.
- devil's-advocate: **SAFE to §2.7-freeze**. Independently RE-DERIVED all
  numbers (own reimplemented harness tokenizer, self-validated vs metrics.rs
  vectors; own fresh oracle replay; did NOT trust the verify/ aids) —
  reproduced the assembler's figures to 4 d.p. Per-URL: **NOT inverted on
  any URL**. Subtle subset reasoning resolved: gold ⊊ Trafilatura on the
  Wikipedia pages is *correct, not circular* — Trafilatura itself scores only
  J≈0.54/0.62, P≈0.56/0.65 vs gold (materially disagrees; the ~3k/0.5k
  oracle-only tokens = the refs/infobox DEC-G3 excludes), so a clean-prose M2
  can demonstrably EXCEED the oracle; a lazy oracle-copy M2 *ties, never
  beats* — no laundering path to a false "beats SOTA". Provenance
  independently verified (gold prose verbatim in snapshots; excluded material
  genuinely absent; U+FFFD=0 at codepoint level). Bug-E2 mutation-tested:
  8 mutations (empty/whitespace .txt, 5-field row, missing .txt, url∉corpus,
  impossible band, hidden gold.tsv) all correctly HARD-RED; the new test
  proven non-vacuous (pins the committed 4-entry artifact, not just "loads").
  **DA earned its seat again** — it genuinely attacked the load-bearing claim
  and confirmed it by independent re-derivation, not assertion.

**Honesty corrections (Bug-E2 ethos — scrub inaccurate claims even in
scratch; the load-bearing conclusions STAND and are independently verified):**
- *"Oracles emit `�` mojibake" is INACCURATE.* DA's fresh adapter replay
  shows Trafilatura/Readability emit correct `£` and **0 U+FFFD** on these
  snapshots — no `�18,479`/`Baiti�h`. The mojibake seen in #13 dossiers /
  VERIFY_SUMMARY was a **dossier-agent console/encoding artifact, not real
  oracle output**. The fact this journal relied on — *gold is Unicode-correct,
  derived from the snapshot source (0 U+FFFD, correct £ é – —)* — STANDS and
  is independently codepoint-verified. The earlier "Progress" claim
  "`�` is the ORACLES' corruption" is hereby corrected: it was an
  environment artifact; anti-inversion does not depend on it.
- *"0 gold-only vocab" used a non-harness tokenizer.* With the real harness
  tokenizer, gold-only-vs-Trafilatura = 231 (Apple) / 84 (Morrison) — ALL
  punctuation-attached prose tokens (`'1976.'`, `'$80.'`), not invented
  vocabulary. The "clean prose subset" conclusion HOLDS; the precise figure
  was wrong. Corrected.
- *MINOR (by design, no fix):* word bands are a weak secondary guard
  (≈±10% prose, ±3 tiny); Jaccard-vs-gold is the primary discriminator. DA
  confirmed no band-leniency path to a false perfect M2 score.
- The two inaccuracies live only in untracked `wrk_gold/` scratch (never
  frozen, never committed); DA's verdict used independent re-derivation, not
  those scripts, so the freeze decision is untainted. Scratch left as-is
  (throwaway adjudication aids); accuracy recorded HERE for Arthur's audit.

**FREEZE DECISION (DEC-G6, supervisor, in Arthur's stead, documented,
reversible).** Arthur wanted gold "with him" then explicitly granted autonomy
("push on; sensible decisions; don't get blocked"). The artifact faithfully
implements his locked DEC-G3/G4 and passed the locked integrity discipline
(code-reviewer + DA) with anti-inversion *independently proven*. §2.7's
requirement is "gold curated+committed BEFORE any M2 crate tuning" + "post-
freeze edits need written justification" — so freezing now (a) satisfies
methodological independence, (b) unblocks the pipeline (tranche-2, re-bless),
and (c) is itself REVERSIBLE via the §2.7 written-justification mechanism if
Arthur amends on return. This is tranche-1 of the HLD §7 set; **M2 stays
GATED until tranche-2 is also frozen** (DEC-G2). Freeze executed by
supervisor; fully git+journal documented for Arthur's audit; he may amend
any entry via a §2.7-justified commit.

### #16 re-bless DONE + tranche-2 kickoff (2026-05-17 cont.)

- Session commits: `f6ed1e3` tranche-1 §2.7 freeze · `8185baf` journal ·
  `4ddedb9` re-bless (run 22-51-50Z, gold-era floor).
- Re-bless verified: fresh run B vs the NEW baseline → **exit 0** (no
  spurious regression from the baseline swap); **run A ≡ run B modulo
  timestamp** ⇒ gold static-file reads are deterministic (no extra 2-run
  determinism sweep needed — the scoring path's determinism was already
  independently proven in Stage C; gold adds only static reads; A≡B
  re-confirms it for free). Gate honestly **VACUOUS at M1** by design; the
  report "## Gold set" table shows each of the 4 gold URLs as
  `FAIL (not scored: crate not_implemented)`, `Coverage: n/a` — NO
  laundering of the floor into a fake 0.0/pass. Instrument behaving exactly
  per Bug-E2 doctrine.
- **DEC-G7 (supervisor URL picks for tranche-2, in Arthur's stead,
  documented, amendable via §2.7).** Completes HLD §7 (lean-4 covered §7
  #1/#2/#4/#7; tranche-2 covers #3/#5/#6):
  - §7 #3 — Apple FY2025 10-K `aapl-20250927.htm` (62 tables) — snapshot
    `9a1590d0917107a7.html`.
  - §7 #5 — *IBM FY2008 10-K* `a2189817z10-k.htm` (legacy uppercase
    `<TABLE>` + ~1500 `<font>` presentational soup) — snapshot
    `41d2afac25d46010.html`. Chosen over Apple FY2019 (which urls.tsv
    *suggests* as the "#5 candidate") because tranche-2 is explicitly the
    HARDEST cases and IBM FY2008 is "the classic legacy-EDGAR extraction
    weak-point"; it also diversifies issuer/era (avoids 2× Apple 10-K
    gold). The urls.tsv annotation was a non-binding suggestion; Arthur's
    bindings are DEC-G1..G4. Documented for his audit; amendable.
  - §7 #6 — gov.uk HMRC employer rates 2025-26 (23 tables, strongest
    table-heavy regulator) — snapshot `9ec7aaf8edb71ac1.html`.
- Method = **DEC-G1 scoped excerpt**: gold = a bounded, human-verifiable
  correct NARRATIVE span (10-K "Part I, Item 1. Business" prose; the
  regulator page's explanatory narrative), financial-statement tables /
  table-cell soup EXPLICITLY EXCLUDED, + honest word bands + why_critical.
  This is the harder adjudication (excerpt-boundary judgement on
  table-heavy filings); I make the boundary calls per DEC-G1 and document
  each heavily. Pipeline mirrors tranche-1: dossiers (structure + candidate
  boundaries) → supervisor scoped-excerpt adjudication → assemble from
  SNAPSHOT source (anti-inversion absolute) → code-reviewer + DA →
  supervisor §2.7 freeze (completes §7) → re-bless → final closeout.
- **M2 remains GATED** until tranche-2 is §2.7-frozen (DEC-G2). Not started.

### Tranche-2 dossiers in → DEC-G8 scoped-excerpt adjudication (supervisor, in Arthur's stead, documented, §2.7-amendable)

Dossiers (`wrk_gold/tranche2/`, untracked) surfaced that scoped-excerpt
adjudication here is genuinely hard and forced a reasoned decision:

- **Apple FY2025 10-K** (`9a1590d0917107a7.html`): "Item 1. Business" =
  ≈2,380 w, **0 tables**, contiguous; body headings are `<span>`
  `Item N.<NBSP×4>Title` (TOC uses `<a>` links — distinguishable). Both
  oracles interleave 40 tables into the narrative (so NOT an oracle proxy —
  good for independence). **DEC-G8 #3: gold = "Item 1. Business" narrative
  only** (start anchor body-`<span>` "Item 1." Business, end anchor
  body-`<span>` "Item 1A." Risk Factors; TOC links + all tables excluded).
  Tight, human-verifiable, exactly the §7#3 intent.
- **IBM FY2008 10-K** (`41d2afac25d46010.html`): **rejected as a GOLD
  source** (stays in corpus as a differential-test case). Reasons: (a) NO
  clean table-free narrative span (Item 1 Business = 4,247 w / 7 tables;
  table-free Items are ≤164-w incorporate-by-reference stubs); (b) EDGAR
  typesetting control-strings (`ZEQ.=5,SEQ=10,EFW=…`, `COMMAND=ADD_DCR…`,
  `TOC_BEGIN/END`, `THIS IS THE END OF A COMPOSITION COMPONENT`, bare page
  numbers) leak into visible text, polluting every excerpt; (c) double-TOC
  ambiguity. A trustworthy human-verifiable scoped excerpt is impractical
  here — and a gold I cannot confidently verify would itself poison the M2
  measurement (worse than no gold). **Bug-E2 instrument validation:**
  Trafilatura returns clean `ok` with `text:""` / `word_count:0` on IBM
  FY2008 — the harness faithfully records `ok` (the contractually-correct
  "found-nothing is a valid ok:true success", NOT an oracle error). This is
  the instrument honestly surfacing a real legacy-EDGAR oracle weakness —
  precisely why IBM FY2008 is a valuable *corpus* case but a poor *gold*
  case.
- **DEC-G8 #5 (amends DEC-G7): SWAP §7#5 IBM FY2008 → Apple FY2019 10-K**
  `https://www.sec.gov/Archives/edgar/data/320193/000032019319000119/a10-k20199282019.htm`
  snapshot `577e61856ca2770d.html` (124 tables, FY2019 — satisfies §7#5
  "older table-heavy EDGAR weak point"; modern Apple structure ⇒ expected
  clean "Item 1. Business" span like FY2025). Rationale: gold MUST be
  high-confidence human-verifiable ground truth (DEC-G1); the specific URL
  for "one older table-heavy SEC filing" was always a supervisor pick (HLD
  §7 does not bind it; urls.tsv even *suggested* Apple FY2019 as the "#5
  candidate"). Choosing trustworthy-gold over a dogmatic "hardest markup"
  pick is the sensible call; IBM FY2008 retained in corpus. Gold =
  "Item 1. Business" narrative. **Honest-failure gate:** if Apple FY2019
  lacks a clean table-free verifiable Item 1 span, assembler STOPS and
  reports — no forced bad gold; §7#5 would then escalate to Arthur.
- **gov.uk HMRC 2025-26** (`9ec7aaf8edb71ac1.html`): no large contiguous
  table-free narrative (23 rate tables interleaved one-per-section; ~2,757-w
  full narrative is non-contiguous + restates rate figures inline →
  low-confidence "correct"). The only clean contiguous table-free span is
  the ≈150-w guidance lead-in before the first rate table. **DEC-G8 #6:
  gold = that contiguous lead-in narrative** (all 23 tables + "Updates to
  this page" changelog + in-page "Contents" TOC excluded). Small but
  high-confidence verifiable; tests "extract guidance prose, reject the 23
  tables" — the actual weak point. A small high-confidence gold beats a
  large low-confidence one (Bug-E2: don't manufacture confidence).

§7 completion after tranche-2 = lean-4 (#1 Apple WP, #2 Morrison WP, #4
example.com, #7 gov.uk hub) + tranche-2 (#3 Apple FY2025 10-K, #5 Apple
FY2019 10-K, #6 gov.uk HMRC). All 7 §7 members covered. Method DEC-G1
scoped-excerpt for the 3 hard cases; anti-inversion absolute (excerpts cut
from SNAPSHOT source, oracles exhibits-only). Next: assemble (verify-then-
build, honest-failure) → code-reviewer + DA → §2.7 freeze tranche-2
(completes §7) → re-bless → closeout. M2 stays GATED.

### Tranche-2 assembly outcome + DEC-G9 (FY2019 gate refinement) + encoding-fix note

**Assembly: 2 of 3 built; Apple FY2019 honest-STOPPED (gate worked).**
- ✅ **Apple FY2025 10-K** → `apple-fy2025-10k-item1.txt`, "Item 1.
  Business", **n=2293**, band [2063,2523], 0 tables in content span,
  verbatim anchors, J vs Traf/Read = 0.18/0.16 (oracle 15× larger w/ all 62
  tables — strongly asymmetric ⇒ scoped excerpt, NOT oracle output;
  anti-inversion holds), excluded financial figures verified absent, 0
  U+FFFD.
- ✅ **gov.uk HMRC 2025-26** → `govuk-hmrc-2025-26-leadin.txt`, table-free
  lead-in, **n=151**, band [135,167], 0 tables in span, J=0.14/0.15,
  verbatim, rate cells verified absent, 0 U+FFFD, thinness-gate pass
  (151≥40).
- ⛔ **Apple FY2019 10-K STOPPED** at the honest-failure gate (NO
  substitution, NO forced gold — exactly the designed behaviour). Evidence:
  legacy filing has no modern `<span>` Item-heading; body Item 1 Business =
  DOM pos 3374, body Item 1A = pos 3591; the ONLY `<table>` in [3374,3591)
  is at pos 3584 — a 2-row layout table whose entire text is
  "Item 1A.Risk Factors" (the legacy filing wraps the NEXT section's
  heading in a presentational table).

**DEC-G9 (supervisor, documented, §2.7-amendable) — FY2019 gate
refinement, then VERIFY-then-build.** The honest-failure gate's purpose is
"no table pollution *inside the gold content*" (the IBM FY2008 rejection
was 7 financial tables + EDGAR control-strings *within* Item 1 Business
prose). FY2019 is categorically different: the single table is the
*Item 1A heading-wrapper at the END boundary we exclude anyway*; the
Business narrative content span [3374, 3584) has **zero** tables and no
control-string pollution. Correct rule reading: measure tables in the GOLD
CONTENT span (what enters the `.txt`), not a raw DOM range that includes
the excluded next-section delimiter. ⇒ Apple FY2019 "Item 1. Business" IS
a valid scoped gold with end-anchor set *before* the Item 1A
heading-wrapper table. This is the correct definition of "the span", not a
rationalisation to avoid escalation — and per Bug-E2 it is **verified
empirically** (focused agent: confirm [Item1 → just-before-Item1A-wrapper]
is contiguous, table-free, control-string-free, verbatim-in-snapshot;
honest-FAIL + escalate §7#5 to Arthur if any real pollution is found).
This completes §7 properly (7 entries) rather than leaving an
artificially-blocked #5.

**Encoding-fix note (verify-don't-trust validated).** The tranche-2
*dossier* `_derived/*` used `lxml.html.fromstring(raw_bytes)` which
mis-decodes the UTF-8 Apple snapshots (XML encoding decl) → mojibake +
inflated word estimates. The *assembler* parsed with explicit UTF-8 (0
U+FFFD; real U+2019/U+2014/NBSP) so ITS counts are authoritative and differ
from the dossier (Apple FY2025 n=2293 not ≈2380; gov.uk 151≈150). Dossier
word estimates are ADVISORY ONLY henceforth. Tranche-1 frozen gold is
UNAFFECTED — it was independently DA-verified at codepoint level (0 U+FFFD,
own re-derivation), so only dossier *estimates* could ever have been off,
never the frozen `.txt`. This is exactly why every artifact is
independently re-derived before a freeze.

**gov.uk lead-in metadata — ENDORSED.** The excerpt includes the
From/Published/Last-updated block: genuine contiguous pre-first-table page
lead content (not a rate table, not the Contents TOC, not chrome),
verbatim-from-source (cut, not prettified — anti-inversion preserved). The
slightly-raw `From:`/`Published` labels are acceptable; gold reflects the
real page lead. Documented for Arthur's audit.

**Verification (assembler, debuginfo=0):** fmt/clippy clean; benchmark 206
/ mdrcel 9 / 0 failed (the test's exact-count assertion moved 4→6, stays
GREEN, not weakened to `>=`); end-to-end `(6 gold)` exit 0; all 6 gold URLs
honestly `FAIL (not scored: crate not_implemented)` — correctly VACUOUS M1
floor, no laundering. Frozen tranche-1 rows/.txt byte-untouched (pure
append). After DEC-G9 FY2019 lands → 7 entries; then code-reviewer + DA on
all of tranche-2 → §2.7 freeze (completes §7) → re-bless → closeout.

### §7 COMPLETE — FY2019 gate PASS under DEC-G9 (verified, not assumed)

FY2019 verify-then-build **PASSED** (independent re-derivation, not trusting
prior probe). Material correction the agent surfaced: the prior probe MISSED
the *symmetric* START "Item 1. Business" heading-wrapper `<table>` at DOM
pos 3367 (it counted from 3374, inside that wrapper). Reality: this 2019
filing wraps BOTH section headings in identical 2-row layout tables; the
narrative content strictly between them `[3378, 3584)` has **zero** tables,
**zero** EDGAR control-strings (modern iXBRL, 0 `<font>` — NOT the IBM
legacy pattern), contiguous, verbatim (1 occurrence, 0/29 line misses).
DEC-G9 reading (heading-wrapper at the excluded boundary ≠ disqualifying;
measure tables in the GOLD CONTENT span) is the only consistent reading and
the symmetric-wrapper finding *strengthens* it. Built
`apple-fy2019-10k-item1.txt` n=1865 band[1678,2052], structurally identical
to the FY2025 gold; independence J=0.135/0.136, containment strongly
asymmetric (gold⊂traf=0.999 but traf⊂gold=0.135; oracle 17–23× larger w/
all 124 tables) ⇒ scoped excerpt ≠ oracle output, anti-inversion holds;
excluded FY2019 financial figures verified absent; both oracles ok (no
Bug-E2). Verification: fmt/clippy clean, benchmark 206 / mdrcel 9 / 0
failed (exact-count 6→7 GREEN, not weakened), end-to-end `(7 gold)` exit 0,
all 7 honestly `FAIL (not scored: crate not_implemented)` (correct VACUOUS
M1 floor). Pure append (4 frozen tranche-1 + 2 + FY2019 = 7 rows; header +
frozen rows byte-unchanged); stale "REJECTED" score.rs comment rewritten to
accurate DEC-G9 PASS rationale.

**HLD §7 set COMPLETE (7):** #1 Apple WP, #2 Morrison WP, #4 example.com,
#7 gov.uk hub (lean-4, FROZEN f6ed1e3) + #3 Apple FY2025 10-K Item 1,
#5 Apple FY2019 10-K Item 1, #6 gov.uk HMRC lead-in (tranche-2,
uncommitted). Next: code-reviewer + devil's-advocate on all of tranche-2
(highest-stakes scoped-excerpt gold; DA must independently re-derive table
positions + attack the DEC-G9 refinement as possible rationalisation) →
apply → supervisor §2.7 freeze tranche-2 (completes §7) → re-bless →
closeout. M2 still GATED until tranche-2 frozen.

### GOLD SUB-PHASE CLOSEOUT (2026-05-18)

**HLD §7 gold set COMPLETE & FROZEN. M2 methodological precondition (§2.7)
satisfied. M2 NOT started (explicit Arthur gate).**

Tranche-2 dual sign-off: code-reviewer **APPROVE-WITH-NITS** (frozen
tranche-1 SHA256 byte-intact; policy-faithful; gates clean; one cosmetic
annotation nit — row-5 NBSP-normalization note — APPLIED before freeze
since why_critical is frozen-forever committed doc and exact-truthfulness
is this sub-phase's discipline). devil's-advocate **SAFE** — independently
re-derived everything (own DOM parser + own tokenizer self-validated vs 21
metrics.rs vectors + fresh oracle replay; trusted neither prior probe nor
assembler aids): 0 tables strictly inside both EDGAR narratives; gold a
faithful verbatim snapshot cut (J≈0.99 vs independent reconstruction, 0
invented tokens, excluded financial figures absent, 0 U+FFFD); **NOT
inverted on any row** (oracle precision vs gold ≈0.14–0.18 ⇒ SOTA tool
materially disagrees ⇒ clean extractor provably beats it, oracle-copy only
ties); **DEC-G9 attacked as motivated reasoning and found SOUND** ("only
self-consistent reading; symmetric-wrapper finding strengthens it"). No
CRITICAL/MAJOR; the one MINOR (bands a weak secondary guard) is by-design
and matches the prior DA's recorded position. **Devil's-advocate has now
earned its seat in every Phase-2/gold stage (A,B,C, gold-T1, gold-T2) — it
genuinely attacked the load-bearing anti-inversion claim and confirmed it
by independent re-derivation, not assertion.**

Session commit chain (branch `feat/oracles-and-corpus`, NOT merged/pushed):
`c561148` Stage C · `611e0a9` re-bless · `700aecd` jrn · `f6ed1e3` gold
tranche-1 §2.7 freeze · `8185baf` jrn · `4ddedb9` re-bless · `f58ae57`
gold tranche-2 §2.7 freeze (HLD §7 COMPLETE) · `df42ec9` re-bless
complete-§7 floor · (this) jrn. Re-bless verified each time: fresh run vs
new baseline exit 0, run A≡run B modulo timestamp (deterministic incl.
static gold reads). End-to-end final: `scored 51 urls (7 gold) | crate
not_implemented:51 | trafilatura ok:51 | readability ok:51 |
trusted-scores 0`, exit 0, gate **honestly VACUOUS** at M1 (all 7 gold
URLs `FAIL (not scored: crate not_implemented)` — no laundering).

**Decision ledger (all supervisor-decided in Arthur's stead while he
slept; reversible; §2.7-amendable via a written-justification commit):**
- DEC-G1 *(Arthur)* scoped-excerpt for hard cases · DEC-G2 *(Arthur)*
  lean-4-then-complete · DEC-G3 *(Arthur)* Wikipedia=prose-only ·
  DEC-G4 *(Arthur)* hub=minimal-genuine-body.
- DEC-G5 build with `CARGO_PROFILE_DEV_DEBUG=0` (MSVC LNK1140 PDB-limit
  workaround; the agent's "disk full" was a misdiagnosis — 90 GB free).
- DEC-G6 supervisor froze tranche-1 in Arthur's stead (autonomy granted;
  reversible via §2.7).
- DEC-G7 → **DEC-G8** §7#5 SWAP IBM FY2008 → Apple FY2019 (IBM rejected as
  GOLD — control-string pollution / no table-free span / Trafilatura
  empty-ok; kept in CORPUS as a valuable oracle-weakness case).
- **DEC-G9** heading-wrapper layout-table at the *excluded* span boundary
  is not disqualifying; measure tables in the gold CONTENT span. Verified
  empirically + DA-attacked-and-upheld.
- example.com gold uses the committed snapshot's *actual* sentence (newer
  than Arthur's quoted IANA text) — snapshot is the immutable source of
  truth.

**FOR ARTHUR (review when back; nothing here blocks — all reversible):**
1. Endorse/adjust DEC-G6/G8/G9 (esp. the IBM→FY2019 §7#5 swap and the
   DEC-G9 gate refinement) — both fully documented, DA-validated; amend
   via a §2.7-justified commit if you disagree.
2. **M2 (extraction algorithm) is the next phase and is YOURS to start.**
   The §2.7 methodological-independence precondition is now satisfied (all
   §7 gold curated+frozen before any crate tuning). Not started.
3. Branch `feat/oracles-and-corpus` is NOT merged/pushed (no request).
4. example.com snapshot-vs-rule call (FYI; gold stands).

## Risks / watch
- Anti-inversion is the load-bearing claim; DA must genuinely try to break it.
- `LNK1140` recurs without DEC-G5 env on any fresh full build — keep the env.
- Tranche-2 scoped-excerpt adjudication is harder (EDGAR table-soup); I make
  the calls per DEC-G1 and document heavily for Arthur's audit.
- Crashed-write environmental risk (prior phase) still unmitigated in-repo;
  committing promptly per stage limits blast radius.
