# M6 Forward Push — `/goal` Loop Journal

**Started:** 2026-05-22 (Arthur asleep; bedtime directive in effect)
**Supervisor:** Claude (context-protective; agent-team dispatch model)
**Inherited state:** Cargo `0.16.0`, 1155 lib tests, 6 BLOCKER gates green,
markdown corpus parity 48/51 (41 substantive + 7 allowlist + 3 deferred).

## M6 Charter

**Original target:** Drive `trafilatura_markdown_gate` to **51/51 =
100% substantive** byte-equivalence. Eliminate the
`DEFERRED_KNOWN_DEFECT` bucket entirely.

**Re-baselined target (after Stage 1 anti-inversion finding):**
Resolve all 3 inherited DEFERRED entries by **fix OR correct re-triage
to allowlist**. Structural ceiling is 50/51 substantive + 1
allowlist-ceiling (since `507b9cdb` is doctrinally an allowlist case,
not a fix). The honest end-state for M6 is `42 substantive + 8
allowlist + 0 deferred = 51`, retiring the `DEFERRED_KNOWN_DEFECT`
bucket entirely.

The 3 deferred fixtures inherited from M5:

| Hash | Fixture | ADR | Difficulty (a priori) |
|---|---|---|---|
| `507b9cdb` | Apple FR (French Wikipedia) — `<sup class="reference">` paragraph break | `wrk_docs/m5-deferred/507b9cdb.md` | **EASIEST** (~30 LOC, clear recipe) |
| `e1106c5e` | PBS news — `BODY_XPATH` content-selection divergence | `wrk_docs/m5-deferred/e1106c5e.md` | **MEDIUM** (investigative) |
| `e339ce76` | FRED — jusText classifier divergence | `wrk_docs/m5-deferred/e339ce76.md` | **HARDEST** (classifier port-trace) |

**Stage order rationale:** Easiest first → de-risk Stage 1, build
momentum, then take on PBS, then the deepest jusText work. If Stage 3
doesn't converge in reasonable agent budget, I scope-reduce to a
Stage 3-mini (partial classifier fix + document residual carry-forward)
and pivot to M7 stretch (JSON output-format gate).

## Autonomous decisions (no Arthur consultation possible)

1. **M6 = defect closure**, not format expansion. Reasoning recorded in
   the supervisor's opening message: three ADRs with ready-made
   remediation recipes, bounded scope, clean M5 continuation.
2. **Stage order**: easiest → hardest. Builds momentum, surfaces risk
   early, preserves agent budget for the hard one.
3. **Stage 3 carry-forward policy**: if jusText port-trace stalls,
   scope-reduce and document rather than block. The classifier port may
   genuinely require multiple passes; do not let one fixture block 51/51
   if 50/51 is the achievable closure with FRED as a single carry-over.
4. **M7 stretch**: only if M6 closes cleanly AND context permits. Do
   not initiate any unbounded format-expansion work that would risk
   leaving M6 finale uncommitted.

## Doctrinal inheritances from M5

- **Anti-inversion**: any agent fix must verify against Python source
  truth, not against my brief alone. I (the supervisor) misdiagnosed
  ≥4 root causes in M5; agents caught them.
- **Net char-count is unsafe**: local diffs can cancel (Stage 6h
  documented this — Apple FR was "1-byte" actually 145 bytes structural).
  Stage 1 agent must verify structural alignment, not just net.
- **Brief-driven hypotheses are mostly wrong**: dispatch with the ADR as
  source of truth, ask the agent to verify before fixing.
- **rcdom-vs-lxml tail model is systemic**: 3+ M5 occurrences. Watch
  for this in Stage 2 (PBS body selection — may have a tail-side-effect).
- **lxml `//` XPath is absolute regardless of context**: bit Stage 6g
  twice. Watch for this in Stage 2.

## Stage tracking

(Per-stage entries appended below as work progresses.)

---

### Stage 1 — pivot to allowlist (anti-inversion finding)

**Status:** Closed — bucket-move only, no code change.
**Outcome:** `507b9cdb` moved from `DEFERRED_KNOWN_DEFECT` → `PYTHON_UNDER_EXTRACT_ALLOWLIST`. Gate accounting: **41 substantive + 8 allowlist + 2 deferred = 51**. Lib tests: 1155 passing (unchanged).

**Diagnosis (M6 Stage 1 investigation agent).** The M5 deferred ADR misdiagnosed the mechanism. It claimed Python's `xmltotxt` has a special `<sup class="reference">` paragraph-break branch and that mdrcel should mirror it (a ~30 LOC port in `output.rs`). Direct verification of Python source disproves this:

- `htmlprocessing.py:402-407` — `convert_tags` walks the tree and renames `<sup>` → `<hi rend="#sup">`, calling `elem.attrib.clear()` FIRST. The `class="reference"` discriminant is destroyed before `xmltotxt` ever sees the element.
- `xml.py:253-351` — `process_element` (the `xmltotxt` body renderer) has NO `<sup>`-class branch. There is nothing in the rendering path that could distinguish `<sup class="reference">` from any other `<sup>`, even if the attribute had survived.
- `main_extractor.py:108-115, 272-351` — the paragraph break is actually a side-effect of `handle_paragraphs` / `handle_formatting` running on inline `<hi rend="#sup">` markers when `formatting=True`. `strip_tags` mutates the live `iter("*")` during `handle_paragraphs`, leaving some `<hi>` descendants un-marked-"done" with their parent renamed. Subsequent `_extract` iteration dispatches them to `handle_formatting`, which wraps each in a fresh `<p>` whose tail becomes the new paragraph's body.

**Smoking gun.** `trafilatura.extract(output_format='txt')` (`formatting=False`) on the same fixture produces inline references matching mdrcel exactly; the paragraph break appears ONLY in `output_format='markdown'`. Python's XML/txt and markdown paths disagree on the same body. This is the textbook anti-inversion case: the break is a `formatting=True` side-effect, not a class-aware rendering choice.

**Prior art.** Same class as the already-allowlisted `86df4d2e` (M5 Stage 6i, `wrk_docs/m5-allowlist/86df4d2e.md`) — the `xmltotxt(body, formatting=True)` re-fracture pattern. `86df4d2e` was triaged correctly at M5 close; `507b9cdb` was misdiagnosed and deferred. M6 Stage 1 corrects the bucket.

**Bucket move.**
- Created `wrk_docs/m5-allowlist/507b9cdb.md` (new ADR — cites Python source, smoking gun, prior art, supersession).
- Deleted `wrk_docs/m5-deferred/507b9cdb.md` (superseded — mechanism claim incorrect).
- Updated `tests/trafilatura_markdown_gate.rs`: moved `"507b9cdbe036bf58.html"` from `DEFERRED_KNOWN_DEFECT` to `PYTHON_UNDER_EXTRACT_ALLOWLIST` with a rationale comment block.

**Verification.** `cargo test --test trafilatura_markdown_gate` reports `GREEN 51 = 41 substantive + 8 allowlisted + 2 deferred / 51`; the 507b9cdb row shows `[allowlist_python_bug]`. `cargo test --lib` reports 1155 passing.

**Doctrinal note.** No code change. The M5 deferred ADR's "~30 LOC fix in `output.rs`" recipe would have invented a class-aware code path that Python itself does not have, then defended it to future supervisors as "matching Python" — the worst kind of anti-inversion drift. M6's deferred bucket now legitimately holds only fixtures where mdrcel IS the buggy side (e1106c5e PBS body-xpath, e339ce76 FRED jusText classifier).

**M6 implication.** Stage 1 closes without a Stage 2 unblocker (no Rust code change to validate). The remaining M6 deferred-bucket targets are PBS (Stage 2) and FRED (Stage 3); the 51/51 target is now structurally 50/51 substantive + 1 allowlist-ceiling (since 507b9cdb cannot become substantive without anti-inversion violation). Supervisor should re-baseline the Stage Final goal to "all DEFERRED entries resolved by either fix or correct re-triage to allowlist".

---

### Stage 3 — FRED fix (two-surface bug; outcome A)

**Status:** Closed — fixture green, deferred bucket retired.
**Outcome:** `e339ce76eb1cba73.html` moved from `DEFERRED_KNOWN_DEFECT` → substantive. Gate accounting: **43 substantive + 8 allowlist + 0 deferred = 51**. Cargo `0.16.1` → `0.16.2` PATCH. Lib tests: 1156 (unchanged). All 5 BLOCKER gates remain green.

**Diagnosis (M6 Stage 3 investigation agent).** The M5 deferred ADR diagnosed the divergence as "mdrcel's `justext.core.classify_paragraphs` returns 487 chars where Python's returns 2907" — a classifier-internal defect. M6 Stage 3 anti-inversion verification revealed the diagnosis was *partially* correct but the root cause is **two independent surface bugs upstream of `classify_paragraphs`**, neither of which is the classifier itself.

**Surface bug (1) — threshold mismatch (`src/trafilatura/justext_core.rs`).** Python's `trafilatura.external.try_justext` (`external.py:129-150`) does NOT call jusText with library defaults — it calls `custom_justext` (`external.py:121-126`), a thin wrapper that passes **trafilatura-specific overrides**:

```python
def custom_justext(tree, stoplist):
    paragraphs = ParagraphMaker.make_paragraphs(tree)
    classify_paragraphs(paragraphs, stoplist, 50, 150, 0.1, 0.2, 0.25, True)
    revise_paragraph_classification(paragraphs, 150)
    return paragraphs
```

vs jusText library defaults at `justext/core.py:28-36`: `(70, 200, 0.30, 0.32, 0.2, False)`. mdrcel's `try_justext` was wired to `classify_and_revise` (a convenience wrapper that uses jusText's library defaults — `justext_core.rs:1140`). The override thresholds are dramatically more permissive on stopword density (0.1/0.2 vs 0.30/0.32) — English-narrative paragraphs at 0.10–0.30 stopword density (typical for news / data exposition) were classified as `bad` instead of `neargood`/`good`. **Fix:** new private `custom_justext` helper inside `justext_core.rs` (line 1294-1361) that invokes `classify_paragraphs_with`/`revise_paragraph_classification_with` with the override thresholds verbatim, line-cited to `external.py:121-126`.

**Surface bug (2) — missing `cleaned_tree_backup` (`src/trafilatura/readability_fork.rs::bare_extraction_with_cascade`).** Python (`core.py:280-297`) deep-copies the post-`tree_cleaning` body BEFORE `convert_tags` / `extract_content` mutate it, then hands the copy to `compare_extraction` as its `tree` arg so `justext_rescue(tree, ...)` (`external.py:97`) sees the full un-extracted tree. mdrcel passed the same `body` NodeRef to both `extract_content` and `compare_extraction` — `extract_content` mutates the body in place (strips elements via `strip_tags_multi`, marks `<done>` etc.), so jusText was running on a body already pruned to ~9 paragraphs out of the original 19. **Fix:** `deep_clone(&body)` between `tree_cleaning` and `convert_tags`, capture into `cleaned_body_backup`, pass that to `compare_extraction` as the `tree` arg. This is the **canonical `rcdom` shared-ownership trap** the M5 journal flagged as systemic (3+ M5 occurrences).

**Anti-inversion findings.**
- The M5 deferred ADR's "487 vs 2907 chars" measurement is correct **as a symptom** but misattributes the cause. The classifier in `justext_core.rs` is structurally faithful to `justext/core.py:243-275`; the divergence was entirely in the call shape upstream (wrong thresholds) AND the tree fed into it (wrong tree).
- **Both fixes are required.** Fix (1) alone with the un-backed-up tree gives 1299 chars of partial content (some narrative paragraphs missing because `extract_content` stripped them). Fix (2) alone with the wrong thresholds keeps most narrative paragraphs classified as `bad` and leaves the noscript stub in the output. Together they produce byte-equivalent output to Python (2915 chars).
- **Anti-inversion doctrine reinforced:** the brief said "the classifier itself looks structurally faithful — the divergence is more likely upstream." That hunch was correct. The brief's enumerated possible causes (stoplist staleness, ParagraphMaker boundaries, link-density accounting) were ALL wrong — the actual causes were neither in the classifier nor in its inputs, but in the **call-shape configuration** (thresholds) and **caller's tree-sharing model** (no backup). The M5 deferred ADR was written from inside the classifier's perspective; the real bugs were one layer up.

**Empirical reproduction.**
- Python oracle: `python benchmark/oracles/trafilatura/run.py --markdown benchmark/corpus/snapshots/e339ce76eb1cba73.html` → 2915 chars (M5 ADR cites 2907; the ~8-char drift is between trafilatura point releases, immaterial).
- mdrcel before fix: 5186 chars beginning with "Please enable JavaScript..." (noscript stub from readability arm; jusText override gate kept readability winner because `jt_len=487` was too small).
- mdrcel after fix (1) only: 1299 chars beginning with "An official website..." (right starting paragraph, missing OMO narrative paragraphs that `extract_content` stripped before jusText saw them).
- mdrcel after fixes (1) + (2): 2915 chars, byte-identical to Python.

**Verification.**
- `cargo test --test trafilatura_markdown_gate` reports `GREEN 51 = 43 substantive + 8 allowlisted + 0 deferred / 51`; deferred bucket retired entirely (M6 charter ✅).
- `cargo test --lib` reports 1156 passing (unchanged).
- All 5 prior BLOCKER gates (`parser_equivalence_gate`, `trafilatura_equivalence_gate`, `trafilatura_extract_smoke`, `trafilatura_extract_content_gate`, `htmldate_parity_gate`) remain green.

**Bucket move.**
- Moved `"e339ce76eb1cba73.html"` out of `DEFERRED_KNOWN_DEFECT` in `tests/trafilatura_markdown_gate.rs:147-172`; replaced the per-fixture comment block with a post-mortem describing both surface bugs and citing the fix sites.
- Deleted `wrk_docs/m5-deferred/e339ce76.md` (superseded — the classifier-internal diagnosis is incorrect). The `m5-deferred/` directory is now empty; the bucket can be removed from the gate file when M7 next touches it, but the empty directory is harmless.

**Doctrinal notes worth recording.**
- **Anti-inversion three-precedent rule held a fourth time.** Stage 1 (Apple FR), Stage 2 (PBS), Stage 3 (FRED) all had M5 ADRs with the wrong root cause. The Stage 3 ADR was the most plausibly correct of the three (it had measured numbers, not just a "looks like" theory) — but it was still wrong about mechanism. Going forward: an ADR's *measurement* can be trusted as a starting point; its *causal claim* must be re-derived from Python source before any fix.
- **rcdom shared-ownership is a systemic trap.** This is the 4th M5/M6 occurrence (Stage 2's readability retry-loop in-place mutation, the noted `<aside>`/footer tail-promotion cases, and now FRED). The general pattern: any two cascade arms that nominally take "the tree" as input but actually share a NodeRef will silently corrupt one another whenever the first mutates in place. Going forward: any new cascade arm that mutates its input tree MUST be flagged at the type level or wrapped in `deep_clone` at the call site. (No tooling to enforce this yet; supervisor discipline is the rail.)
- **Library defaults vs application overrides is a tier-2 anti-inversion class.** A faithful port of a library API can still be a defective port of the application that uses that library, if the application configures the library non-defaultly. The threshold mismatch was invisible from inside `classify_paragraphs` — it was visible only at the `try_justext` call site where Python calls `custom_justext` and mdrcel called `classify_and_revise`. Going forward: when porting an application's use of a vendored library, the FIRST cross-check is "does the application configure the library with non-default arguments?" — not "is the library faithfully ported?"

**Regressions caught.** None. All BLOCKER gates + lib tests remain green. The fix is mechanically minimal: ~80 lines of new code in `justext_core.rs` (the `custom_justext` helper and re-wiring of `try_justext`), ~20 lines in `readability_fork.rs` (the `cleaned_body_backup` deep_clone), and the bucket-move comment rewrite.

**M6 outcome.** Charter target achieved. M6 closes at **43 substantive + 8 allowlist + 0 deferred = 51 / 51**, with the `DEFERRED_KNOWN_DEFECT` bucket retired (empty). The 8-fixture allowlist ceiling stands — all 8 are anti-inversion-clean Python bugs (per their individual ADRs in `wrk_docs/m5-allowlist/`). The doctrinal reset is the more valuable deliverable: M6 caught the M5 supervisor's misdiagnosis pattern three times and pivoted appropriately every time.

