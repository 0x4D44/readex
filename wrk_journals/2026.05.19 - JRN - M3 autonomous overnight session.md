# JRN — M3 autonomous overnight session

**Author:** Claude (supervisor) — autonomous /goal loop driven by Arthur
**Started:** 2026-05-19 (evening — Arthur off to bed)
**Stops only on:** NEEDS-ARTHUR, doctrine red, or manual halt
**Review tier:** Adaptive (code-reviewer always; DA when change touches scoring/regexps/constants OR reviewer flags non-trivial concerns)
**Quality philosophy:** Anti-inversion preserved; faithfulness to Python source-of-truth over fixture-shape matching

---

## State at session start

- Repo: `D:\language\mdrcel` v0.4.7 → in-progress 0.4.8
- Branch: `main`
- HEAD: `41d9f8b` (M3 Stage 2b': htmlprocessing.py remaining + utils helpers + 4 dom.rs facade adds)
- BLOCKER gates green:
  - `parser_equivalence_gate` byte-identical to `149c686` (4/4)
  - `trafilatura_equivalence_gate` 10/10 fixtures (8/8 tests)
- lib tests: 515 passing (post Stage 2c-i implementer; pre-commit)

## Stage progression to date

| Stage | Commit  | Status   | What |
|-------|---------|----------|------|
| 0a    | 0317fb9 | done     | dom.rs lxml-facade adds (tail / delete_with_tail_preserve / document_order_triplets) |
| 0b    | 972d0cf | done     | XPath 1.0 engine + 51-row Python-lxml conformance harness |
| 0c    | 14c2ec8 | done     | Trafilatura-equivalence BLOCKER gate skeleton |
| 1b    | 54a61f3 | done     | tree_cleaning + convert_tags + prune_html + GATE ACTIVATED |
| 1c    | eba9dd3 | done     | baseline() rescue extractor (first real Ok) |
| 2a    | 43dde5e | done     | xpaths.py vendoring + engine-gap survey (37/37 accept, 0 gaps) |
| 2b'   | 41d9f8b | done     | htmlprocessing.py remaining + utils helpers + 4 dom.rs facade adds |
| 2c-i  | -       | reviewer-feedback-pending | handler primitives (handle_titles/formatting/etc.) |

## Stage 2c-i status (entering autonomous session)

**Implementer reported:**
- NEW `src/trafilatura/main_extractor.rs` (918 LOC, 18 tests)
- MODIFIED `src/readability/dom.rs` (+153 LOC; adds `deep_clone(node) -> NodeRef`)
- MODIFIED `src/trafilatura/mod.rs` (+11 LOC declaration)
- Cargo PATCH bump 0.4.7 → 0.4.8

**Reviewer findings:**
- **MEDIUM**: `deep_clone` docstring claims lxml `deepcopy` does NOT preserve tail. Empirical lxml probe says lxml DOES preserve tail. Rust drops tail because rcdom's tail-as-sibling-Text representation has no anchor for a detached root. Inert for Stage 2c-i's only caller (handle_titles) but misleading for future stages.
- **NIT**: `handle_formatting_uses_previous_when_no_parent` test exercises (None, None) fallthrough, not the getprevious() branch. Rename + (optionally) add real getprevious-fallback test.

**Decision (autonomous):** Apply both fixes inline. Docstring correction + test rename are trivial (~30 LOC). Then commit.

## Plan for this session

1. ✅ Create this journal
2. Apply Stage 2c-i fixes (MEDIUM + NIT)
3. Commit Stage 2c-i
4. Dispatch Stage 2c-ii implementer (block handlers: handle_lists, handle_quotes, handle_paragraphs, handle_other_elements, handle_code_blocks) — ~190 LOC source
5. Review Stage 2c-ii
6. Dispatch Stage 2c-iii implementer (table/image/textelem dispatch + prune_unwanted_sections + recover_wild_text) — ~210 LOC source
7. Review Stage 2c-iii
8. Dispatch Stage 2d implementer (_extract + extract_content orchestrator + gate ext)
9. Continue forward through Stages 3-9 as long as context allows

## Risk-management policy for autonomous mode

- **NEEDS-ARTHUR triggers:** novel architectural decisions, undocumented engine gaps requiring expansion, dom.rs facade changes that affect existing semantics (additive-only is safe), Cargo.toml dependency additions
- **Auto-fix:** code-reviewer NITs and MEDIUMs that are surgical (docstring, test rename, magic-number alignment to Python source)
- **Loud-fail policy:** stubs MUST panic (`unimplemented!()`) when reached, NEVER silently return Default. Established in Stage 2b' Fix 1 and Fix 5; continuing.
- **Commit cadence:** every clean stage. No accumulated WIP.
- **Journal updates:** at minimum every commit + every NEEDS-ARTHUR + every stage transition

---

## Log

(append below — most recent at bottom)

### 1. Stage 2c-i closed (commit `6f23a95`)

- Applied 2 reviewer fixes inline:
  - **MEDIUM (docstring):** Rewrote `deep_clone` docstring in `src/readability/dom.rs:1407-1437` to honestly state lxml-preserves-tail-on-deepcopy and rcdom-drops-root-tail-because-of-anchor-representation. Inert for current callers but honest for future.
  - **NIT (test rename + 2 new tests):** Renamed `handle_formatting_uses_previous_when_no_parent` → `handle_formatting_wraps_when_no_parent_and_no_previous`. Added `handle_formatting_previous_element_sibling_facade_smoke` (verifies Text-node-skipping prev finds `<div>`) and `handle_formatting_previous_element_sibling_finds_protected_p` (verifies prev finds a protected `<p>`). Together they pin the facade-level invariant the Python branch depends on, with explicit documentation that the (parent=None, previous=Some) Python shape is structurally unreachable through pure rcdom semantics.
- One test failure midway through fix application: first attempt at `handle_formatting_uses_previous_element_sibling_when_no_parent` probed `previous_element_sibling(&hi)` AFTER calling `handle_formatting(&hi)` — which moves `hi` into a new `<p>` wrapper, so the probe correctly returned None. Restructured to probe BEFORE the call.
- 516 lib tests pass post-commit. Both BLOCKER gates green.

### 2. Stage 2c-ii closed (commit `3b8f0e7`)

**Backstory note:** The Stage 2c-ii implementer dispatch reported `"Not logged in"` at first attempt and was assumed failed. On resume after compact, however, the working tree carried +1098 LOC of completed Stage 2c-ii work in `src/trafilatura/main_extractor.rs` plus the `cleaning::strip_tags_multi` `pub` promotion and the Cargo 0.4.8→0.4.9 bump. The implementer had clearly succeeded on a subsequent retry that wasn't surfaced in the visible message stream. Inherited the work; reviewed inline; committed.

**Reviewer findings (supervisor self-review):** none CRITICAL or MEDIUM. Checked:
- Double-append in `handle_lists` leaf branch (Python lines 179 + 192) — safe because `dom::append_child` does `remove(child)` first, so the second append is a detach-then-reattach to the same parent.
- `iterdescendants("item")` semantics — `get_elements_by_tag_name` is descendants-only document order, matches lxml.
- `iter("*")` includes self semantics — properly mirrored in `handle_quotes`/`handle_paragraphs` with explicit `vec![element.clone()]` prepended to `collect_descendant_elements`.
- rcdom Drop quirk — every block handler that holds NodeRefs across `replace_element_tag` calls pins returned "done" handles in a `dones_alive: Vec<NodeRef>`. In-source comments cite `markup5ever_rcdom-0.39.0/lib.rs:268-284` rationale.
- `text_chars_test`, `clear_attributes`, `delete_with_tail_preserve_free` — all correct facade choices.
- Faithful preservation of Python source quirks (e.g., the leaf-branch double-append) with Rust commentary.

**DA escalation decision:** Not warranted. Stage 2c-ii doesn't touch scoring algorithms or new regex/constant additions (only uses existing `P_FORMATTING`); reviewer found no non-trivial concerns. Per adaptive review tier, DA was skipped.

**Test deltas:** +28 new lib tests covering all 6 functions and edge cases. Total lib tests 516 → 544 passing. Both BLOCKER gates green (`parser_equivalence_gate` 4/4, `trafilatura_equivalence_gate` 8/8 with 10/10 fixtures). One renamed test: `process_nested_elements_dispatches_list_to_handle_lists_stub` (was `#[should_panic]`) → `process_nested_elements_routes_list_descendant_through_handle_lists` (now asserts real handle_lists output structure).

### Next: Stage 2c-iii (table/image/dispatch + prune_unwanted_sections + recover_wild_text)

Splitting into 3 atomic sub-commits to keep diffs reviewable and avoid catastrophic loss if context compacts mid-stage:
- **2c-iii-a**: prune_unwanted_nodes `with_backup=true` substrate (deep_clone-based restore) + `handle_image` real impl.
- **2c-iii-b**: `handle_textelem` (the dispatch hub) + `handle_table` + `define_cell_type`.
- **2c-iii-c**: `recover_wild_text` + `prune_unwanted_sections`.

### 3. Stage 2c-iii-a closed (commit `5c11c6d`)

(Dispatch-failure note: After the Stage 2c-ii commit at `3b8f0e7`, an attempted Stage 2c-iii implementer agent dispatch returned `"Not logged in · Please run /login"`. With agent dispatch proving unreliable on this thread, supervisor switched to inline implementation, split into 3 sub-commits to keep each atomic.)

Substrate fix:
- `cleaning::prune_unwanted_nodes` return type widened from `()` to `NodeRef` to support Python's `tree = prune_unwanted_nodes(...)` rebind shape (additive — all existing call sites are tests that ignored the return).
- `with_backup=true` branch: pre-prune `text_content` length + `deep_clone(tree)` snapshot; post-prune threshold test (`new_len * 7 > old_len`) returns either the pruned tree or the pristine backup. Stage 2b' panic stub replaced with full restore. The integer-multiplication form `new_len * 7 > old_len` is used instead of Python's `new_len > old_len / 7` to avoid divide-by-zero on empty inputs (equivalent for non-negative integers).

Function port:
- `handle_image` (`main_extractor.py:445-480`) replaces the Stage 2c-ii `unimplemented!()` forward stub. Faithful port: `("data-src", "src")` first-loop break; `for..else` fallback scans `data-src*` variants; alt/title copy with Python truthy-check; bail when no usable src; URL canonicalisation rewrites protocol-relative `^//` to `http://`.

Tests:
- `prune_unwanted_nodes`: `#[should_panic]` test replaced with `with_backup_true_restores_when_threshold_trips`; added `with_backup_true_keeps_tree_when_text_survives`. The "restores" test asserts the in-place-mutated `b` still has `<section>` removed (correct Python semantics: tree is mutated permanently then `backup` is returned).
- `handle_image`: 7 new tests covering all branches.
- `handle_paragraphs_graphic_dispatches_to_handle_image_stub` renamed and re-asserted to exercise the now-real dispatch end-to-end.

Total lib tests 544 → 552 passing. Both BLOCKER gates green. Cargo 0.4.9 → 0.4.10.

### 4. Stage 2c-iii-b closed (commit `50a1065`)

Ported `define_cell_type` (`main_extractor.py:354-360`), `handle_table` (`:363-442`), and `handle_textelem` (`:482-509`).

- handle_table is the largest single block handler (~80 LOC source). Key behaviors faithfully preserved: thead/tbody/tfoot strip via `strip_tags_multi`, max_cols + colspan summing for row span, iter-descendants walk with `<tr>`/TABLE_ELEMS branches, nested-table break, mutual-recursive `handle_textelem` dispatch in non-leaf cells, residual-row span pop (Python source quirk — only the LAST appended row loses its span).
- handle_textelem is the dispatch hub for `_extract`: routes each tag to its handler with `<table>`/`<graphic>` gated by `potential_tags`. The `<lb>` branch is unusual — builds a fresh `<p>` from the `<lb>`'s tail.
- rcdom Drop quirk pinning via `dones_alive` Vec applied to handle_table's outer and inner loops.

**Implementation test failure caught + fixed:** First-pass of two span-attribute tests asserted span="3" on a row from a single-tr table. Python's `newrow.attrib.pop("span", None)` (line 435) runs on the RESIDUAL row pre-append — so in a 1-tr table, the only row loses its span. Test was wrong, implementation was right. Renamed the test to `_pops_span_attribute_from_residual_row` and added a `_two_row_table_first_row_keeps_span_last_loses_it` test pinning the per-row span/no-span behavior. Both pass.

**Test deltas:** +18 new lib tests (2 define_cell_type + 8 handle_table + 8 handle_textelem). Total 552 → 570 passing. Both BLOCKER gates green. Cargo 0.4.10 → 0.4.11.

### 5. Stage 2c-iii-c closed (commit `b2e2efa`) — **Stage 2c COMPLETE**

Closing port of `main_extractor.py:512-564`:

- `TAG_CATALOG` constant added (settings.py:436-438): the default `potential_tags` set used by `_extract` (Stage 2d) and `recover_wild_text`.
- `prune_unwanted_sections` (`:533-564`): the full prune cascade — OVERALL with backup, graphic discard, teaser + precision discard, two-pass div/list/p link-density, table link-density when gated, precision-only trailing-head cleanup. Uses `let tree = ...` shadowing because `prune_unwanted_nodes` can swap to a backup tree.
- `recover_wild_text` (`:512-530`): rescue extractor. Clones the `potential_tags` set to avoid Python's default-mutable-arg footgun. Recall mode extends both the set and the XPath search expression. Dispatches survivors through `handle_textelem`.

**Implementation test failure caught + fixed:** First-pass test expected `<noscript>` to be pruned by `DISCARD_IMAGE_ELEMENTS`. Actually that XPath targets caption-class elements (`xpaths.py:189-195`), not noscript. Updated the test to use a `<div class="caption">` instead. Pass.

**Test deltas:** +4 new lib tests (2 prune_unwanted_sections + 2 recover_wild_text). Total 570 → 574 passing. Both BLOCKER gates green. Cargo 0.4.11 → 0.4.12.

### Stage 2c milestone reached

The entire `trafilatura@v2.0.0/main_extractor.py` source file (664 LOC) is now faithfully ported. Five sub-stages, five clean commits, +43 net new lib tests for `main_extractor` alone:

- 2c-i (`6f23a95`): handler primitives + dom::deep_clone.
- 2c-ii (`3b8f0e7`): block handlers (lists/quotes/paragraphs/other_elements/code_blocks).
- 2c-iii-a (`5c11c6d`): prune_unwanted_nodes backup-restore + handle_image real impl.
- 2c-iii-b (`50a1065`): handle_table + handle_textelem + define_cell_type.
- 2c-iii-c (`b2e2efa`): prune_unwanted_sections + recover_wild_text + TAG_CATALOG.

### 6. Stage 2d closed (commit `9203f49`) — **M3 Stage 2 COMPLETE**

Ported `_extract` (`main_extractor.py:567-617`) and `extract_content` (`:620-640`). Also added `Options.min_extracted_size: usize` (default 250 matching `settings.cfg:MIN_EXTRACTED_SIZE`).

`_extract` is the central orchestrator: builds `potential_tags`, iterates `BODY_XPATH`, runs `prune_unwanted_sections`, gates "enough p text", strips inline wrappers, walks descendants through `handle_textelem`, trims trailing NOT_AT_THE_END elements.

`extract_content` is the high-level wrapper: backup → _extract → wild-text fallback → strip done elements + lift div wrappers.

**Key discovery (rcdom Drop quirk in test fixtures):** While debugging a failing test, traced the bug to test fixtures: `let body = parse_body(html)` (helper returning just the body NodeRef) caused the underlying `Dom` to drop, which iteratively drains every descendant's children Vec via `markup5ever_rcdom-0.39.0/lib.rs:268-284` Drop impl — even when the caller holds Rc references to body. The test helper now returns `(Dom, NodeRef)` to pin the Dom alive. Same quirk we've been pinning in handle_table/handle_lists/handle_paragraphs via `dones_alive` Vec, just manifesting on a different surface. Kept the diagnostic probe test (`_extract_probe_body_xpath_evaluation`) as a permanent regression-pin documenting the discovery.

**Test deltas:** +6 new lib tests (3 _extract + 2 extract_content + 1 diagnostic probe). Total 574 → 579 passing. Both BLOCKER gates green. Cargo 0.4.12 → 0.4.13.

### M3 Stage 2 milestone reached

The entire `trafilatura@v2.0.0/main_extractor.py` source file (664 LOC) is now faithfully ported across six sub-stages, six clean commits. All BODY_XPATH constants are vendored (Stage 2a), all 12 handle_* functions are implemented, the dispatch hub routes correctly, prune/recover cascades work, and the top-level orchestrator pipes them all together.

### Next: Stages 3-9 umbrella

The faithful main_extractor port is done. The remaining M3 work is:

- **Stage 3**: discard XPaths runtime fixture validation. Plug BODY_XPATH-driven extraction into the trafilatura_equivalence_gate; add fixtures that exercise the full _extract pipeline.
- **Stage 4**: vendored_readability — fork the readability-lxml extractor as a fallback in the 4-extractor cascade.
- **Stage 5a/5b**: jusText port (boilerplate-removal extractor) + stoplist support.
- **Stage 6**: arbiter — combine outputs from baseline/own/readability/justext via 7-branch heuristic.
- **Stage 7**: metadata extraction (title/author/date/etc.) + JSON-LD `datePublished` bridge for htmldate.
- **Stage 8**: comments extraction + sanity checks.
- **Stage 9**: byte-equality push — get the gate fixtures to byte-identical match with Python.

The natural next step is Stage 3 (gate fixture activation), which will provide oracle-driven validation of the extraction pipeline before moving to Stages 4+.



