# M10 — Post-Fuzz Fidelity Cleanup

**Started:** 2026-05-23
**Lead:** Claude (team lead) + Arthur (supervisor)
**Predecessor:** M9 Differential-Fuzz Hardening (closed 2026-05-23, commit `3297423`)

## Why this milestone

M9 closed with the held-out KPI at md 69.80% / txt 75.00% / xml 71.40% — substantially
below what the 51-fixture gates had been implying. The wild-distribution divergence
breakdown (per M9 Stage-5 Round-3 triage):

- **~60%** selection-phase (different paragraph or table chosen as main content;
  100% diff-shape diversity — each one essentially a different page).
- **~10%** known-deferred control chars (the unported `remove_control_characters`).
- **~30%** scattered structural patterns, 1–5 pages each.

M10 attacks the **bounded, known-yield** work first before deciding whether the
selection-phase pile is worth taking on — partly because that decision depends on
what's left after the cheap base hits land, and partly because the metric question
("is byte-for-byte selection equivalence the right success criterion?") is
unresolved.

## Phase plan

1. **Phase 1 — Control-char port** (bounded; known target; ~10% working-slice yield):
   port `remove_control_characters` from vendored Python faithfully; remove any
   now-stale mask from the diff harness; re-baseline.

2. **Phase 2 — Long-tail structural triage** (post-Phase-1; targeted):
   triage post-Phase-1 working-slice divergences; identify patterns with ≥5
   records that are HIGH-confidence one-liners; fix and re-baseline.

3. **Phase 3 — Selection-phase go/no-go decision** (gated on Phase 2 results):
   only if remaining divergences are ≥50% selection-phase AND a specific
   algorithmic difference is localized (not "the algorithm differs somehow") do
   we proceed. Otherwise surface the metric question to Arthur before sinking a
   multi-session refactor.

## Process discipline (signed off by Arthur 2026-05-23)

- **HLD-first.** Each phase: agent writes an HLD; team lead reviews and iterates
  with the agent until satisfied; only then does the agent write code.
- **Code-review discipline.** Agents use `/code-reviewer` skill and
  `/code-review-apply-feedback` skill on every implementation diff.
- **Anti-inversion (HLD §7).** Every root-cause claim verified against vendored
  Python under
  `benchmark/oracles/trafilatura/.venv/Lib/site-packages/trafilatura/`.
- **Commit policy.** Commits authorised only by team lead; never amend without
  permission; no push (Arthur pushes).
- **Journal cadence.** This file is updated after each significant step
  (HLD sign-off, implementation landing, KPI re-baseline, phase close).

## Baseline KPI (frozen at M10 start)

500-page held-out slice (last 500 entries of `benchmark/fuzz/manifest.jsonl`):

| Format   | Pages | Byte-equal | KPI    |
|----------|------:|-----------:|-------:|
| markdown |   500 |        349 | 69.80% |
| txt      |   500 |        375 | 75.00% |
| xml      |   500 |        357 | 71.40% |

(Source: M9 wrap-up summary, commit `3297423`.)

## Log

### 2026-05-23 — M10 kickoff

Plan signed off by Arthur (control-chars first → long-tail triage → selection
go/no-go decision; HLD-first discipline; code-reviewer skill). Phase 1 HLD design
agent spawned.

### 2026-05-23 — Phase 1 HLD v1 returned

Design agent (`a87980fa8c6136262`) returned HLD v1. Two material brief corrections
the agent flagged up front:

1. M9 did NOT add a diff-harness Cf/U+2063 mask — only the xml fingerprint mask
   exists (`tests/fuzz_diff.rs:488-523`). What needs cleanup are five gate
   `DEFERRED_KNOWN_DEFECT` entries for fixture `507b9cdb` citing
   `wrk_docs/m7-deferred/507b9cdb.md`.
2. Python's `str.isspace()` returns True for U+001C-U+001F (FS/GS/RS/US) and
   U+0085 (NEL). Rust's `char::is_whitespace()` disagrees on exactly these five.
   The Cc-kept carve-out has 10 codepoints, not 5.

Empirical verification by the agent: a 1,112,064-codepoint sweep showed zero
disagreements between Python's predicate and the proposed regex. Solid §7
discipline (anti-inversion).

### 2026-05-23 — Phase 1 HLD review feedback (team lead)

Four review comments returned to design agent:

- **RC1 (CHANGE REQUEST):** §4 pseudocode does three full scans + per-char
  allocation. Refactor to single-pass `Regex::replace_all` with explicit-range
  character class covering Cf/Co/Cn + the four Cc-strip ranges.
- **RC2 (RESOLVE BEFORE SIGN-OFF):** Open Question O1 (metadata-slot serialisation)
  must be resolved in design phase. Not a "verify at implementation" — could miss
  call sites and require revisit.
- **RC3 (SIGN-OFFS):** O2 → A3 (regex, no new crates). O3 → option (b), amend ADR
  in place. O4 → yes, fuzz re-run in same commit (M9 precedent).
- **RC4 (minor):** add one long-Unicode-text unit test for the fast path.

### 2026-05-23 — Phase 1 HLD v2 signed off

Revision agent (`a6fdca2f10dca4fe0`) returned HLD v2 addressing all four review
comments:

- **§4 pseudocode** now six lines — one `OnceLock<Regex>` + one `replace_all`.
  Probe-compiled against `regex =1.12.3`; hex-range syntax `\x00-\x08` IS
  accepted inside a `\p{}`-bearing character class. Full-Unicode equivalence
  sweep (1.1M codepoints) shows zero disagreements with Python's predicate.
- **§5 O1 resolution:** mdrcel's `extract_metadata` has NO `clean_and_trim`
  equivalent — every str-typed metadata slot (title, author, description,
  hostname, url, sitename, date, language, image, pagetype, license, filedate,
  + categories + tags lists) leaks Cf today. Fix: one new helper
  `clean_and_trim_metadata(&mut Metadata)` called once at the end of
  `extract_metadata`. Single patch, full coverage of YAML / JSON / CSV / XML /
  TEI metadata emission. Phase 1 ports the strip portion only; the
  10,000-char cap and `html.unescape` portions of Python's `clean_and_trim`
  are deferred.
- **§6 test plan:** 17 unit tests for `strip_control_chars` (RC4 added); 5
  unit tests for `clean_and_trim_metadata`; O4 fuzz re-run locked in.
- **§8 out-of-scope:** A2 fallback hedge removed; A3 locked; perf tuning
  explicitly out-of-scope unless a follow-up surfaces a hot path.
- **Review Log section** added at HLD bottom documenting all v2 changes.

Empirical artefacts retained in `notes/m10-strip-probe/` (probe regex,
Cn-edge probe, full-sweep generator). Generated CSVs cleaned.

**HLD v2 signed off by team lead 2026-05-23 — implementation may proceed.**

HLD path: `wrk_docs/2026.05.23 - HLD - M10 Phase 1 control-char port.md`.

### 2026-05-23 — Phase 1 implementation landed

Implementation agent (`a5579881840fa131f`) returned a clean diff faithful to the
HLD. Files touched (11 total, +356/-77):

- `src/trafilatura/output.rs` — `strip_control_chars` helper + `line_processing`
  patch + 17 unit tests.
- `src/trafilatura/metadata.rs` — `clean_and_trim_metadata` helper (14 slots) +
  call from `extract_metadata` + 5 unit tests.
- `src/trafilatura/metadata_url.rs` — `line_processing` patch.
- `tests/trafilatura_{csv,json,tei,txt,xml}_gate.rs` — removed the five
  507b9cdb `DEFERRED_KNOWN_DEFECT` entries.
- `wrk_docs/m7-deferred/507b9cdb.md` — appended closure note.
- `Cargo.toml` / `Cargo.lock` — patch bump `0.18.1` → `0.18.2`.

**Validation:**

- `cargo build` clean, 0 warnings.
- `cargo test --lib` — 1186 pass (22 new tests included).
- All 6 format gates 51/51 green.
- 507b9cdb fixture flipped from `DEFERRED_KNOWN_DEFECT` to byte-equal
  substantively on txt / xml / tei / json / csv (markdown untouched).

**Fuzz held-out KPI delta:**

| Format   | Baseline | New     | Lift    |
|----------|---------:|--------:|--------:|
| markdown |   69.80% |  78.80% |  +9.00  |
| txt      |   75.00% |  84.60% |  +9.60  |
| xml      |   71.40% |  80.60% |  +9.20  |

**Mean lift +9.27 abs across formats — 2-3× the HLD's conservative +2-4%
prediction.** The HLD reasoned conservatively about mixed-divergence fixtures
where control chars were one issue among several; the actual result suggests
the control-char leak was a more dominant single driver than the working-slice
scan suggested. No regressions on the 51-fixture corpus.

### 2026-05-23 — Phase 1 code review (team lead)

Ran `/code-reviewer` skill on the diff. Verdict: **APPROVED for commit**. No
required changes, two optional Nits (Cs documentation, `Option::as_mut` style)
both non-blocking. Strengths: anchoring discipline (every call site cites the
Python source line), substantive test design (each test verifies behaviour
with specific representative inputs), uniform gate-deferral removals.

The implementation agent's self-review #7 (verbose preservation-comment
collapse) was a strictly-simpler deviation from HLD pseudocode and is
acceptable.

**Phase 1 committed as `4fdcd46`** (M10 Phase 1: port remove_control_characters;
+9% mean held-out KPI).

### 2026-05-23 — Phase 2 triage finding: zero HIGH-confidence patterns

Triage agent (`a323b68bbe54c927e`) ran against post-Phase-1
`benchmark/fuzz/divergences.jsonl` (490 records, down from 787 pre-Phase-1 —
38% reduction). Triage report saved to
`wrk_docs/2026.05.23 - REPORT - M10 Phase 2 triage.md`.

**Headline finding: zero HIGH-confidence one-liner patterns identified.** No
pattern clears the three-pronged bar (specific Python source pointer + specific
mdrcel function pointer + one-sentence patch sketch).

**Bucket breakdown (490 working-slice divergences):**

| Class                          | Records | Confidence |
|--------------------------------|--------:|------------|
| A. Selection-phase             |   ~286  | DEFERRED   |
| B. Blank-line accounting       |    ~24  | MEDIUM     |
| C. SEO-spam tag leakage        |    ~47  | MEDIUM     |
| D. Entity decoding             |     ~2  | LOW        |
| E. Smart-quote                 |     ~1  | LOW        |
| Unclassified per-page residue  |   ~130  | LOW        |

**Selection-phase ratio: 58.4%** — crosses the HLD Phase-3 ≥50% threshold.
But the gate's second clause ("specific algorithmic difference localized") is
NOT satisfied: the triage finds the selection algorithms are *equivalent*; the
difference is in the *parser input* (html5ever strict vs lxml permissive
foster-parenting). That makes Phase 3 a parser-replacement milestone, not a
selection-algorithm-fix milestone. Multi-session, separate concern.

**Cross-format same-sha correlation**: 121/146 G1 shas appear in G2; 122/146
G1 shas appear in at least one xml divergence. Same page → different
selection across formats. Strongest selection-phase signal.

**Allowlist candidates: NONE.** Conservative per §7 asymmetric trust.

**Three forks the triage agent surfaces:**

- **(a)** Pursue Hypothesis B (blank-line) via 1-session empirical tracer
  round. MEDIUM-confidence, ~24-record yield, ~+1-3% KPI on md/txt.
- **(b)** Skip to Phase 3 selection refactor. Multi-session, requires
  parser-layer work, deviates from the original Phase-3 gate's spirit.
- **(c)** Accept negative result, close Phase 2, surface metric question to
  Arthur — is byte-equivalence the right success criterion when 58% of
  remaining failures are "both extractors picked valid main content but
  different valid ones"?

**Triage agent's own recommendation: (c).** The structural long tail is fully
explained by known M9 root causes (A residue + tail-trap side effects +
snowflake-per-page); diminishing returns are real. Surface to Arthur.

### 2026-05-23 — Arthur chose: tracer round on Hypothesis B, then close

### 2026-05-23 — Phase 2B tracer outcome: MEDIUM on B, HIGH on a neighbour

Tracer agent (`a74f20b6638a4aac8`) ran empirical traces on 8 G5 / G14
representatives (rep numbers 0, 2, 3, 4, 5, 10, 14, 18). Outcome:

**Hypothesis B (serialiser-level blank-line accounting) FALSIFIED.**
`process_element`, `sanitize`, and `line_processing` are all algorithmically
faithful to Python. The divergence is *upstream* of the serialiser: the DOM
tree shape going INTO process_element differs (Python has more `<p>` siblings;
mdrcel has flatter `<div>` + tail-text-on-inline structure). G5 dissolves into
at least two sub-loci: ~8/19 records where mdrcel has FEWER `<p>` boundaries
than Python; ~8/19 records where mdrcel has MORE blank lines (text-vs-tail
apportionment / markdown-only divergences with byte-equal XML). The original
hypothesis was the wrong framing.

**Sub-finding, HIGH-confidence on a DIFFERENT hypothesis: rescue pass missing
from `transform_misused_divs_into_paragraphs`.**

- **Python source:** `benchmark/oracles/trafilatura/.venv/Lib/site-packages/
  trafilatura/readability_lxml.py:297-325` — the function has TWO passes. The
  first retags `<div>` to `<p>` when no block-tag children present; the
  second (lines 305-325) wraps any `<div>` leading text in a fresh `<p>`
  child, and any child's tail text in a fresh `<p>` sibling, and drops `<br>`
  children.

- **mdrcel state:** `src/trafilatura/readability_fork.rs:801-819` implements
  ONLY the retag pass. Comment at lines 784-790 explicitly omits the rescue
  pass on the justification that it "does not affect the scoring decisions
  the rest of summary() consumes (paragraph scoring already concatenates
  descendant text via text_content)."

- **Justification verifiably wrong.** Python's `score_paragraphs`
  (readability_lxml.py:225-259) iterates `<p>`/`<pre>`/`<td>` descendants.
  The rescue pass CREATES new `<p>` elements. Therefore those new `<p>`s
  become scoring candidates, change `select_best_candidate` inputs, and
  influence which body element is picked. The `text_content` concatenation
  argument is about a different invariant.

- **Caller-order verification:** `readability_lxml.py:138-139` runs
  `transform_misused_divs_into_paragraphs()` then `score_paragraphs()` —
  rescue pass is unambiguously upstream of scoring.

- **Empirical evidence (rep 5 sha `781765a839`):** mdrcel keeps the
  Vasilyev/sup pattern as flat `<div>.text="А.И. Васильев…"`; Python's tree
  shows `<div><p>А.И. Васильев</p><sup> 1</sup><a>…</a><p>, М.М. Березенцев</p>…</div>`
  with rescue-pass `<p>` wrappers visible. Final mdrcel xml has the flat
  layout; final Python xml has the wrapped layout.

- **Estimated yield:** ~5-8 of 19 G5 records + unknown number elsewhere in
  the working slice (probably +10-15 records total ≈ 2-3% working slice,
  ≈ +0.5-1% KPI on md/txt).

- **Blast-radius risk:** non-trivial. Adding `<p>` elements during readability
  CHANGES selection decisions, which could break 51-fixture-corpus gates that
  currently pass because mdrcel makes a particular (different from Python)
  selection. Measurable empirically by running all 6 gates after the fix
  attempt — but a real risk.

**Tracer scaffolding:** `examples/m10_phase2b_trace.rs` (uncommitted; per the
brief, scratch).

**Allowlist candidates this round: NONE.** Mirror finding (no Python idiosyncrasy).

### 2026-05-23 — Arthur chose: port the rescue pass with empirical blast-radius measurement

### 2026-05-23 — Phase 2C HLD v1 + sign-off

Design agent (`afa2c386e378523d8`) returned HLD v1. Substantive corrections to
my brief that the agent caught and verified empirically:

- **lxml `drop_tree()` is tail-PRESERVING, not tail-dropping.** Empirically
  confirmed via vendored Python interpreter: tails get re-anchored to previous
  sibling or `parent.text`. mdrcel's existing `delete_with_tail_preserve_free`
  is the correct primitive (already documented at `readability_fork.rs:758-762`).
  My brief stated the opposite; the HLD corrects it inline as a "Pre-HLD note".
- **Element-index vs all-children-index conversion** for `elem.insert(pos+1, p)`
  spelled out in HLD §3c — Python's insert is element-index-based; mdrcel's
  `insert_with_tail(parent, child, idx)` is all-children-index-based. Resolution:
  drain the tail first, then compute `child`'s all-children index, target `idx+1`.
- **All required dom-facade primitives already exist** in `src/readability/dom.rs`:
  `create_element`, `set_element_text`, `set_tail`, `insert_with_tail`, `tail`,
  `element_text`, `children`, `delete_with_tail_preserve_free`. No new dom
  surface required.
- **Algorithm empirically verified on 11 representative inputs** against the
  vendored Python interpreter — byte-equal to the HLD's expected test oracles.

Three open questions, all with design-agent recommendations and team-lead
sign-off:

- **O1 — Pinning policy:** push new `<p>`s onto `pinned`. **SIGNED OFF.**
  Defensive, matches M3 Stage 3-B precedent, O(1) cost.
- **O2 — Test-visibility seam:** bump `transform_misused_divs_into_paragraphs`
  to `pub(crate) fn`. **SIGNED OFF.** Minimal change; matches existing
  internal-API surface in the file; enables direct unit tests.
- **O3 — xml KPI direction:** **SIGNED OFF with strengthening.** Land if
  mean-positive AND xml regressors are selection-shift cases (mdrcel now
  agrees with Python on a different valid main-content choice). ANY case where
  the rescue port itself produces incorrect serialised output is a port bug
  to fix, not a tradeoff to accept. Implementation phase spot-checks 2-3
  xml regressors if they appear.

**HLD path:** `wrk_docs/2026.05.23 - HLD - M10 Phase 2C rescue pass.md`.

**HLD v1 signed off by team lead 2026-05-23 — implementation may proceed.**

### 2026-05-23 — Phase 2C implementation landed

Implementation agent (`a82b11321649b13f0`) returned a clean diff faithful to
the HLD. Files touched (4 total, +541/-31):

- `src/trafilatura/readability_fork.rs` — Pass 2 rescue body (lines 830-918),
  `pub(crate)` visibility bump, doc-comment rewrite, 6 new unit tests.
- `Cargo.toml` / `Cargo.lock` — patch bump `0.18.2` → `0.18.3`.

**Validation:**

- `cargo build` clean, 0 warnings.
- `cargo test --lib` — 1192 pass (6 new rescue-pass tests included).
- All 6 format gates 6/6 green; NO fixture flipped buckets.
- The at-risk format (xml) actually posted the LARGEST gain (+3.80).

**Fuzz held-out KPI delta (post-Phase-2C):**

| Format   | Pre-2C  | Post-2C | Lift   |
|----------|--------:|--------:|-------:|
| markdown |  78.80% |  82.40% |  +3.60 |
| txt      |  84.60% |  87.60% |  +3.00 |
| xml      |  80.60% |  84.40% |  +3.80 |

**Mean lift +3.47 abs — 3-4× the HLD's conservative +0.5-1.0 prediction.**
Same shape as Phase 1: the right fix had broader scope than triage suggested
(rescue-pass-missing was the locus for more records than the Phase 2B tracer's
8 sampled representatives revealed).

### 2026-05-23 — Phase 2C code review (team lead)

Ran `/code-reviewer` skill on the diff. Verdict: **APPROVED for commit**. No
required changes, three optional Nits (intermediate string allocation; defensive
`.expect` message; 6 tests vs HLD's 3 minimum), all non-blocking and judgment-
positive. Strengths: anchoring discipline (every Python step cited file:line),
load-bearing test design (combined-fixture exercises the reverse-iteration
invariant — a forward-iteration impl would fail it), defensive pin policy at
four sites.

**Phase 2C committed as `1b58079`** (M10 Phase 2C: port rescue pass; +3.47%
mean held-out KPI).

---

## M10 Close-out Summary

### Cumulative KPI lift

Pre-M10 → Post-Phase-2C:

| Format   | Pre-M10 | Phase 1  | Phase 2C | Total Lift |
|----------|--------:|---------:|---------:|-----------:|
| markdown |  69.80% |   78.80% |   82.40% |    +12.60  |
| txt      |  75.00% |   84.60% |   87.60% |    +12.60  |
| xml      |  71.40% |   80.60% |   84.40% |    +13.00  |
| **mean** |  72.07% |   81.33% |   84.80% |    **+12.73** |

From ~72% mean held-out byte-equivalence to ~85% in two commits. The remaining
~15% is the snowflake-per-page + parser-layer residue identified in the
Phase 2 triage.

### Commits landed (M10)

- `4fdcd46` — M10 Phase 1: port remove_control_characters; +9% mean lift.
- `1b58079` — M10 Phase 2C: port rescue pass; +3.47% mean lift.

### Apparatus

The Phase 1 fuzz harness (`tests/fuzz_diff.rs`, M9 Stage 4) and the 1500-page
working-slice / held-out-slice corpus (M9 Stage 3) are reusable for any future
KPI work. Both Phase 1 and Phase 2C lifts were measured against the same
500-page held-out substrate frozen at M10 start; the gains generalise (not
overfit to a working-slice triage substrate).

### Discipline observed

- HLD-first for both phases (Phase 1 + Phase 2C); both HLDs iterated through
  review feedback before sign-off.
- Anti-inversion (§7) honoured throughout: every Python claim cited
  file:line + verbatim quote; the agent caught one factual error in my
  Phase 2B brief (lxml `drop_tree()` is tail-preserving, not tail-dropping)
  and one factual error in my Phase 1 brief (no Cf/U+2063 diff-harness mask
  exists; the cleanup was gate allowlists not harness code).
- `/code-reviewer` skill run on both implementation diffs before commit.
- Honest negative results from triage and tracer were ratified (Hypothesis B
  was falsified empirically rather than blind-patched).

### Phase 3 status: NOT escalated

The Phase 2 triage's selection-phase ratio (58%) crossed the HLD's literal
≥50% gate, but the gate's spirit (specific algorithmic difference localized)
was not satisfied — the divergence traced to html5ever-vs-lxml parser
permissiveness, not to selection algorithm differences. Phase 3 would be a
parser-replacement milestone, not a selection-algorithm-fix milestone.

The metric question remains unresolved: ~286 of the pre-Phase-2C working-slice
divergences were pages where mdrcel and Python both picked valid main content
but a different valid one. Byte-equivalence rewards lxml-mimicry; a selection-
equivalence or text-similarity metric would not. This is a strategic question
for Arthur, separate from any specific patch.

### Outstanding follow-ups (deferred to future milestones)

- The 10,000-char length cap and `html.unescape` portions of Python's
  `metadata.clean_and_trim` (Phase 1 ported only the strip portion).
- Hypothesis C (SEO-spam tag leakage, ~47 records) — requires parser-layer
  investigation (html5ever foster-parenting vs lxml permissiveness).
- The "mdrcel MORE blank lines" sub-pattern from Phase 2B (~8 G5 records;
  text-vs-tail apportionment).
- The today_utc vs today_local TEI flake (M8 residual; ~1-fixture impact).
- The metric question itself.

### 2026-05-23 — Arthur chose: keep pushing for bigger lift

### 2026-05-23 — Phase 2D re-triage outcome: diminishing returns confirmed

Re-triage agent (`aded90463acd84e73`) re-ran the triage methodology on the
post-Phase-2C `divergences.jsonl` (440 working-slice records, down from 490
pre-Phase-2C). Report saved to `wrk_docs/2026.05.23 - REPORT - M10 Phase 2D
re-triage.md`.

**Headline finding: ZERO new HIGH-confidence patterns post-Phase-2C.** Same
verdict as Phase 2 triage. The rescue pass cleaned up the div-text-collapse
residue completely (~30-40 records eliminated from that signature class) but
revealed no new tractable cluster — the remaining 440 divergences trace to
the same root causes already documented in the Phase 2 triage (Hypothesis A
selection-phase; Hypothesis C parser-layer SEO-spam; unclassified per-page
residue).

**Selection-phase ratio post-2C: 57.7%** (down trivially from 58.4%) — the
rescue pass uniformly reduced both halves of the pile (selection-phase −11%;
structural −9%). Phase-3's literal ≥50% gate still clears; the spirit-of-the-
gate verdict (parser-layer, not selection-algorithm) is unchanged.

**No regressions from Phase 2C.** Six new small groups appeared post-2C
(notably `(xml,extra-element,'cell')` with 4 records — N1 — and
`(xml,extra-element,'head')` with 3 records), but inspection shows these
are previously-divergent records whose shape signature shifted to more
specific near-tag classification after the rescue pass. No new ground lost.
N1 actually surfaces Hypothesis C's `<td>`/`<th>` raw-tag signature in a
sharper form than was visible pre-2C — clarification not regression.

**Re-triage recommendation:** close M10 with `html.unescape` + length-cap
port as optional bounded fallback (Phase 1 deferral, single function, <1%
expected yield).

### 2026-05-23 — Team lead next step: Phase 2E html.unescape + length-cap port

Given Arthur's "keep pushing" directive and the diminishing-returns curve,
the next-best bounded session is closing the Phase 1 deferral: port Python's
full `metadata.clean_and_trim()` semantics — the 10,000-char length cap and
`html.unescape(value)` — into mdrcel's `clean_and_trim_metadata` helper.

Expected yield: realistically <1%, but the algorithm is verbatim-quoted in
Python source (`settings.py:288-298`), the integration point already exists
(`metadata.rs:1096-1120`, the helper Phase 1 introduced), and the work is
strictly bounded. Low risk; modest reward.

After Phase 2E, the natural next probe is the Hypothesis C parser-layer
investigation (5-page html5ever-vs-lxml probe to assess feasibility of the
SEO-spam tag-leakage fix). Information-gathering session, no immediate yield.

### 2026-05-23 — Phase 2E HLD v1 signed off

Design agent (`a97bfc81062c04523`) returned HLD v1. Substantive findings:

- **Zero new transitive crates.** `web_atoms = "=0.2.4"` is **already in
  mdrcel's Cargo.lock** as a transitive via html5ever → markup5ever. Promoting
  to a direct dep adds NOTHING to the build graph. Empirically verified by
  scratch-build (clean, 7.62s, PHF transitive chain already present at
  Cargo.lock line 347).
- **Entity table cardinality verified.** `web_atoms::NAMED_ENTITIES` has
  exactly 2231 entries — byte-equal to Python's `html.entities.html5` (also
  2231). PHF map shape `&'static str → (u32, u32)`; key omits leading `&`;
  second tuple element is `0` for single-codepoint entities.
- **Order is cap → unescape → strip,** lexically verified against
  `settings.py:295-298`. Load-bearing: a 10K slot ending with `&amp;` truncates
  mid-entity to literal `&am` (no decode happens after cap).
- **Edge cases probed via vendored Python interpreter:** longest-prefix
  fallback, Windows-1252 substitution, surrogate guard, invalid-codepoints
  empty return, all `_invalid_charrefs` table entries.
- **mdrcel already has TWO incomplete unescape helpers** (`output.rs:1027`,
  `readability/metadata.rs:714`). Phase 2E adds a THIRD that IS faithful;
  consolidation is out of scope for Phase 2E but flagged for future.

Three open questions, signed off:

- **Q1 — `_invalid_codepoints` representation:** `matches!` inline. SIGNED OFF.
  Dense ranges are idiomatic; matches CLAUDE.md "Simplicity is hard."
- **Q2 — `python_html_unescape` visibility:** `pub(crate)` (slight bump from
  agent's hedge — agent had asked "fn (private) is the right call?"). SIGNED
  OFF with bump. Future consolidation of mdrcel's three unescape helpers
  needs this visibility; trivial cost now.
- **Q3 — Extend version-bump fence to cover `web_atoms`:** YES. SIGNED OFF.
  Algorithm-correctness dep; semver-minor bumps that change the entity
  table could silently change byte output. Add to the existing
  html5ever/markup5ever/tendril fence in `Cargo.toml`.

**HLD path:** `wrk_docs/2026.05.23 - HLD - M10 Phase 2E html unescape and length cap.md`.

**HLD v1 signed off by team lead 2026-05-23 — implementation may proceed.**

### 2026-05-23 — Phase 2E implementation landed (commit `b25866c`)

Implementation agent (`a383c6a7eaed5a66f`) returned a clean diff. Files
touched (6 total, +2321/-30 including HLD/journal):

- `src/trafilatura/metadata.rs` — `cap_at_python_length`,
  `python_html_unescape` (`pub(crate)`), `lookup_named_entity`,
  `invalid_charref_replacement` (35-arm match), `is_invalid_codepoint`
  (`matches!` inline), extended `clean_and_trim_metadata` body,
  23 new unit tests.
- `Cargo.toml` — `web_atoms = "=0.2.4"` direct dep (zero new transitive
  crates), extended version-bump fence to cover `web_atoms`, patch
  bump `0.18.3` → `0.18.4`.
- `Cargo.lock` — `web_atoms` promoted to direct dep entry.

**Material correctness finding the HLD missed:**
`web_atoms::NAMED_ENTITIES` has 9854 entries — NOT 2231 as the HLD's
empirical probe claimed. The extra 7766 are PREFIX SENTINELS stored with
value `(0, 0)` along the HTML5 entity trie. Python's
`html.entities.html5` has no such sentinels. Without mitigation, the
longest-prefix descent would match e.g. `"am"` as a valid entity
returning `(0, 0)` and produce 0-char output, swallowing the `&am`
literal. Discovered empirically via unit test failure. Mitigation:
`lookup_named_entity` treats `(cp1=0, cp2=0)` as "not a real entity" —
safe because no real HTML5 entity decodes to U+0000 and the only
`_invalid_charrefs[0x00]` substitution is on the numeric path.

**Validation:**

- cargo build clean, 0 warnings; cargo clippy --lib clean.
- cargo test --lib: 1215 pass, 0 fail (23 new tests).
- All 6 format gates 6/6 green; zero fixture bucket flips.
- Fuzz held-out KPI: **md/txt/xml all UNCHANGED at 82.40/87.60/84.40**.
  Exact no-op on the wild corpus, as the HLD predicted. The port is
  anti-divergence insurance: future Python changes propagate cleanly;
  future corpora with HTML-entity-heavy metadata WILL decode correctly.

**Phase 2E committed as `b25866c`** (M10 Phase 2E: port html.unescape +
10K cap; closes Phase 1 deferral).

### 2026-05-23 — Phase 2F parser probe outcome

Probe agent (`a8bf9e76b341f9106`) ran 5-representative side-by-side
parser comparison (mdrcel html5ever vs Python lxml) on SEO-spam fixtures
(Hypothesis C). Report saved to `wrk_docs/2026.05.23 - REPORT - M10
Phase 2F parser probe.md`.

**Headline findings:**

- **Parsers DISAGREE on 4/5 reps.** mdrcel's html5ever drops
  `<td>`/`<tr>`/`<th>`/`<tbody>`/`<tfoot>` when they appear outside
  `<table>` context (HTML5 spec compliance: "in body" insertion mode
  ignores stray table-cell tokens). Plus raw-text elements like
  `<xmp>` swallow content. lxml's HTML parser keeps the stray tags
  as children of whatever element is open.
- **html5ever has NO knob to relax this.** `TreeBuilderOpts` exposes
  `exact_errors`, `scripting_enabled`, `iframe_srcdoc`, `drop_doctype`,
  `ignore_missing_rules`, `quirks_mode` — none affects the
  table-context rules.
- **HOWEVER: the SEO-spam leakage primary cause is pipeline-scoping,
  not parser-layer.** The agent empirically verified that
  `prune_unwanted_nodes(body, OVERALL_DISCARD_XPATH, true)` removes
  100% of spam containers across all 5 reps when invoked on the full
  body. The xpath plumbing IS correct; the filter DOES fire. But it
  isn't reaching the spam div in the actual pipeline — likely because
  `BODY_XPATH`'s sub-tree scope and/or `compare_extraction`'s
  readability-arm re-parse misses it.

**Confidence-rated recommendation: MEDIUM-confidence tractable** with
two §7 caveats:

1. The bounded fix would be "ensure compare_extraction's readability
   arm applies the same `display:none` filter Python does in
   `focus=Balanced`." This requires another tracer round to pin
   exactly which pipeline boundary the leak crosses, AND a §7
   verification that Python actually runs the equivalent filter at
   that point.
2. Raw-text `<xmp>` residue (~4 records) is unaddressable without
   HTML-byte pre-processing, which itself violates §7.

**Estimated yield ranges:**

- Bounded fix only (pipeline-scoping): +0.4 to +1.0% KPI (~14 records).
- Bounded + `<xmp>` pre-processing: +1.2 to +1.7% KPI (~18-25 records).
- Defer (close M10): 0% additional lift; cumulative M10 stays at
  +12.73% mean.

**Open question for the team lead: spawn Phase 2G (tracer + fix
attempt, 1-2 sessions, MEDIUM-confidence) or close M10 here?**

### 2026-05-24 — Arthur chose: Phase 2G tracer + fix if §7 clean

### 2026-05-24 — Phase 2G tracer spawned

Tracer agent goal: pin exactly which pipeline boundary the SEO-spam leak
crosses in mdrcel; verify whether Python's pipeline applies the
equivalent filter at the same boundary. If yes → HLD + bounded fix. If
Python does NOT apply an equivalent filter → §7 escalation, stop and
report.

Canonical reproduction fixture: one of the 5 Phase 2F representatives.
Tracer inherits the existing `examples/m10_phase2f_parser_probe.rs` and
Python probe scripts as starting scaffolding.

### 2026-05-24 — Phase 2G outcome: (B') §7 escalation — STOP

Tracer agent (`a922b9e6f97926c2f`) returned **outcome class (B') —
parser-permissiveness divergence, §7 escalation needed**. Escalation
report saved to `wrk_docs/2026.05.24 - REPORT - M10 Phase 2G escalation.md`.

**Per autonomous-overnight discipline: STOP. Do not implement.**
Arthur is asleep; §7 ratification requires the human supervisor.

**Headline finding (HIGH-confidence diagnosis, empirically traced):**

The Phase 2F "MEDIUM-confidence tractable / sub-tree-scoping" hypothesis
is **empirically refuted**. The leak boundary is not a missing filter at
all — it's `compare_extraction` → `try_readability`'s readability
scoring on the un-pruned backup tree (mdrcel `readability_fork.rs:1988`;
Python `external.py:58`). Both pipelines reach this boundary identically:

- `OVERALL_DISCARD_XPATH` is precision-only in Python
  (`external.py:54-55`), and mdrcel mirrors this exactly.
- Both arms pass the un-pruned backup tree into the readability arm.
- Both arms have `own_text == 0`; the Branch-2 arbiter fires identically.

The OUTPUT divergence comes entirely from `readability_lxml.Document.
summary()` picking different best candidates on the two trees:

- lxml's permissive parse → `<section class="container">` wins
  (legitimate content has enough scorable mass).
- html5ever's HTML5-spec-compliant parse → `<div class="pl_css_ganrao">`
  wins (spam div has higher scoring text content because section
  content gets stripped + `<xmp>` inner becomes text).

**Python's `readability_lxml` has NO explicit `display:none` /
`pl_css_ganrao` / `hidden` filter** (verbatim REGEXES quoted in
escalation report §3). Byte parity on Python's side is COINCIDENTAL —
the result of parser-permissiveness + scoring math, NOT an equivalent
filter mdrcel could mirror.

Any mdrcel-only filter (body-level OVERALL_DISCARD pre-pass; HTML byte
pre-processing; new readability-stage filter) would be a §7 deviation
requiring Arthur's ratification. Autonomous mode does not have that
authority.

**Decision: close Phase 2G with escalation; pivot to other bounded
work.** Hypothesis C is confirmed as parser-layer-deep (Phase 2F was
right that parsers disagree; Phase 2F's secondary hypothesis about
sub-tree scoping was the red herring). Tracked for a future
parser-replacement milestone (M11 candidate).

**Phase 2F report supersession note:** Phase 2F §5/§6 sub-tree-scoping
hypothesis is now empirically refuted. The Phase 2F report is preserved
for historical context; the supersession is recorded here and in the
Phase 2G escalation report.

### 2026-05-24 — Pivoting to "mdrcel MORE blank lines" investigation

Per autonomous overnight plan (no §7 escalation; no Arthur in loop):
the next bounded tractable candidate is Phase 2B's deferred other-half
sub-pattern — "mdrcel MORE blank lines than oracle" (~8 G5 records,
text-vs-tail apportionment). Phase 2B reported this as MEDIUM-confidence
with dissolution risk (multiple sub-loci). After Phase 2C's rescue pass,
the post-2D triage may have reshaped this; the post-Phase-2C state needs
inspection before committing to a fix attempt.

**Autonomous stop conditions (self-imposed):**

- If two consecutive phases produce no fix-commit → close M10.
  (Phase 2G already produced no fix-commit; one more strike and stop.)
- If next bounded session's expected yield is <+0.3% → close M10.
- Never make §7-deviating changes without Arthur's ratification.
